---
context: default
phase: FL-files-sidebar
task: 4
total_tasks: 4
status: in_progress
last_updated: 2026-07-15T00:41:13.010Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Trash access — the restricted sandbox makes system-Trash delete/recovery tests fail even when the product behavior is correct; run the full gate with normal macOS permissions.
- [ ] CONSTRAINT: isolated worktree — all FL-01 work belongs in `/private/tmp/slate-fl-01-derived-metadata`; do not modify or clean the user's original `/Users/coryj/Dev/slate` working tree.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Treat sandbox Trash denial as a product failure | The restricted `make ci` attempt produced broad delete/recovery failures because macOS Trash was inaccessible; the same paths passed with normal access. | blocking | Run Trash-dependent/full CI with normal macOS permissions and judge the elevated result. |
| Restart benchmark archaeology | Final symmetric benchmark evidence, source hashes, amendment, and review are already recorded and approved. Overlapping or unsettled Criterion runs were explicitly excluded. | advisory | Do not rerun performance measurements unless implementation code changes; read `BENCHMARKS.md` first. |
| Touch the original workspace | The user's primary workspace has unrelated state. | blocking | Operate only in the named FL-01 worktree until this PR is merged. |

<current_state>

Milestone FL delivery is paused in PR slice FL-01 (issues #650–#652), after all
three implementation tasks were committed. Branch
`codex/fl-01-derived-metadata` is at
`e30f6782e0eb207d6398251921823b5a5a5d4fac` in
`/private/tmp/slate-fl-01-derived-metadata`.

The active elevated `make ci` process was stopped cleanly at the user's request
while `cargo bench --workspace --no-run` was compiling `slate-uniffi`. No test,
watcher, server, or other background process remains.

Before interruption, the same `make ci` run completed formatting, workspace
Clippy, and the full Rust test phase. The core aggregate was 1,460 passed, 0
failed, 2 ignored in 1040.30s; every Rust integration suite, UniFFI suite, and
doctest also passed. The command had not yet completed benchmark compilation or
the later binding/Swift gates, so do not call the full gate complete.

</current_state>

<completed_work>

Completed Tasks:

- FL-00 was merged as PR #920 (`f07d78e`).
- Task 1: derived `file_meta` projection, migration/backfill, scan/save hooks —
  commits `865cf4f`, `90d88ea`.
- Task 2: enriched file summaries, FFI/CLI/Swift civil-date contracts — commits
  `bbe5b83`, `6e66a60`.
- Task 3: deterministic censuses, bounded scan batching, exact Unicode tree
  paging, shared Markdown event projection, benchmarks, and the owner-approved
  save-gate amendment — commit `e30f678`.
- Independent Task-3 red-team verdict: `APPROVED`.
- Independent benchmark-document audit approved the final arithmetic,
  provenance, power-state qualification, source hashes, and amendment wording.
- Post-commit targeted evidence: full release census 3/3 in 39.28s; metadata
  29/29; links 42/42; strict Clippy; fmt; benchmark no-run; clean diff/status.

</completed_work>

<remaining_work>

- Resume and complete the authoritative full CI gate with normal macOS access.
- Spawn a fresh cumulative FL-01 branch red-team over
  `f07d78eddf0215e510c2f9beaa0377097dd80f5c..HEAD`; fix and re-review until
  `APPROVED`.
- Fetch current `origin/main`; rebase/range-diff if it moved, then obtain a
  post-rebase review if needed.
- Push `codex/fl-01-derived-metadata` and open the ready PR closing #650, #651,
  and #652.
- Have an independent agent poll CI and PR/Codoki feedback every 90 seconds;
  fix every failure/comment, re-review fixes, and continue until all checks are
  green and Codoki explicitly says safe to merge. Only then merge.
- Continue the serial milestone train with FL-02 through FL-15.

</remaining_work>

<decisions_made>

- Owner-approved 2026-07-14 save-gate amendment: order-balanced geometric-p50
  additive overhead must be ≤0.5 ms at 1k, 10k, and 50k; metadata work remains
  O(changed-file), existing O(N) growth is not worsened, and feature/derivation
  scope is unchanged.
- Preserve the reliable existing vault-index rebuild rather than add a risky
  late cache solely to force a literal zero-regression reading.
- Keep one authoritative Markdown parser/event stream; do not introduce a
  second link grammar.
- Keep exact NFC + full Unicode lowercase + binary-path tie ordering in SQLite
  before page enrichment.

</decisions_made>

<blockers>

- Human pause only: the user is changing locations. There is no technical
  blocker and no process left running.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable resume point and exact next action.
2. `/private/tmp/slate-fl-01-sdd/task-3-report.md` — committed Task-3 results and review history.
3. `docs/plans/15_files_sidebar/specs/fl0_spec.md` — governing FL-01 contract and approved amendment.
4. `BENCHMARKS.md` — final release evidence and excluded-run rationale.
5. `docs/superpowers/plans/2026-07-14-milestone-fl-delivery.md` — complete PR train and release gates.

## Critical Anti-Patterns (do NOT repeat these)

- Run full CI in the restricted sandbox and interpret system-Trash denial as a
  regression → use normal macOS permissions for the authoritative run.
- Overlap Criterion processes or omit the 90-second settle → existing final
  performance evidence is already approved; only rerun if implementation
  changes, using the recorded symmetric protocol.
- Push before cumulative independent approval → the user requires red-team
  approval before every push.
- Merge on green CI alone → Codoki must explicitly say safe to merge, with no
  unresolved comments, before merge permission exists.

## Infrastructure State

- Active worktree: `/private/tmp/slate-fl-01-derived-metadata`.
- Active branch: `codex/fl-01-derived-metadata`.
- HEAD: `e30f6782e0eb207d6398251921823b5a5a5d4fac`.
- Base: merged FL-00 `f07d78eddf0215e510c2f9beaa0377097dd80f5c`.
- No background test/server process remains; `make ci` exited 130 by explicit
  user-requested interrupt during `bench-check`.
- External SDD handoff: `/private/tmp/slate-fl-01-sdd/task-3-report.md`.

<context>

FL-01 is implementation-complete and independently approved at the Task-3
level. The only unfinished local gate is the tail of full CI plus the cumulative
branch review. Preserve the evidence already gathered; resume from verification,
not from implementation or benchmarking.

</context>

<next_action>

Start with: confirm no process is running and `git status --short` is clean in
`/private/tmp/slate-fl-01-derived-metadata`, then rerun `make ci` with normal
macOS permissions. After it passes, request the fresh cumulative FL-01 red-team.

</next_action>
