// Copyright (C) 2026 Cory Joseph
// SPDX-License-Identifier: AGPL-3.0-or-later

import XCTest

@testable import SlateMac

/// §W-D parity census (W0.5-3, #719): the mac half of the canonical
/// a11y-event corpus check. Constructs every representative event from
/// `slate_core::a11y::corpus()` in the SAME order, renders each through
/// the FFI (`a11yRender` — the exact path `postAccessibilityAnnouncement(
/// _ event:)` posts through), and asserts (priority, text) against the
/// committed artifact `tests/fixtures/a11y/corpus.json`. Each entry's
/// `event` field (core's Debug identity, via `a11yEventIdentity`) is
/// asserted too, so the mirror must construct the SAME semantic event —
/// not merely one that happens to render identical text. The Windows
/// census (`A11yCorpusCensus`, #1114) consumes the same file, which is
/// what makes the corpus the cross-platform anchor: if this test and its
/// Windows twin are green, both hosts speak identical announcements for
/// identical events. The two mirrors are maintained in lockstep.
///
/// Corpus changes are DELIBERATE: regenerate the artifact core-side
/// (`SLATE_REGENERATE_FIXTURES=1 cargo test -p slate-core a11y` — that
/// run fails by design after rewriting; re-run clean to prove the pin),
/// update the mirrored construction below, and review the diff as a
/// §W-D delta.
final class A11yCorpusCensusTests: XCTestCase {

    private struct CorpusEntry: Decodable {
        let event: String
        let priority: String
        let text: String
    }

    /// Repo-root fixture path derived from this file's location — the first
    /// delete strips the filename, then four directory hops
    /// (SlateMacTests → Tests → slate-mac → apps) reach the repo root.
    private static var corpusURL: URL {
        URL(fileURLWithPath: #filePath)
            .deletingLastPathComponent()  // strips the filename → SlateMacTests
            .deletingLastPathComponent()  // Tests
            .deletingLastPathComponent()  // slate-mac
            .deletingLastPathComponent()  // apps
            .deletingLastPathComponent()  // repo root
            .appendingPathComponent("tests/fixtures/a11y/corpus.json")
    }

    /// The Swift mirror of `slate_core::a11y::corpus()` — same events,
    /// same sample values, same order.
    private static var corpus: [A11yEvent] {
        [
            .filesRegionFocused,
            .leafPanelShown(title: "Outline"),
            .editorPaneFocused(ordinal: 2, total: 3, title: "notes.md", prefix: ""),
            .tabFocused(prefix: "Now", filename: "notes.md", index: 1, count: 4),
            .tabClosed(closedTitle: "draft.md", successor: "notes.md"),
            .tabClosed(closedTitle: "draft.md", successor: nil),
            .noSplitPanesToResize,
            .paneResized(percent: 60),
            .graphOpensSinglePane,
            .rightPaneShown,
            .rightPaneHidden,
            .historyPanelShown,
            .reopenTargetMissing(filename: "gone.md"),
            .reopenedFile(filename: "notes.md"),
            .reopenedNamed(name: "Open tasks"),
            .reopenedGraph,
            .vaultOpened(vaultTitle: "Garden", sidebarNotice: ""),
            .removedRecentVault(displayName: "Garden"),
            .welcomeShown(recentVaultCount: 0),
            .welcomeShown(recentVaultCount: 1),
            .welcomeShown(recentVaultCount: 2),
            .commandPaletteNeedsVault,
            .searchNeedsVault,
            .searchResultsSummary(count: 0),
            .searchResultsSummary(count: 1),
            .searchResultsSummary(count: 7),
            .searchFailed(message: "the index is unavailable"),
            .searchResultOpened(filename: "notes.md", line: 12, snippet: "the quick brown fox"),
            .externalLinkUnsupported(target: "ftp://example.com"),
            .externalLinkOpened,
            .externalLinkFailed(target: "https://example.com"),
            .linkUnresolved(target: "Missing Note"),
            .helpOpened,
            .helpFailed,
            .internalNavigated(kind: "Opened", filename: "notes.md"),
            .citationNotLoaded,
            .noResolvedEmbedAtCursor,
            .noEmbedAtCursor,
            .headingNotFound,
            .headingScrollFailed(heading: "Roadmap"),
            .scrolledToHeading(heading: "Roadmap"),
            .scrolledToLine(filename: "notes.md", line: 40),
            .openedAtLine(filename: "notes.md", line: 40),
            .openedFile(filename: "notes.md"),
            .showingNote(displayName: "notes"),
            .taskToggleUnsaved(filename: "notes.md"),
            .taskToggleConflict(filename: "notes.md"),
            .tasksReviewShown(filterName: "Open tasks"),
            .tasksFilterSet(filterName: "All tasks"),
            .noteSaved(filename: "notes.md"),
            .saveConflict(filename: "notes.md"),
            .restoredVersionFrom(formattedDate: "July 19, 2026 at 9:41 AM"),
            .restoredFile(filename: "notes.md"),
            .restoredFileAs(sourceName: "notes.md", filename: "notes-restored.md"),
            .printNeedsNote,
            .printDialogOpened(name: "notes.md"),
            .batchCheckStarted(formattedCount: "1,024", actionName: "Move"),
            .selectionCopied,
            .sidebarSettingsStillDefaults(detail: "the file is malformed."),
            .sidebarSettingsReloadedStaleRefs,
            .sidebarSettingsReloaded,
            .vaultClosed,
            .vaultClosedAllSaved,
            .vaultClosedChangesDiscarded,
            .propertiesUpdated,
            .propertyChanged(key: "tags", deleted: false),
            .propertyChanged(key: "tags", deleted: true),
            .propertyEditConflict(filename: "notes.md"),
            .propertiesSourceRejected(reason: "the YAML does not parse"),
            .propertyEditFailed(detail: "io error"),
            .propertiesReloaded,
            .propertiesReloadedBodyChanged,
            .noteChangedAgain(detail: nil),
            .noteChangedAgain(detail: "The note changed while saving."),
            .propertiesReloadFailed(reason: "io error"),
            .propertyRetainedCopied,
            .propertyRecoveryUnverified(displayName: "notes"),
            .propertyRetainedDiscarded,
            .propertyRetainedReapplyFailed(detail: nil),
            .propertyReloadStillFailed(reason: "io error"),
            .propertyLoadCurrentFailed(reason: "io error"),
            .addPropertySheetShown,
            .sourceChangesDiscarded,
            .bulkRenameSheetShown,
            .renameReloadFailed(detail: nil),
            .renameFailed(detail: "io error"),
            .renameSummary(applied: true, renamed: 3, skipped: 1, failed: 0),
            .renameSummary(applied: false, renamed: 1, skipped: 0, failed: 0),
            .renameSummary(applied: false, renamed: 3, skipped: 2, failed: 0),
            .duplicateFilesOnly,
            .mathSpeechStyle(name: "ClearSpeak"),
            .mathVerbosity(name: "Verbose"),
            .mathBrailleCode(name: "Nemeth"),
            .codePreambleVerbosity(name: "Concise"),
            .editorTextSize(percent: 110),
            .spellCheckToggled(enabled: true),
            .spellCheckToggled(enabled: false),
            .citationStyleChanged(title: "APA"),
            .citationsCount(count: 1),
            .citationsCount(count: 3),
            .outlineCount(count: 1),
            .outlineCount(count: 5),
            .fileListCount(count: 1),
            .fileListCount(count: 12),
            .itemsSelected(count: 4),
            .itemsSelected(count: 1),
            .noItemsSelected,
            .treeFolderSelected(name: "Archive"),
            .rowSelected(name: "notes"),
            .switcherRecentCount(count: 2),
            .switcherRecentCount(count: 1),
            .switcherRecentCount(count: 0),
            .switcherNoMatches(query: "zzz"),
            .switcherMatchCount(count: 2, query: "foo"),
            .switcherMatchCount(count: 1, query: "foo"),
            .paletteCommandSelected(label: "Save", disabledReason: nil),
            .paletteCommandSelected(
                label: "Save",
                disabledReason: "A structural operation is in progress."
            ),
            .paletteFilterCount(count: 0, query: "zzz"),
            .paletteFilterCount(count: 1, query: "save"),
            .paletteFilterCount(count: 4, query: "e"),
            .paletteCommandFailed(label: "Save", detail: "disk full"),
            .paletteCommandFailed(label: "Save", detail: nil),
            .paletteCommandNotFound(id: "slate.nope"),
            .paletteCommandUnavailable(reason: "A structural operation is in progress."),
            .recentSearchFocused(query: "fox"),
            .quickSwitcherCount(count: 2, query: nil),
            .quickSwitcherCount(count: 1, query: nil),
            .quickSwitcherCount(count: 2, query: "foo"),
            .quickSwitcherCount(count: 1, query: "foo"),
            .quickSwitcherCount(count: 0, query: "zzz"),
            .baseViewMode(mode: "cards"),
            .baseViewSwitcher(viewCount: 1),
            .baseViewSwitcher(viewCount: 2),
            .basesNewQueryBuilder,
            .basesEditingFilters(viewName: "Table"),
            .basesFiltersOpenFailed(detail: "io error"),
            .basesPreviewFailed(detail: "bad expression"),
            .basesBuilderSaved,
            .basesViewSaveFailed(detail: "io error"),
            .basesSavedQueryNameNeeded,
            .basesSavedQueryCreated(name: "Open tasks"),
            .basesSavedQueryCreateFailed(detail: "io error"),
            .basesSavedQueryUpdated(name: "Open tasks"),
            .basesSavedQueryUpdateFailed(detail: "io error"),
            .basesViewSelected(name: "Cards"),
            .basesSortSaveFailed(detail: "io error"),
            .baseRefreshed,
            .baseWhereAmI(base: "Reading", view: nil, quickFilter: nil),
            .baseWhereAmI(base: "Reading", view: "Table", quickFilter: nil),
            .baseWhereAmI(base: "Reading", view: "Table", quickFilter: "CAFE"),
            .baseResultsPopover(audioSummary: "12 results.", whereAmI: nil),
            .baseResultsPopover(
                audioSummary: "12 results.",
                whereAmI: "Base: Reading, quick filter: CAFE"),
            .baseQuickFilterResult(shown: 0, total: 0),
            .baseQuickFilterResult(shown: 1, total: 1),
            .baseQuickFilterResult(shown: 1, total: 2),
            .baseRowReorderRefused(label: "Sort 1"),
            .baseRowReorderAtBoundary(label: "Sort 1", atFirst: true),
            .baseRowReorderAtBoundary(label: "Sort 2", atFirst: false),
            .baseRowReorderMoved(label: "Sort 1", movedUp: false, position: 2, count: 3),
            .baseRowReorderMoved(label: "Status column", movedUp: true, position: 1, count: 3),
            .baseQueryPreviewIdle,
            .baseQueryPreviewLoading,
            .baseQueryPreviewReady(audioSummary: "12 results.", firstResult: nil),
            .baseQueryPreviewReady(audioSummary: "12 results", firstResult: "Alpha"),
            .baseQueryPreviewReady(audioSummary: "12 results.", firstResult: "Alpha"),
            .baseQueryPreviewFailed(detail: "invalid expression"),
            .baseSortedByColumn(column: "Status", ascending: true),
            .baseSortedByColumn(column: "Status", ascending: false),
            .baseSortSavedToView(column: "Status", ascending: true),
            .baseSortSavedToView(column: "Status", ascending: false),
            .basesSavedQueryReferenceMissing(reference: "Open tasks"),
            .basesSavedQueryMissing,
            .basesQueriesRefreshFailed(detail: "io error"),
            .basesSavedQueryEditing(name: "Open tasks"),
            .basesSavedQueryEditFailed(detail: "io error"),
            .basesSavedQueryRenameNameNeeded,
            .basesSavedQueryRenamed(name: "Open tasks"),
            .basesSavedQueryRenameFailed(detail: "io error"),
            .basesSavedQueryDeleted,
            .basesSavedQueryDeleteFailed(detail: "io error"),
            .basesSavedQueryExportPathNeeded,
            .basesSavedQueryExported(name: "Open tasks.base"),
            .basesSavedQueryExportFailed(detail: "io error"),
            .basesPathOutsideVault,
            .basesDashboardNameNeeded,
            .basesDashboardSaved(name: "Reading"),
            .basesDashboardSaveFailed(detail: "io error"),
            .basesDashboardUpdated(name: "Reading"),
            .basesDashboardUpdateFailed(detail: "io error"),
            .basesDashboardSectionStale,
            .basesDashboardSectionRemoveFailed(detail: "io error"),
            .basesDashboardSectionReplaceFailed(detail: "io error"),
            .basesDashboardDeleted,
            .basesDashboardDeleteFailed(detail: "io error"),
            .basesDashboardEditFailed(detail: "io error"),
            .basesDashboardMissing,
            .basesDockUpdatedForNote,
            .basesLinkCopied(name: "Reading"),
            .basesBacklinksFor(name: "Reading"),
            .basesViewCopyNoActiveBase,
            .basesViewCopiedAsMarkdown,
            .basesViewCopyFailed(detail: "io error"),
            .basesRowSelectionNeeded,
            .basesNoEditableProperty,
            .basesCellReadOnly(fileMetadata: true),
            .basesCellReadOnly(fileMetadata: false),
            .basesCellSaved(column: "Status", value: "Done"),
            .basesCellCleared(column: "Status"),
            .basesCellRowNoLongerMatches,
            .basesCellEditFailed(detail: "io error"),
            .basesCellEditCanceled,
            .basesViewExported,
            .basesViewExportFailed(detail: "io error"),
            .basesDataviewConverted,
            .basesDataviewConversionSaveFailed(detail: "io error"),
            .basesQuickFilterChoiceCanceled(verb: "Export"),
            .basesCellMustBeFiniteNumber,
            .basesCellMustBeWholeNumber,
            .basesCellMustBeFiniteDecimal,
            .basesCellMustBeBoolean,
            .basesCellMustBeDate,
            .basesRefreshUpdated(audioSummary: "1 note."),
            .dataviewConversionFailed(detail: "unsupported query"),
            .citationInsertUnavailable,
            .citationWalkThrough,
            .codeCopied,
            .readingNavNoTarget(target: .heading, forward: true),
            .readingNavNoTarget(target: .headingLevel(level: 2), forward: false),
            .readingNavNoTarget(target: .link, forward: true),
            .readingNavNoTarget(target: .list, forward: false),
            .readingNavNoTarget(target: .table, forward: true),
            .readingNavNoTarget(target: .embed, forward: false),
            .readingNavNoTarget(target: .codeBlock, forward: true),
            .readingNavLanded(target: .headingLevel(level: 2), text: "Lists and tasks"),
            .readingNavLanded(target: .link, text: "Target Note"),
            .readingNavLanded(target: .list, text: "first bullet"),
            .readingNavLanded(target: .table, text: "column a"),
            .readingNavLanded(target: .embed, text: "Embedded note Target Note"),
            .readingNavLanded(target: .codeBlock, text: "fn spoken_interior() -> usize { 42 }"),
            .readingNavNoTarget(target: .math, forward: true),
            .readingNavLanded(
                target: .math,
                text: "x equals negative b plus or minus the square root of b squared minus 4 a c, over 2 a"),
            .readingNavNoTarget(target: .diagram, forward: true),
            .readingNavLanded(target: .diagram, text: "Flowchart with 3 steps"),
            .readingNavLanded(target: .embed, text: ""),
            .gridSorted(column: "Status", ascending: true),
            .gridSorted(column: "Due", ascending: false),
            .gridRowMoved(
                description: "Ship the plan. Status: Open. Due: Friday",
                focusedCell: "Status: Open"),
            .gridRowMoved(description: "Ship the plan", focusedCell: "Status: Open"),
            .gridRowMoved(description: "Done reviewing.", focusedCell: "Status: Open"),
            .gridRowMoved(description: "Substatus: Opening", focusedCell: "Status: Open"),
            .gridRowMoved(
                description: "Status: Open Questions remain",
                focusedCell: "Status: Open"),
            .gridCellMoved(column: "Status", value: "Open"),
            .gridGroup(label: "Open", rowCount: 1, summary: nil),
            .gridGroup(label: "Done", rowCount: 12, summary: "Count: 12"),
            .templatePickerOpened(count: 0),
            .templatePickerOpened(count: 1),
            .templatePickerOpened(count: 7),
            .templateNoteCreated(name: "Meeting 2026-08-20.md", template: "Meeting"),
            .hostComposed(text: "Composed by a host engine.", priority: .high),
            // --- Canvas (W6-1 0a, #745) ---
            .canvas(event: .canvasMovedTo(verbosity: .terse, kindLabel: "text", title: "Research", ordinalN: 2, totalM: 5, container: "Q3", connectionCount: 3, colorName: "red", marked: true)),
            .canvas(event: .canvasMovedTo(verbosity: .standard, kindLabel: "text", title: "Research", ordinalN: 2, totalM: 5, container: "Q3", connectionCount: 3, colorName: "red", marked: true)),
            .canvas(event: .canvasMovedTo(verbosity: .verbose, kindLabel: "text", title: "Research", ordinalN: 2, totalM: 5, container: "Q3", connectionCount: 3, colorName: "red", marked: true)),
            .canvas(event: .canvasMovedTo(verbosity: .standard, kindLabel: "group", title: "Q3", ordinalN: 1, totalM: 3, container: nil, connectionCount: 0, colorName: nil, marked: false)),
            .canvas(event: .canvasMovedTo(verbosity: .verbose, kindLabel: "file", title: "Notes.md", ordinalN: 1, totalM: 1, container: nil, connectionCount: 1, colorName: nil, marked: false)),
            .canvas(event: .canvasGroupEntered(label: "Q3", count: 4)),
            .canvas(event: .canvasGroupEntered(label: "Solo", count: 1)),
            .canvas(event: .canvasGroupLeft(label: "Q3")),
            .canvas(event: .canvasViewportNoPane),
            .canvas(event: .canvasConnectionTraversed(direction: .outgoing, kindLabel: "text", title: "Ideas", label: "supports")),
            .canvas(event: .canvasConnectionTraversed(direction: .incoming, kindLabel: "text", title: "Research", label: nil)),
            .canvas(event: .canvasConnectionTraversed(direction: .undirected, kindLabel: "group", title: "Q3", label: nil)),
            .canvas(event: .canvasConnectionTraversed(direction: .bidirectional, kindLabel: "link", title: "example.com", label: nil)),
            .canvas(event: .canvasTracePathEnd(titles: ["Research", "Ideas", "Draft"])),
            .canvas(event: .canvasMoveRelative(descs: [], overlap: nil)),
            .canvas(event: .canvasMoveRelative(descs: [.below(anchorTitle: "Research")], overlap: nil)),
            .canvas(event: .canvasMoveRelative(descs: [.below(anchorTitle: "Research"), .rightOf(anchorTitle: "Ideas")], overlap: .onset)),
            .canvas(event: .canvasMoveRelative(descs: [.above(anchorTitle: "Ideas")], overlap: .cleared)),
            .canvas(event: .canvasResizeGeometry(preset: nil, width: 320, height: 200, overlap: nil)),
            .canvas(event: .canvasResizeGeometry(preset: .defaultSize, width: 260, height: 140, overlap: nil)),
            .canvas(event: .canvasResizeGeometry(preset: .fitToContent, width: 260, height: 88, overlap: .onset)),
            .canvas(event: .canvasResizeClamped),
            .canvas(event: .canvasModeEntered(mode: .move, object: .card(title: "Research"))),
            .canvas(event: .canvasModeEntered(mode: .move, object: .cards(count: 3))),
            .canvas(event: .canvasModeEntered(mode: .resize, object: .card(title: "Research"))),
            .canvas(event: .canvasModeEntered(mode: .connect, object: .card(title: "Research"))),
            .canvas(event: .canvasModeRejected(activeMode: .move)),
            .canvas(event: .canvasModeCommitted(verb: .move, object: .card(title: "Research"))),
            .canvas(event: .canvasModeCommitted(verb: .move, object: .cards(count: 3))),
            .canvas(event: .canvasModeCommitted(verb: .resize, object: .card(title: "Research"))),
            .canvas(event: .canvasModeEndedWithoutEffect(mode: .move)),
            .canvas(event: .canvasModeEndedWithoutEffect(mode: .resize)),
            .canvas(event: .canvasModeEndedWithoutEffect(mode: .connect)),
            .canvas(event: .canvasModeCancelled(mode: .move, restoration: .unstated)),
            .canvas(event: .canvasModeCancelled(mode: .move, restoration: .cardsReturned(count: 1))),
            .canvas(event: .canvasModeCancelled(mode: .move, restoration: .cardsReturned(count: 3))),
            .canvas(event: .canvasModeCancelled(mode: .resize, restoration: .unstated)),
            .canvas(event: .canvasModeCancelled(mode: .resize, restoration: .sizeRestored)),
            .canvas(event: .canvasModeCancelled(mode: .connect, restoration: .unstated)),
            .canvas(event: .canvasModeCancelled(mode: .connect, restoration: .backAt(title: "Research"))),
            .canvas(event: .canvasCreated(kindLabel: "text", title: "New idea", relative: .below(anchorTitle: "Research"))),
            .canvas(event: .canvasCreated(kindLabel: "group", title: "Q3", relative: .rightOf(anchorTitle: "Research"))),
            .canvas(event: .canvasCreated(kindLabel: "file", title: "Notes.md", relative: .above(anchorTitle: "Research"))),
            .canvas(event: .canvasCreated(kindLabel: "link", title: "example.com", relative: .leftOf(anchorTitle: "Research"))),
            .canvas(event: .canvasCreated(kindLabel: "text", title: "Untitled", relative: .atOrigin)),
            .canvas(event: .canvasFileCreated(name: "Roadmap")),
            .canvas(event: .canvasConnectedCardCreated(relative: .below(anchorTitle: "Research"), originTitle: "Research")),
            .canvas(event: .canvasConnected(fromTitle: "Research", toTitle: "Ideas", label: "supports")),
            .canvas(event: .canvasConnected(fromTitle: "Research", toTitle: "Ideas", label: nil)),
            .canvas(event: .canvasConnectionUpdated(label: "supports")),
            .canvas(event: .canvasConnectionUpdated(label: nil)),
            .canvas(event: .canvasMovedIntoGroup(label: "Q3")),
            .canvas(event: .canvasRemovedFromGroup(label: "Q3")),
            .canvas(event: .canvasColorSet(title: "Research", color: .preset(preset: 1))),
            .canvas(event: .canvasColorSet(title: "Research", color: nil)),
            .canvas(event: .canvasRenamedGroup(label: "Q3")),
            .canvas(event: .canvasCardUpdated(title: "Research")),
            .canvas(event: .canvasCardRetargeted(title: "Research", path: "notes/research.md")),
            .canvas(event: .canvasCardPlaced(verb: .moved, title: "Research", relative: .below(anchorTitle: "Ideas"))),
            .canvas(event: .canvasCardPlaced(verb: .duplicated, title: "Research", relative: .rightOf(anchorTitle: "Research"))),
            .canvas(event: .canvasCardAligned(title: "Research", targetTitle: "Ideas")),
            .canvas(event: .canvasConvertedToNote(path: "notes/research.md")),
            .canvas(event: .canvasDeleted(target: .card(kindLabel: "text", title: "Research"), verbosity: .standard, undoChord: "⌘Z")),
            .canvas(event: .canvasDeleted(target: .card(kindLabel: "text", title: "Research"), verbosity: .terse, undoChord: "⌘Z")),
            .canvas(event: .canvasDeleted(target: .group(label: "Q3"), verbosity: .standard, undoChord: "⌘Z")),
            .canvas(event: .canvasDeleted(target: .cards(count: 3), verbosity: .standard, undoChord: "Ctrl+Z")),
            .canvas(event: .canvasDeleted(target: .cards(count: 1), verbosity: .verbose, undoChord: "⌘Z")),
            .canvas(event: .canvasDeleted(target: .connection(direction: .outgoing, otherTitle: "Ideas", label: "supports"), verbosity: .standard, undoChord: "⌘Z")),
            .canvas(event: .canvasDeleted(target: .connection(direction: .incoming, otherTitle: "Research", label: nil), verbosity: .terse, undoChord: "⌘Z")),
            .canvas(event: .canvasDeleted(target: .connection(direction: .undirected, otherTitle: "Q3", label: nil), verbosity: .standard, undoChord: "⌘Z")),
            .canvas(event: .canvasBulkMoved(count: 3, relative: .below(anchorTitle: "Research"))),
            .canvas(event: .canvasBulkColorSet(count: 3, color: .preset(preset: 5))),
            .canvas(event: .canvasBulkColorSet(count: 1, color: nil)),
            .canvas(event: .canvasGrouped(count: 3, label: "Q3")),
            .canvas(event: .canvasBulkDuplicated(count: 2)),
            .canvas(event: .canvasMarkToggled(marked: true, title: "Research", count: 2)),
            .canvas(event: .canvasMarkToggled(marked: false, title: "Research", count: 1)),
            .canvas(event: .canvasMarksCleared(count: 0)),
            .canvas(event: .canvasMarksCleared(count: 3)),
            .canvas(event: .canvasFilterCount(matched: 3)),
            .canvas(event: .canvasFilterCount(matched: 1)),
            .canvas(event: .canvasFilterCleared(total: 40)),
            .canvas(event: .canvasFilterCleared(total: 1)),
            .canvas(event: .canvasZoom(context: nil, percent: 100)),
            .canvas(event: .canvasZoom(context: .fitCanvas, percent: 80)),
            .canvas(event: .canvasZoom(context: .zoomedToSelection, percent: 150)),
            .canvas(event: .canvasFollowSelectionToggled(following: true)),
            .canvas(event: .canvasFollowSelectionToggled(following: false)),
            .canvas(event: .canvasSurfaceShown(surface: .outline)),
            .canvas(event: .canvasSurfaceShown(surface: .table)),
            .canvas(event: .canvasSurfaceShown(surface: .visual)),
            .canvas(event: .canvasHistoryApplied(verb: .undo, name: "move \"Research\"")),
            .canvas(event: .canvasHistoryApplied(verb: .redo, name: "move \"Research\"")),
            .canvas(event: .canvasUndoMenuTitle(verb: .undo, name: "delete \"My Card\"")),
            .canvas(event: .canvasUndoMenuTitle(verb: .redo, name: "")),
            .canvas(event: .canvasStatus(note: .nothingSelected)),
            .canvas(event: .canvasStatus(note: .noMarks)),
            .canvas(event: .canvasStatus(note: .notAGroup)),
            .canvas(event: .canvasStatus(note: .notATextCard)),
            .canvas(event: .canvasStatus(note: .notAFileCard)),
            .canvas(event: .canvasStatus(note: .noGroups)),
            .canvas(event: .canvasStatus(note: .noNotesInVault)),
            .canvas(event: .canvasStatus(note: .noMediaInVault)),
            .canvas(event: .canvasStatus(note: .noFilesToPointAt)),
            .canvas(event: .canvasStatus(note: .onlyTextCardsConvert)),
            .canvas(event: .canvasStatus(note: .noConnections)),
            .canvas(event: .canvasStatus(note: .pickOutsideMovingSet)),
            .canvas(event: .canvasStatus(note: .pickDifferentTarget)),
            .canvas(event: .canvasStatus(note: .noChanges)),
            .canvas(event: .canvasStatus(note: .notReadable)),
            .canvas(event: .canvasStatus(note: .empty)),
            .canvas(event: .canvasStatus(note: .endOfCanvas)),
            .canvas(event: .canvasStatus(note: .startOfCanvas)),
            .canvas(event: .canvasStatus(note: .atCanvasLevel)),
            .canvas(event: .canvasStatus(note: .noCardsMatchFilter)),
            .canvas(event: .canvasStatus(note: .nothingToUndo)),
            .canvas(event: .canvasStatus(note: .nothingToRedo)),
            .canvas(event: .canvasStatus(note: .groupIsEmpty(label: "Q3"))),
            .canvas(event: .canvasStatus(note: .noOutgoingPath(title: "Research"))),
            .canvas(event: .canvasStatus(note: .notInAGroup(title: "Research"))),
            .canvas(event: .canvasStatus(note: .noConnection(forward: true, ordinal: nil))),
            .canvas(event: .canvasStatus(note: .noConnection(forward: true, ordinal: 2))),
            .canvas(event: .canvasStatus(note: .noConnection(forward: false, ordinal: nil))),
            .canvas(event: .canvasBlocked(reason: .modeBusy)),
            .canvas(event: .canvasBlocked(reason: .undoBlocked)),
            .canvas(event: .canvasBlocked(reason: .redoBlocked)),
            .canvas(event: .canvasBlocked(reason: .undoQuarantined)),
            .canvas(event: .canvasBlocked(reason: .redoQuarantined)),
            .canvas(event: .canvasHistoryQuarantinedTitle(verb: .undo)),
            .canvas(event: .canvasHistoryQuarantinedTitle(verb: .redo)),
            .canvas(event: .canvasBlocked(reason: .linkOpenFailed)),
            .canvas(event: .canvasBlocked(reason: .alignWouldOverlap)),
            .canvas(event: .canvasBlocked(reason: .notAUrl)),
            .canvas(event: .canvasBlocked(reason: .cardTextUnreadable)),
            .canvas(event: .canvasBlocked(reason: .notePathMustEndInMd)),
            .canvas(event: .canvasBlocked(reason: .noFreeSpaceInGroup(label: "Q3"))),
            .canvas(event: .canvasBlocked(reason: .notePathExists(path: "notes/research.md", onDisk: false))),
            .canvas(event: .canvasBlocked(reason: .notePathExists(path: "notes/research.md", onDisk: true))),
            .canvas(event: .canvasBlocked(reason: .noteReadFailed(message: "The card text is unavailable."))),
            .canvas(event: .canvasBlocked(reason: .noteCreateFailed(path: "notes/research.md", message: "io error"))),
            .canvas(event: .canvasBlocked(reason: .noteRetargetFailed(path: "notes/research.md", message: "io error"))),
            .canvas(event: .canvasBlocked(reason: .headingNotFound(heading: "Roadmap", filename: "notes.md"))),
            .canvas(event: .canvasBlocked(reason: .reopenFailed(message: "The file moved."))),
            .canvas(event: .canvasActionFailed(action: .newCard, detail: "the file is read-only")),
            .canvas(event: .canvasActionFailed(action: .newGroup, detail: "the file is read-only")),
            .canvas(event: .canvasActionFailed(action: .newCanvas, detail: "the file is read-only")),
            .canvas(event: .canvasActionFailed(action: .moveIntoGroup, detail: "the file is read-only")),
            .canvas(event: .canvasActionFailed(action: .placement, detail: "the file is read-only")),
            .canvas(event: .canvasActionFailed(action: .align, detail: "the file is read-only")),
            .canvas(event: .canvasActionFailed(action: .create, detail: "the file is read-only")),
            .canvas(event: .canvasActionFailed(action: .removeFromGroup, detail: "the file is read-only")),
            .canvas(event: .canvasActionFailed(action: .duplicate, detail: "the file is read-only")),
            .canvas(event: .canvasActionFailed(action: .createConnectedCard, detail: "the file is read-only")),
            .canvas(event: .canvasActionFailed(action: .canvasAction, detail: "the file is read-only")),
            .canvas(event: .canvasActionFailed(action: .whereAmI, detail: "the file is read-only")),
            .canvas(event: .canvasSaveConflict),
            .canvas(event: .canvasFileNotFound(target: "media/diagram.png")),
            .canvas(event: .canvasOpened(title: "Notes.md", target: .defaultApp)),
            .canvas(event: .canvasOpened(title: "example.com", target: .browser)),
            .canvas(event: .canvasMutationRefused(reason: .opening)),
            .canvas(event: .canvasMutationRefused(reason: .reopening)),
            .canvas(event: .canvasMutationRefused(reason: .retargetFailed)),
            .canvas(event: .canvasMutationRefused(reason: .unavailable)),
            .canvas(event: .canvasMutationRefused(reason: .readOnly)),
            .canvas(event: .canvasMutationRefused(reason: .cardEditorUnavailable)),
            .canvas(event: .canvasMutationRefused(reason: .refreshPending)),
            .canvas(event: .canvasLoadedDegraded(skipped: 3)),
            .canvas(event: .canvasLoadedDegraded(skipped: 1)),
            .canvas(event: .canvasEmptyOnboarding(newCardChord: "Option Command N", paletteChord: "Command Shift P")),
            .canvas(event: .canvasWhereAmI(kindLabel: "text", title: "Research", groupPath: ["Quarter", "Q3"], ordinalN: 2, totalM: 5, connectionCount: 3, inCount: 1, outCount: 2, colorName: "red", marked: true, mode: .move, filter: .active(matched: 3, total: 40))),
            .canvas(event: .canvasWhereAmI(kindLabel: "text", title: "Loose", groupPath: [], ordinalN: 1, totalM: 1, connectionCount: 1, inCount: 1, outCount: 0, colorName: nil, marked: false, mode: nil, filter: .inactive)),
            // Cardinality boundary witnesses (contract 0a-14).
            .canvas(event: .canvasTracePathEnd(titles: ["Research"])),
            .canvas(event: .canvasTracePathEnd(titles: [])),
            .canvas(event: .canvasBulkMoved(count: 1, relative: .below(anchorTitle: "Research"))),
            .canvas(event: .canvasBulkDuplicated(count: 1)),
            .canvas(event: .canvasModeEntered(mode: .move, object: .cards(count: 1))),
            .canvas(event: .canvasModeCommitted(verb: .move, object: .cards(count: 1))),
            .canvas(event: .canvasGrouped(count: 1, label: "Q3")),
            .canvas(event: .canvasMarksCleared(count: 1)),
            .canvas(event: .canvasMovedTo(verbosity: .verbose, kindLabel: "text", title: "Loose", ordinalN: 1, totalM: 1, container: nil, connectionCount: 0, colorName: nil, marked: false)),
            .canvas(event: .canvasMarkToggled(marked: false, title: "Research", count: 0)),
            .canvas(event: .canvasBulkDuplicated(count: 0)),
            .canvas(event: .canvasFilterCount(matched: 0)),
            .canvas(event: .canvasFilterCleared(total: 0)),
            .canvas(event: .canvasWhereAmI(kindLabel: "text", title: "Loose", groupPath: [], ordinalN: 1, totalM: 1, connectionCount: 0, inCount: 0, outCount: 0, colorName: nil, marked: false, mode: nil, filter: .inactive)),
            // Read-verb refusal during the reopening window (W6-1 0b-2).
            .canvas(event: .canvasStatus(note: .reopening)),
            // …and during a first load or a prepared replacement.
            .canvas(event: .canvasStatus(note: .loading)),
        ]
    }

    func testEveryCorpusEventRendersTheCommittedTextAndPriority() throws {
        let data = try Data(contentsOf: Self.corpusURL)
        let entries = try JSONDecoder().decode([CorpusEntry].self, from: data)
        let corpus = Self.corpus

        XCTAssertEqual(
            corpus.count, entries.count,
            "Swift corpus mirror and the committed artifact must stay in lockstep"
        )

        for (index, (event, entry)) in zip(corpus, entries).enumerated() {
            XCTAssertEqual(
                a11yEventIdentity(event: event), entry.event,
                "event identity mismatch at corpus[\(index)] — the mirror "
                    + "constructed a different variant or parameters than the artifact pins"
            )
            let rendered = a11yRender(event: event)
            XCTAssertEqual(
                rendered.text, entry.text,
                "text mismatch at corpus[\(index)] (\(entry.event))"
            )
            let priority = rendered.priority == .high ? "high" : "medium"
            XCTAssertEqual(
                priority, entry.priority,
                "priority mismatch at corpus[\(index)] (\(entry.event))"
            )
        }
    }

    /// The poster path itself: posting an event routes core's rendered
    /// (text, priority) into the platform primitive verbatim.
    func testEventPosterUsesTheCanonicalRendering() {
        final class RecordingPoster: AnnouncementPosting {
            var posted: [(String, AnnouncementPriority)] = []
            func post(_ message: String, priority: AnnouncementPriority) {
                posted.append((message, priority))
            }
        }
        let poster = RecordingPoster()
        poster.post(A11yEvent.noteSaved(filename: "notes.md"))
        poster.post(A11yEvent.commandPaletteNeedsVault)

        XCTAssertEqual(poster.posted.count, 2)
        XCTAssertEqual(poster.posted[0].0, "Saved notes.md.")
        XCTAssertEqual(poster.posted[0].1, .medium)
        XCTAssertEqual(poster.posted[1].0, "Open a vault to use the command palette.")
        XCTAssertEqual(poster.posted[1].1, .high)
    }
}
