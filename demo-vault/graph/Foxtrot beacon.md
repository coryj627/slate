# Foxtrot beacon

The home of the unresolved links. Three targets below do not exist, so
the graph shows them as ghosts: the Unresolved preset lists them, the
Ghosts filter hides them, and on the Connections leaf each ghost row
offers Create note.

- [[Graph ghost — unresolved target]] — a plain ghost.
- [[Café ghost]] — an accented name; Create note writes `Café ghost.md`
  at the vault root.
- [[graph/missing/Ghost in a folder]] — a ghost with a path; Create
  note writes it under `graph/missing/`.

Back to [[Graph hub]].
