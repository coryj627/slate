# Island north

One corner of a triangle that touches nothing else in the vault, so
the graph shows it as a component of its own: the Component column
reads one number for the three island notes and another for the hub's
cluster.

To [[Island south]] and [[Island east]].
