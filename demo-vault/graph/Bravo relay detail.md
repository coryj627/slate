# Bravo relay detail

Two links from the hub. Re-rooting the Connections leaf here (Enter on
the row) makes this the centre; Ctrl+[ returns to the previous root.

Up to [[Bravo relay]]. Down to [[Bravo relay detail deep]], which lives
in the `deep` folder so the Folder column shows a second value.
