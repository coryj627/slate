# §W-C accessibility matrix — W1 shell/workspace and W2 editor

Updated 2026-07-23. This is the wave-close instrument required by `w7_spec.md`. “CI-required” means the scenario is implemented in `SlateWindows.AccessibilityTests` and `windows.yml` fails if an interactive Windows desktop is unavailable; it does not claim that the current non-interactive local session exercised UI Automation. Every CI attempt retains a 30-day `slate-windows-accessibility-<commit>` artifact containing the VSTest TRX plus dated, revision-bound JSON summaries for the workspace, Quick Open, and welcome axe scans. Human screen-reader cells remain release-blocking until a named tester records a build, Windows version, AT version, result, and evidence link.

| Surface | UIA control type | Name / HelpText source | Required patterns | Focus order and keyboard route | Notification contract | Automated evidence | Narrator | NVDA | JAWS |
|---|---|---|---|---|---|---|---|---|---|
| Main window and menu bar | Window; Menu / MenuItem | Window title; native headers, access keys, and `InputGestureText` | Window; Invoke on commands | Menu access keys; declarative named chords | Canonical dispatcher for lifecycle outcomes | Production startup smoke passed locally; axe scan CI-required | Pending | Pending | Pending |
| Welcome / Open Vault | Pane; Button | “Welcome to Slate”; `OpenVaultButton`; HelpText from XAML | Invoke | Initial focus on Open Vault; recent vaults follow | Core/host lifecycle result through dispatcher | Invoke + welcome axe scan CI-required | Pending | Pending | Pending |
| Recent vaults | Group; Button per vault | Vault display name; full path tooltip | Invoke | Tab through recent buttons | Open/validation result through dispatcher | Recent-vault button assertion CI-required | Pending | Pending | Pending |
| Vault scan status | Text; ProgressBar | “Vault status”; “Vault scan progress” | RangeValue on progress | Not a focus stop | Throttled progress and one final canonical summary | Dispatcher/unit coverage and production startup smoke passed; live 2/2 RangeValue assertion passed in final PR #1030 CI | Pending | Pending | Pending |
| Files tree | Tree; TreeItem | “Files”; node `AutomationName` | SelectionItem; ExpandCollapse for folders | Native arrows; type-ahead; F2; Enter/open commands | Canonical mutation/filter events; destructive operations confirmed | Tree/type/pattern assertions, UIA Expand/Collapse, native Right/Left child reveal with focus retention, and axe scan CI-required | Pending | Pending | Pending |
| File filter/results | Edit; List / ListItem | “Filter files”; “Filtered files”; filter grammar tooltip | Value; Selection / SelectionItem | Alt+Ctrl+F focuses edit; arrows move result; Enter opens | Canonical count event | Value/list/result assertions CI-required | Pending | Pending | Pending |
| Tag tree | ToggleButton; Tree / TreeItem | “Show tag tree”; “Tags”; hierarchical tag name | Toggle; SelectionItem; ExpandCollapse when nested | Tab to toggle; native tree navigation | Selection/filter result through dispatcher | Tagged-fixture tree/pattern assertion CI-required | Pending | Pending | Pending |
| Sidebar organization and actions | ComboBox; CheckBox / ToggleButton; Expander; Button | Human-readable sort labels and explicit action names | Selection; Toggle; ExpandCollapse; Invoke | Native tab order; all actions command-enabled by state | Mutation/import summaries through dispatcher | Workspace axe scan plus exact live Selection/Toggle/ExpandCollapse/Invoke census passed in final PR #1030 CI | Pending | Pending | Pending |
| Workspace tabs | Tab; TabItem | “Workspace tabs”; tab title; close-button name | Selection / SelectionItem; Invoke on close | Ctrl+W/T/Shift+T; Ctrl+Shift+[ or ]; context menu | Focus-route and close/save events through dispatcher | Tab/type/pattern assertions CI-required | Pending | Pending | Pending |
| Split workspace | Pane; multiple Tab controls; `WeightedSplitPanel`; Thumb boundaries | “Editor pane”; “Split editor panes”; named resize handles with percentage ItemStatus and HelpText | Selection; focusable pointer/arrow resize handles; bounded global resize commands | Ctrl+\ / Ctrl+Alt+\; Ctrl+Alt+arrows; Ctrl+Alt+=/-; arrows while a handle is focused | Terminal-boundary focus event; resize-completion size announcement | Two-TabControl assertion, model geometry/focus scenarios, real Ctrl+Alt+Left focus assertion CI-required, drag/weight-invalidation regressions, and live handle-focus/arrow scenario CI-required | Pending | Pending | Pending |
| Editor document | Document | “`<filename>` editor” | Text; Value; Scroll | Native text editing/selection/undo; Ctrl+S; Ctrl+Click and Ctrl+Enter activate the core semantic target | Canonical save/drift and interaction outcomes through dispatcher | Live Document/Value/focus assertions, delta/IME/undo/drift censuses, and exact semantic-action model tests CI-required | Pending | Pending | Pending |
| Editor semantic menu and popover | Menu / MenuItem; popover ContentControl; read-only Edit; Button | Explicit Activate/Preview/Spelling/Zoom command names; core-derived citation/embed title and body; Open source; Close | Invoke; Toggle on spelling item; Value/Scroll on preview body; Invoke on popover buttons | Ctrl+E preview; Ctrl+Enter general activation; Escape closes and returns editor focus; Ctrl+Click pointer parity | Canonical unresolved/citation/task/navigation events; host-composed saved-state and scale status | FlaUI opens embed via both keyboard paths, inspects/closes the live popover, and asserts all six Editor menu items; axe scan CI-required | Pending | Pending | Pending |
| Right-pane leaf registry | List / ListItem | “Right pane panels”; 16 human-readable leaf titles | Selection / SelectionItem | Ctrl+Alt+I toggles; native list arrows | Selection/focus state through dispatcher where spoken | List type/pattern and exact 16-item assertion CI-required | Pending | Pending | Pending |
| Reading view (W3-1) | Document (read-only RichTextBox); structural peers: heading level, List/ListItem; native Hyperlink/CheckBox/Button children | "Reading view"; core `AxText` as HelpText (citation speech, "Unresolved link"); embed cards named from core keys | Text (with `StyleId` heading attribute via decorator; quotes answer `StyleId_Quote` + `StyleName` "Quote" — NVDA speaks the latter under "report style", off by default, G27); Invoke on links/buttons; Toggle on task checkboxes (routed through the core task command) | Ctrl+Shift+E toggles; full caret navigation; chorded structural nav Ctrl+Alt+{H,K,U/L,T,C,E,1-6} (+Shift reverses); Enter/Ctrl+Enter activate at caret; links open in a new tab by default (Editor-menu preference flips to in-place — gap G22); landings/misses via canonical vocabulary | `ReadingNavLanded` / `ReadingNavNoTarget` / task + degraded-ceiling announcements through the canonical dispatcher | 26 reading-surface unit tests incl. live-merged-document navigation, StyleId pin, activation seams, task-toggle round trip, chunked streaming + 2,000-block degraded ceiling, two-reading-tab rebind regression, crash regression; per-key chord diagnostics. **Identity contract** (exe `SlateWindows.exe`, `Slate.MainWindow`, `ReadingSurface` — externally-dependable API for the W-E7 AT layers) asserted in `ShellAccessibilityTests` | Pending (smoke scope) | **Navigation + reading verified 2026-07-26/27; activation (click/Enter/Ctrl+Enter, all run kinds) verified 2026-07-27; task toggles (Space/Enter at caret + click, announcements, position preserved) and the new-tab link default (G22) verified 2026-07-27 after the undo-serialization root-cause fix (`82b3003`) — Cory Joseph, NVDA 2026.1.1, Win11 26200; [full record](reports/w3_1_nvda_field_verification.md). Degraded mode (2,000-block ceiling) not yet field-passed.** | Pending |
| Code blocks (W3-4) | Group peer per block (preamble Name); interior in the document Text pattern; copy = in-range Hyperlink with BUTTON control type (`CodeCopyHyperlink` peer — field 2026-07-31: "link" misread an in-place action; a real Button is G23-blank) | **Core preamble** ("Code block, rust, 3 lines." — `code_block_preamble`, hoisted from mac wording and pinned by the §W-A goldens both twins byte-check); copy "Copy code" + HelpText; preamble caption-styled (12px tertiary — owner call 2026-07-31: mac hides its preamble AX-only, Windows keeps it visible-but-subtle in range) | Text (interior); Invoke on Copy | Say-all reads the token-colored interior in place (W3-1 in-range invariant); Ctrl+Alt+C/±Shift chords land on blocks; Tab reaches the Copy button; Editor menu radio group sets preamble verbosity | Canonical `CodeCopied` ("Code copied."), `CodePreambleVerbosity` echo; landing announcements per W3-1 vocabulary | Token rendering, preamble, drift fallback, copy, peer, verbosity persistence pinned in `ReadingViewTests`; APCA gate covers all six token brushes vs surface + raised in both themes; §W-A `code_blocks` rows green in both twins | Pending (smoke scope) | Pending — token rendering/preamble/copy not yet field-passed | Pending |
| Math blocks (W3-2) | Focusable Group per display block ("math" localized type); registered MathML custom UIA property (Word/Chromium/Narrator convention, gap G23) | Name = **canonical MathCAT speech** ("Math expression." empty-speech fallback, mac parity); HelpText = authored source; ItemStatus = **decoded braille in the session's Nemeth/UEB code** (the analog of mac's "Braille" custom content, W-E7 read channel) | Invoke n/a; MathML property via the G23 bridge (loud startup smoke test) | Focus/Tab/object-nav speak the math (stock NVDA + JAWS, verified routes); Ctrl+Alt+M/±Shift chords land with "{speech}, math."; Enter at caret speaks the block; Ctrl+Enter at caret reads the braille artifact ("Braille not available." when core degraded); say-all crosses a marker (in-flow speech = W-E7 layer, G23) | `ReadingNavLanded/NoTarget` Math vocabulary; `MathSpeechStyle`/`MathVerbosity`/`MathBrailleCode` pref echoes | Math tests over REAL MathCAT (genuine ClearSpeak speech asserted, bmatrix coverage gap degrades to source-in-range, prefs re-projection proven by Nemeth→UEB flipping the retrievable ItemStatus value); bounded pipeline: core per-note budgets (2,000 formulas / 16 KiB per formula → typed-speech degradation) + host visual budgets (4 KiB per formula, 64 KiB per projection, W3-4 pool shape) degrade visuals only, artifacts stay live; §W-A `math_blocks` rows byte-check MathCAT speech + braille in both twins; WPFMath currency + subset recorded (#729 PR); MathSpeak known-issue #1056 | Pending (smoke scope) | Pending — not yet field-passed | Pending |
| Diagram blocks (W3-3) | Focusable Group per rendered block ("diagram" localized type); rendered image carries NO automation peer (mac `accessibilityHidden` parity); failure header/reason/source in the document Text pattern | Name = **canonical structured description** verbatim ("Mermaid diagram." empty-artifact fallback, mac parity — never composed); HelpText = authored source ("Source not available." when empty); ToolTip = 3-line/120-char preview (mac audit #254 M2) | Invoke n/a — the description IS the whole convention (no MathML-style side channel exists for diagrams) | Focus/Tab/object-nav speak the description; Ctrl+Alt+D/±Shift chords land with "{description}, diagram." (trailing period stripped so the vocabulary composes); Enter and Ctrl+Enter at caret re-read the description; say-all reads failure header/reason/source in place; unmatched live-edit fallback = raw source, never a fabricated failure status | `ReadingNavLanded/NoTarget` Diagram vocabulary; no diagram pref events (mac parity: none exist) | 8 diagram tests over the REAL renderer (description verbatim, unsupported/stale/fetch-degraded/Ok-with-undecodable-SVG fallbacks, pool-bounded decode with probe seam); bounded pipeline: core per-note budgets (2,000 diagrams / 64 KiB per source → typed RenderFailed, description+source intact) + host visual budgets (512 KiB per SVG, 2 MiB per projection, W3-4 pool shape); §W-A `diagram_blocks` rows byte-check description/status/source/position in both twins (SVG bytes deliberately excluded — machine-dependent float text-measurement; presence pins render success); Svg.Skia currency + click-directive-href triaged fallback recorded (G19, #730 PR); mac's Dynamic-Type-scaled 600pt height cap ships as fixed 600 (WPF has no text-size metric — recorded deviation); cross-process Name assertion in the identity gate | Pending (smoke scope) | Pending — not yet field-passed | Pending |
| Embed cards (W3-5) | Section per card with resolved content IN the document Text pattern (the W3-1 spike's BlockUIContainer say-all lesson); "Jump to source" HYPERLINK per header (in-range text — field fix 2026-07-30: a Button in an InlineUIContainer is an embedded object, blank in the caret stream with its Name suppressed, the G23 mechanism; the caret heard a bare "button"); nested embeds as indented header-only child cards (G25) | Header = mac EmbedView name shapes VERBATIM ("Embedded note: {path}" / "Embedded section: {heading} from {path}" / "Embedded block from {path}" / "Embedded image: {alt-or-filename}" / the five "Unresolved embed: …" strings + depth marker); AX-only explanatory suffixes on Jump HelpText; alt threads from the embed's link record everywhere (mac's fallback alt-loss not ported, G25) | Invoke on Jump hyperlinks (resolved-path navigation, run-kind fallback for degraded cards, G22 new-tab default) | Ctrl+Alt+E/±Shift chords land speaking the header; Enter at the header activates; say-all reads the embedded BODY text in place; image embeds render bounded (DecodeImage 8 MiB/1120 px) at NATURAL size, downscale-only (G26 — mac upscales to column width; owner call 2026-07-31) with the image peer-suppressed (mac accessibilityHidden parity); `.canvas` embeds = raw-JSON note card (mac-verbatim, G25); `.canvas` OPEN-targets route to the canvas workspace tab (surface ships with its owning milestone) | Existing Embed vocabulary (landed suffix-suppressed, corpus unchanged); `InternalNavigated` on open | 9 embed tests (name shapes, alt-or-filename + record threading + duplicate-key last-wins, unresolved strings + suffixes, decode-failure string, nested collapsed splice, degraded header-only cards, canvas raw-JSON parity, depth/truncation notices); §W-A `embed_resolutions` rows byte-check `resolve_embed_preview` in both twins (FIRST cross-platform resolution pin; the fixture vault now carries binary + `.canvas` targets); bounded fetch per G24 (128 keys / 16 MiB images per note, core preview budgets per key) | Pending (smoke scope) | Pending — not yet field-passed | Pending |
| Data grids (W4-1 substrate) | `AccessibleDataGrid` wrapping the **Fluent-restyled** WPF DataGrid (AutomationId `AccessibleDataGrid`); DataGridCell children; separately-focusable summary Text (`AccessibleDataGridSummary`); row-action ContextMenu | Grid Name from the consuming surface; cell Name "{Header}: {value}" (column hint → HelpText); summary Name "Summary: {core summary}"; disabled row actions keep their Name with the reason as HelpText | Grid; Table (addressable ColumnHeaders); Selection / SelectionItem; Invoke on row actions and export commands | Arrow cell navigation; Ctrl+Alt+S toggles sort on the focused column (header click routes through the same seam); first-column type-ahead (1 s rolling buffer); Ctrl+F filter hook (continues routing when no consuming surface subscribed, so app find is never shadowed); Menu/Shift+F10 row actions; reading tables ride the substrate via Enter-at-caret (G28) — the in-range table stays for linear reading, Escape returns to the caret | Canonical #969 grid family — `GridSorted` / `GridRowMoved` / `GridCellMoved` / `GridGroup` — rendered CORE-side (row moves dedup the focused cell against the row `audio_description`), posted via the dispatcher; export text only from the injected core producer (`base_export` precedent — no producer, commands disabled; reading tables ship without export, G28) | 12 unit tests (virtualization pinned Standard + rows-only against the dotnet/wpf #8528 UIA crash class, sort seam + core-render equality, unsortable refusal, dedup, summary region, export producer, type-ahead, actions menu, filter gate) + 5 reading-table tests (source stamp, Enter-at-caret seam, core cell model, core sort event, degenerate fallthrough); reusable FlaUI conformance suite on the FLUENT-themed `GridConformanceHost` (`ValidateResourceDictionaries` fails loudly): §8.7 matrix + 10,000-row virtualization trap probe + axe scan; §W-A corpus: 8 golden grid rows byte-checked in both twins | Pending | Pending | Pending |
| Quick Open | Pane; Edit; List / ListItem; Button | “Quick Open”; search HelpText documents targets; result display name; “Close Quick Open” | Value; Selection / SelectionItem; Invoke | Ctrl+O; arrows wrap; Enter/Ctrl+Enter/Ctrl+Alt+Enter; Esc restores prior focus; overlay tab cycle trapped | Typed `QuickSwitcherCount` rendered by core | Edit/list/item/close assertions + overlay axe scan CI-required | Pending | Pending | Pending |

## Wave-close status

**W3 wave closed 2026-07-30** — all five issues merged (W3-1 #1052 · W3-4 #1054 · W3-2 #1057 · W3-3 #1058 · W3-5 #1059), each shipped through the full per-issue loop: adversarial review to approval (7–9 rounds per issue), PR, CI green on both twins. The wave grew the Windows suite 250 → 351 tests and added four §W-A parity sections byte-checked cross-platform (`code_blocks`, `math_blocks`, `diagram_blocks` — SVG bytes deliberately excluded as machine-dependent — and `embed_resolutions`, the first cross-platform pins of MathCAT speech/braille and embed resolution; the fixture vault now carries binary and `.canvas` targets). Dependency currency checks were discharged in-wave per the 07 §4.2 doctrine — WPFMath 2.1.0 (2026-07-27, pass with caveats: coverage subset matrix-rowed, #1057) and Svg.Skia 5.1.1 (2026-07-28, pass with caveats: click-directive-href fallback triaged, G19 row) — next re-run at W4 close. Owner-visible divergences recorded G21–G25; upstream defect issues filed #1053 (mac consumes core code preamble) and #1056 (MathSpeak unimplemented in MathCAT). Human AT status per row above: W3-1 NVDA field-verified 2026-07-26/27; W3-2/W3-3/W3-4/W3-5 rows pending field passes.

- Automated local at W3 close: 351/351 Windows tests, 3/3 accessibility tests (production startup, bounded transient-UIA retry, and the reading identity contract with cross-process math/diagram assertions), 1,788 `slate-core` lib tests (nextest ci profile green in CI on every W3 PR), evidence validation for 182 command rows (52 chorded)/16 leaves via `generate-parity-matrix.py`.
- Automated local at W2 close: 212/212 Windows tests, 2/2 accessibility tests (production startup plus bounded transient-UIA retry), the complete Windows `slate-core` non-census package tier (1,616 unit, 364 integration, and 2 doctests), evidence validation for 65 command rows/seven surfaces, and all W1/W2 accessibility code compiles. Repository-contract tests pin accessibility artifact retention and the HostLogProbe build-only dependency, while structure censuses guard representative sidebar filter/tree/import/session-work and workspace persistence/layout declarations. Behavioral tests, rather than source strings, enforce shutdown admission/draining, anchored replacement-redirection, post-commit cleanup safety, bounded ordered/reentrant scan progress, process-wide Quick Open serialization with canceled-queue rejection, ordinary child-expansion blocked-provider return/supersession/refresh precedence and cancellation cascade/detached admission/shared-provider serialization/canceled queues/private failure/restoration/close/retry/disposal, and operation correctness.
- Automated interactive: pass on final PR #1031 revision `d2da2cdb023bf0a988b9ba52d2046f8287c61825` in [Windows Actions run 29990822473](https://github.com/coryj627/slate/actions/runs/29990822473). The complete non-census core and CLI tiers ran in the same successful job as the live RT13 tree assertions, RT17 2/2 scan RangeValue plus exact sidebar pattern census, and RT18 Windows tests. The retained `slate-windows-accessibility-d2da2cdb023bf0a988b9ba52d2046f8287c61825` artifact plus its passing TRX and dated, revision-bound `workspace`, `quick-open`, and `welcome` axe evidence steps were green; expanded empty shortcuts retain nonzero accessible geometry.
- W2 editor interactive evidence is CI-required by `ShellAccessibilityTests`. The keyboard/popover/menu assertions **passed** on the W2-3 deliverable ([#1039](https://github.com/coryj627/slate/pull/1039), merged as `50482b1`); they are no longer awaiting a run.
- Human AT: Narrator, NVDA, and JAWS are pending for every row. Per WGA-9, the milestone cannot be called release-complete until NVDA/JAWS evidence is recorded; Narrator is smoke scope.
- Contrast themes: runtime two-layer Fluent + Slate switching is implemented. The four built-in themes plus one customized-theme human pass remains pending.
