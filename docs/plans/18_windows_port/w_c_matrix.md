# §W-C accessibility matrix — W1 shell/workspace and W2 editor

Updated 2026-09-03 (W6-2 PR A: the graph table row added; WcMatrixGraphEvidenceCensus pins it. W6-1 PR H: the canvas rows reshaped to the ten columns and the §E/§F/§G/§G2 surfaces added; WcMatrixCanvasEvidenceCensus pins them). This is the wave-close instrument required by `w7_spec.md`. “CI-required” means the scenario is implemented in `SlateWindows.AccessibilityTests` and `windows.yml` fails if an interactive Windows desktop is unavailable; it does not claim that the current non-interactive local session exercised UI Automation. Every CI attempt retains a 30-day `slate-windows-accessibility-<commit>` artifact containing the VSTest TRX plus dated, revision-bound JSON summaries for the workspace, Quick Open, and welcome axe scans. Human screen-reader cells remain release-blocking until a named tester records a build, Windows version, AT version, result, and evidence link.

| Surface | UIA control type | Name / HelpText source | Required patterns | Focus order and keyboard route | Notification contract | Automated evidence | Narrator | NVDA | JAWS |
|---|---|---|---|---|---|---|---|---|---|
| Main window and menu bar | Window; Menu / MenuItem | Window title; native headers, access keys, and `InputGestureText` | Window; Invoke on commands | Menu access keys; declarative named chords | Canonical dispatcher for lifecycle outcomes | Production startup smoke passed locally; axe scan CI-required | Pending | Pending | Pending |
| Welcome / Open Vault | Pane; Button | “Welcome to Slate”; `OpenVaultButton`; HelpText from XAML | Invoke | Initial focus on Open Vault; recent vaults follow | Core/host lifecycle result through dispatcher | Invoke + welcome axe scan CI-required | Pending | Pending | Pending |
| Recent vaults | Group; Button per vault | Vault display name; full path tooltip | Invoke | Tab through recent buttons | Open/validation result through dispatcher | Recent-vault button assertion CI-required | Pending | Pending | Pending |
| Vault scan status | Text; ProgressBar | “Vault status”; “Vault scan progress” | RangeValue on progress | Not a focus stop | Throttled progress and one final canonical summary | Dispatcher/unit coverage and production startup smoke passed; live 2/2 RangeValue assertion passed in final PR #1030 CI | Pending | Pending | Pending |
| Files tree | Tree; TreeItem | “Files”; node `AutomationName` | SelectionItem; ExpandCollapse for folders | Native arrows; type-ahead; F2; Enter/open commands | Canonical mutation/filter events; destructive operations confirmed | Tree/type/pattern assertions, UIA Expand/Collapse, native Right/Left child reveal with focus retention, and axe scan CI-required | Pending | Pending | Pending |
| File filter/results | Edit; List / ListItem | “Filter files”; “Filtered files”; filter grammar tooltip | Value; Selection / SelectionItem | Alt+Ctrl+F focuses edit; arrows move result; Enter opens | Canonical count event | Value/list/result assertions CI-required | Pending | Pending | Pending |
| Tag tree | ToggleButton; Tree / TreeItem | “Show tag tree”; “Tags”; hierarchical tag name | Toggle; SelectionItem; ExpandCollapse when nested | Tab to toggle; native tree navigation | Selection/filter result through dispatcher | Tagged-fixture tree/pattern assertion CI-required | Pending | Pending | Pending |
| Sidebar organization and actions | ComboBox; CheckBox / ToggleButton; Expander; Button | Human-readable sort labels and explicit action names | Selection; Toggle; ExpandCollapse; Invoke | Native tab order; all actions command-enabled by state | Mutation/import summaries through dispatcher | Workspace axe scan plus exact live Selection/Toggle/ExpandCollapse/Invoke census passed in final PR #1030 CI | Pending | Pending | Pending |
| Workspace tabs | Tab; TabItem | “Workspace tabs”; tab title; close-button name | Selection / SelectionItem; Invoke on close | Ctrl+W/T/Shift+T; Ctrl+Shift+[ or ]; context menu | Focus-route and close/save events through dispatcher | Tab/type/pattern assertions CI-required | Pending | Pending | Pending |
| Split workspace | Pane; multiple Tab controls; `WeightedSplitPanel`; Thumb boundaries | “Editor pane”; “Split editor panes”; named resize handles with percentage ItemStatus and HelpText | Selection; focusable pointer/arrow resize handles; bounded global resize commands | Ctrl+\ / Ctrl+Alt+\; Ctrl+Alt+arrows; Ctrl+Alt+=/-; arrows while a handle is focused | Terminal-boundary focus event; resize-completion size announcement | Two-TabControl assertion, model geometry/focus scenarios, real Ctrl+Alt+Left focus assertion CI-required, drag/weight-invalidation regressions, and live handle-focus/arrow scenario CI-required | Pending | Pending | Pending |
| Editor document | Document | “`<filename>` editor” | Text; Value; Scroll | Native text editing/selection/undo; Ctrl+S; Ctrl+Click and Ctrl+Enter activate the core semantic target | Canonical save/drift and interaction outcomes through dispatcher | Live Document/Value/focus assertions, delta/IME/undo/drift censuses, and exact semantic-action model tests CI-required | Pending | Pending | Pending |
| Editor semantic menu and popover | Menu / MenuItem; popover ContentControl; read-only Edit; Button | Explicit Activate/Preview/Spelling/Zoom command names; core-derived citation/embed title and body; Open source; Close | Invoke; Toggle on spelling item; Value/Scroll on preview body; Invoke on popover buttons | Ctrl+E preview; Ctrl+Enter general activation; Escape closes and returns editor focus; Ctrl+Click pointer parity | Canonical unresolved/citation/task/navigation events; host-composed saved-state and scale status | FlaUI opens embed via both keyboard paths, inspects/closes the live popover, and asserts all six Editor menu items; axe scan CI-required | Pending | Pending | Pending |
| Right-pane leaf registry | List / ListItem | “Right pane panels”; 16 human-readable leaf titles | Selection / SelectionItem | Ctrl+Alt+I toggles; native list arrows | Selection/focus state through dispatcher where spoken | List type/pattern and exact 16-item assertion CI-required | Pending | Pending | Pending |
| Reading view (W3-1) | Document (read-only RichTextBox); structural peers: heading level, List/ListItem; native Hyperlink/CheckBox/Button children | "Reading view"; core `AxText` as HelpText (citation speech, "Unresolved link"); embed cards named from core keys | Text (with `StyleId` heading attribute via decorator; quotes answer `StyleId_Quote` + `StyleName` "Quote" — NVDA speaks the latter under "report style", off by default, G27); Invoke on links/buttons; Toggle on task checkboxes (routed through the core task command) | Ctrl+Shift+E toggles; full caret navigation; chorded structural nav Ctrl+Alt+{H,K,U/L,T,C,E,1-6} (+Shift reverses); Enter/Ctrl+Enter activate at caret; links open in a new tab by default (Editor-menu preference flips to in-place — gap G22); landings/misses via canonical vocabulary | `ReadingNavLanded` / `ReadingNavNoTarget` / task + degraded-ceiling announcements through the canonical dispatcher | 26 reading-surface unit tests incl. live-merged-document navigation, StyleId pin, activation seams, task-toggle round trip, chunked streaming + 2,000-block degraded ceiling, two-reading-tab rebind regression, crash regression; per-key chord diagnostics. **Identity contract** (exe `SlateWindows.exe`, `Slate.MainWindow`, `ReadingSurface` — externally-dependable API for the W-E7 AT layers) asserted in `ShellAccessibilityTests` | Pending (smoke scope) | **Navigation + reading verified 2026-07-26/27; activation (click/Enter/Ctrl+Enter, all run kinds) verified 2026-07-27; task toggles (Space/Enter at caret + click, announcements, position preserved) and the new-tab link default (G22) verified 2026-07-27 after the undo-serialization root-cause fix (`82b3003`) — Cory Joseph, NVDA 2026.1.1, Win11 26200; [full record](reports/w3_1_nvda_field_verification.md). Degraded mode (2,000-block ceiling) not yet field-passed.** | Pending |
| Code blocks (W3-4) | Group peer per block (preamble Name); interior in the document Text pattern; copy = in-range Hyperlink with BUTTON control type (`CodeCopyHyperlink` peer — field 2026-07-31: "link" misread an in-place action; a real Button is G23-blank) | **Core preamble** ("Code block, rust, 3 lines." — `code_block_preamble`, hoisted from mac wording and pinned by the §W-A goldens both twins byte-check); copy "Copy code" + HelpText; preamble caption-styled (12px tertiary — owner call 2026-07-31: mac hides its preamble AX-only, Windows keeps it visible-but-subtle in range) | Text (interior); Invoke on Copy | Say-all reads the token-colored interior in place (W3-1 in-range invariant); Ctrl+Alt+C/±Shift chords land on blocks; Tab reaches the Copy button; Editor menu radio group sets preamble verbosity | Canonical `CodeCopied` ("Code copied."), `CodePreambleVerbosity` echo; landing announcements per W3-1 vocabulary | Token rendering, preamble, drift fallback, copy, peer, verbosity persistence pinned in `ReadingViewTests`; APCA gate covers all six token brushes vs surface + raised in both themes; §W-A `code_blocks` rows green in both twins | Pending (smoke scope) | Pending — token rendering/preamble/copy not yet field-passed | Pending |
| Math blocks (W3-2) | Focusable Group per display block ("math" localized type); registered MathML custom UIA property (Word/Chromium/Narrator convention, gap G23) | Name = **canonical MathCAT speech** ("Math expression." empty-speech fallback, mac parity); HelpText = authored source; ItemStatus = **decoded braille in the session's Nemeth/UEB code** (the analog of mac's "Braille" custom content, W-E7 read channel) | Invoke n/a; MathML property via the G23 bridge (loud startup smoke test) | Focus/Tab/object-nav speak the math (stock NVDA + JAWS, verified routes); Ctrl+Alt+M/±Shift chords land with "{speech}, math."; Enter at caret speaks the block; Ctrl+Enter at caret reads the braille artifact ("Braille not available." when core degraded); say-all crosses a marker (in-flow speech = W-E7 layer, G23) | `ReadingNavLanded/NoTarget` Math vocabulary; `MathSpeechStyle`/`MathVerbosity`/`MathBrailleCode` pref echoes | Math tests over REAL MathCAT (genuine ClearSpeak speech asserted, bmatrix coverage gap degrades to source-in-range, prefs re-projection proven by Nemeth→UEB flipping the retrievable ItemStatus value); bounded pipeline: core per-note budgets (2,000 formulas / 16 KiB per formula → typed-speech degradation) + host visual budgets (4 KiB per formula, 64 KiB per projection, W3-4 pool shape) degrade visuals only, artifacts stay live; §W-A `math_blocks` rows byte-check MathCAT speech + braille in both twins; WPFMath currency + subset recorded (#729 PR); SimpleSpeak replaced the never-implemented MathSpeak (#1056; a stored `mathSpeak` migrates to ClearSpeak) | Pending (smoke scope) | Pending — not yet field-passed | Pending |
| Diagram blocks (W3-3) | Focusable Group per rendered block ("diagram" localized type); rendered image carries NO automation peer (mac `accessibilityHidden` parity); failure header/reason/source in the document Text pattern | Name = **canonical structured description** verbatim ("Mermaid diagram." empty-artifact fallback, mac parity — never composed); HelpText = authored source ("Source not available." when empty); ToolTip = 3-line/120-char preview (mac audit #254 M2) | Invoke n/a — the description IS the whole convention (no MathML-style side channel exists for diagrams) | Focus/Tab/object-nav speak the description; Ctrl+Alt+D/±Shift chords land with "{description}, diagram." (trailing period stripped so the vocabulary composes); Enter and Ctrl+Enter at caret re-read the description; say-all reads failure header/reason/source in place; unmatched live-edit fallback = raw source, never a fabricated failure status | `ReadingNavLanded/NoTarget` Diagram vocabulary; no diagram pref events (mac parity: none exist) | 8 diagram tests over the REAL renderer (description verbatim, unsupported/stale/fetch-degraded/Ok-with-undecodable-SVG fallbacks, pool-bounded decode with probe seam); bounded pipeline: core per-note budgets (2,000 diagrams / 64 KiB per source → typed RenderFailed, description+source intact) + host visual budgets (512 KiB per SVG, 2 MiB per projection, W3-4 pool shape); §W-A `diagram_blocks` rows byte-check description/status/source/position in both twins (SVG bytes deliberately excluded — machine-dependent float text-measurement; presence pins render success); Svg.Skia currency + click-directive-href triaged fallback recorded (G19, #730 PR); mac's Dynamic-Type-scaled 600pt height cap ships as fixed 600 (WPF has no text-size metric — recorded deviation); cross-process Name assertion in the identity gate | Pending (smoke scope) | Pending — not yet field-passed | Pending |
| Embed cards (W3-5) | Section per card with resolved content IN the document Text pattern (the W3-1 spike's BlockUIContainer say-all lesson); "Jump to source" HYPERLINK per header (in-range text — field fix 2026-07-30: a Button in an InlineUIContainer is an embedded object, blank in the caret stream with its Name suppressed, the G23 mechanism; the caret heard a bare "button"); nested embeds as indented header-only child cards (G25) | Header = mac EmbedView name shapes VERBATIM ("Embedded note: {path}" / "Embedded section: {heading} from {path}" / "Embedded block from {path}" / "Embedded image: {alt-or-filename}" / the five "Unresolved embed: …" strings + depth marker); AX-only explanatory suffixes on Jump HelpText; alt threads from the embed's link record everywhere (mac's fallback alt-loss not ported, G25) | Invoke on Jump hyperlinks (resolved-path navigation, run-kind fallback for degraded cards, G22 new-tab default) | Ctrl+Alt+E/±Shift chords land speaking the header; Enter at the header activates; say-all reads the embedded BODY text in place; image embeds render bounded (DecodeImage 8 MiB/1120 px) at NATURAL size, downscale-only (G26 — mac upscales to column width; owner call 2026-07-31) with the image peer-suppressed (mac accessibilityHidden parity); `.canvas` embeds = raw-JSON note card (mac-verbatim, G25); `.canvas` OPEN-targets route to the canvas workspace tab (surface ships with its owning milestone) | Existing Embed vocabulary (landed suffix-suppressed, corpus unchanged); `InternalNavigated` on open | 9 embed tests (name shapes, alt-or-filename + record threading + duplicate-key last-wins, unresolved strings + suffixes, decode-failure string, nested collapsed splice, degraded header-only cards, canvas raw-JSON parity, depth/truncation notices); §W-A `embed_resolutions` rows byte-check `resolve_embed_preview` in both twins (FIRST cross-platform resolution pin; the fixture vault now carries binary + `.canvas` targets); bounded fetch per G24 (128 keys / 16 MiB images per note, core preview budgets per key) | Pending (smoke scope) | Pending — not yet field-passed | Pending |
| Data grids (W4-1 substrate) | `AccessibleDataGrid` wrapping the **Fluent-restyled** WPF DataGrid (AutomationId `AccessibleDataGrid`); DataGridCell children; separately-focusable summary Text (`AccessibleDataGridSummary`); row-action ContextMenu | Grid Name from the consuming surface; cell Name "{Header}: {value}" (column hint → HelpText); summary Name "Summary: {core summary}"; disabled row actions keep their Name with the reason as HelpText | Grid; Table (addressable ColumnHeaders + native ROW headers from the `IsRowHeader` column — the core `ColumnRole` Primary twin — so every row answers with its identity); Selection / SelectionItem; ItemContainer / VirtualizedItem (the named WPF virtualization crash path, probed directly); Invoke on row actions and export commands | Arrow cell navigation; Ctrl+Alt+S toggles sort on the focused column (header click routes through the same seam); first-column type-ahead (1 s rolling buffer); Ctrl+F filter hook (continues routing when no consuming surface subscribed, so app find is never shadowed); Menu/Shift+F10 row actions; reading tables ride the substrate via Enter-at-caret (G28) — the in-range table stays for linear reading, Escape returns to the caret | Canonical #969 grid family — `GridSorted` / `GridRowMoved` / `GridCellMoved` / `GridGroup` — rendered CORE-side (row moves dedup the focused cell against the row `audio_description` on WHOLE-FIELD boundaries — a substring hit inside longer words or a continuing value never suppresses the focused cell), posted via the dispatcher; `GridGroup` heads grouped rows through `ComposeGroupHeading` — the SAME core render the mac twin labels its group rows with (grouped presentation ships with its owning surface, W4-6's Bases grid); export text only from the injected core producer (`base_export` precedent — no producer, commands disabled; reading tables ship without export, G28) | 13 unit tests (virtualization pinned Standard + rows-only against the dotnet/wpf #8528 UIA crash class, sort seam + core-render equality, unsortable refusal, dedup, summary region, export producer, type-ahead, actions menu, filter gate, row-header visibility + first-cell entry) + 5 reading-table tests (source stamp, Enter-at-caret seam, core cell model, core sort event, degenerate fallthrough); reusable FlaUI conformance suite on the FLUENT-themed `GridConformanceHost` (`ValidateResourceDictionaries` fails loudly, desktop suite serialized — one foreground at a time): §8.7 matrix incl. ColumnHeaders + RowHeaders, a 10,000-row probe that walks ItemContainerPattern and realizes rows through VirtualizedItemPattern, the reading-table window's first-cell entry + Escape return (G28), and an axe scan; §W-A corpus: 8 golden grid rows byte-checked in both twins | Pending | Pending | Pending |
| Link & structure panels (W4-2) | Four leaf bodies behind the shipped rail: List / ListItem for outline, backlinks, outgoing links (AutomationIds `PanelOutlineList` / `PanelBacklinksList` / `PanelOutgoingLinksList`); the embeds leaf is an ItemsControl (`PanelEmbedsList`) of the SHARED embed card renderer (`EditorEmbedPreviewView`: Expander per card, “Jump to source: {path}” Button) | Count headers as Level2 headings, mac-verbatim (“Backlinks, N entry\|entries” …); rows carry the composed mac labels (“Backlink from X, context: S” / “Link to D” / “Unresolved link: T” / “External link: T” / “Level N heading: T”) with per-state hints as HelpText (“Opens the source note.” / “Cannot open. Target file is not in the vault.” …); empty/loading states are the mac LeafEmptyState sentences, distinct for no-selection vs no-content | Selection / SelectionItem on rows; Invoke on embed Jump buttons; ExpandCollapse on embed cards | Rail arrows choose the leaf (Ctrl+Alt+I toggles the pane); double-click or Enter opens — Ctrl variant opens a new tab (mac ⌘-click), context menu adds Open in Split (mac #457); outline activation scrolls the CURRENT note by heading anchor; unresolved rows announce only; external links launch through an http/https/mailto allowlist | `LeafPanelShown` on rail switches (host-posted, W1-3); `OutlineCount` once per file, never re-announced on leaf switches or save refreshes (the guard is view-model state); activations post `InternalNavigated` with the mac verbs (“Opened backlink to” / “Opened” / “Opened embed source”), `LinkUnresolved`, `ExternalLinkOpened/Failed/Unsupported`, and `ScrolledToHeading` on outline landings; refresh policy is mac-verbatim (one `NoteLoadBundle` per selection, outline re-reads on save, link rows deliberately not) | 13 unit tests (`RightPanePanelsTests`: exact labels, three-state outgoing contract, allowlist, once-per-file outline announce, retention no-refetch, save-refresh matrix, empty sentences) + FlaUI `RightPanePanels_LeafBodiesCarryRowsAndBacklinkNavigates` (all four leaf bodies over the live app, backlink Enter navigation, axe scan); embeds resolve through the BOUNDED preview path (G24 extended to the panel); §W-A rows pre-exist (links.json backlinks/outgoing, per-file headings, embed_resolutions) | Pending | Pending | Pending |
| Tasks panel + review flow (W4-3) | Two leaf bodies behind the shipped rail: the note-scoped Tasks leaf (List / ListItem in Open/Done sections with Level3 group headings, AutomationIds `PanelTasksOpenList` / `PanelTasksDoneList`; a per-row CheckBox child carries the toggle affordance) and the vault-wide Tasks Review leaf (`PanelReviewList` plus filter Buttons and a Load-more Button) | Headers speak TRUE totals, mac-verbatim (“Tasks, none” / “Tasks, N open of M task\|tasks”; “Tasks Review, showing N of M” / “Tasks Review, N shown”); rows carry the composed mac labels (note rows “<statusWord>. <text>. Due <date>. Priority <level>. Repeats <rec>. <statusPhrase>”; review rows lead with the FILENAME) with the mac hints as HelpText (“Scrolls the editor to this task's line.” / “Opens the source note at this task's line.”); checkboxes are “Mark complete”/“Mark incomplete” with “Toggles the task between open and done.”; the `TaskStatusPhrase` vocabulary is HOST-DUPLICATED BY DESIGNATION (§W-C label goldens: `TaskStatusPhraseTests` twins pin identical verbatim strings on both platforms); status is the OPEN char set — only x/X mean completed, '/' and '-' speak In-progress/Cancelled, and mac's silent-error note-panel quirk plus its unrendered `scheduled` field are recorded divergences (Windows speaks read failures; neither platform renders scheduled dates) | Selection / SelectionItem on rows; Toggle on row checkboxes; Invoke on filter and Load-more buttons | Rail arrows choose the leaves; Ctrl+R (mac ⌘R) runs Show Tasks Review — reveals the pane, loads a FRESH page, moves focus; Enter activates a row (note leaf scrolls the editor silently — mac parity; review opens/scrolls with the mac verbs), Space toggles it; filter chips are focus-then-activate (never commit-on-arrow — the mac rationale); review toggles on files with an open tab route through the guarded tab path, tabless files toggle the session directly with no expected hash (the mac review posture) and re-query page one | `TasksReviewShown` (“Tasks review. <filter>.”) on the command, `TasksFilterSet` on chip commits — the two shipped-but-unused core events, first consumed here with the mac filter display names; toggle refusals post core `TaskToggleUnsaved` (High) / `TaskToggleConflict`; toggle success speaks the Windows host-composed “Task completed.”/“Task reopened.” (the shipped W2-3 divergence from mac's silent success, extended to the tabless review path with residue markers); review row activation posts core `ScrolledToLine`/`OpenedAtLine`; the review is an explicit SNAPSHOT — it never chases unrelated saves (the mac red-team precedent) while the note leaf refreshes on save and after toggles | 25 unit tests (`TaskStatusPhraseTests` label goldens; `TasksPanelTests`: grouping, mac labels, true-total caps over the bounded `note_tasks` core query, save refresh, stale-publish ordering, read-failure honesty, dirty-toggle refusal through the real workspace funnel, disk round-trip; `TasksReviewTests`: UTC filter windows on a fixed clock, paging + Load more, direct-toggle disk round-trip + page-one re-query, snapshot semantics, activation verbs, refusal silence) + FlaUI `TaskPanels_RowsToggleAndReviewCarriesTheMacShapes` (both leaves over the live app, Space toggle to disk, review row shape, axe scan); §W-A: NEW task sections in both serializer twins — per-file `tasks` rows + the vault `tasks.json` ordering artifact — with a metadata fixture exercising every axis | Pending | Pending | Pending |
| Properties header + sheets (W4-4) | In-note header: Expander above the editor host, outside its scroll viewer (AutomationId `PropertiesHeader`), rows ItemsControl (`PropertiesRows`) of per-kind editor rows (TextBox / DatePicker / stepper pair / switch CheckBox / wikilink TextBox+Pick / per-item list editors); add sheet and bulk-rename sheet as window overlays (`AddPropertySheet` / `BulkRenameSheet`, `IsDialog`); rename preview rides the W4-1 `AccessibleDataGrid` (Path row-header / Status / Before / After) | Header group Name "Properties, N property\|properties" with visible Level2 heading "Properties, N item\|items"; every editor carries the TYPE-CUED mac label ("Property {key}, {type}, editable" — the `PropertyPhrase` vocabulary, HOST-DUPLICATED BY DESIGNATION like `TaskStatusPhrase`; §W-C goldens in `PropertyPhraseTests`); list items "Property {key}, item\|tag i of n" with "Remove/Add item\|tag" buttons; validation errors "Validation error: {err}"; empty state "No properties yet. Add one to start." (visible == AX name); the mac label vocabulary is pinned verbatim, unknown core kinds pass through raw | ExpandCollapse on the header; Value on text editors; Toggle-free switch (CheckBox commits through the seam); Invoke on Save/Revert/Delete/Pick/sheet buttons; Grid/Table on the rename preview | Enter commits a row draft, Esc reverts it, Ctrl+Backspace opens delete confirmation (the ⌘⌫ substitution, chords.json); boolean switch and well-formed date picker commit immediately; Ctrl+Shift+R (mac ⇧⌘R) opens bulk rename menu-homed (works with the header unmounted, disabled without a session), default action PREVIEW, Apply armed only after a preview on the exact key pair; Esc cancels in-flight core work via the CancelToken; sheet focus lands on the key field and restores on close | Canonical events on the mac triggers rendered by `A11yRender` verbatim: `PropertyChanged`/`PropertyEditConflict`/`PropertyEditFailed`/`PropertiesReloaded`/`PropertiesReloadFailed`/`AddPropertySheetShown`/`BulkRenameSheetShown`/`RenameSummary` (the sheet FOOTER is the same core render — announcement and display cannot drift)/`RenameFailed`/`RenameReloadFailed`; residue-marked `HostComposed` sites for disabled reasons, revert echo, and the dirty-tab refusal (recorded divergence: Windows whole-file tabs refuse property writes under a dirty or stale tab; mac body-only buffers never conflict); ten feature contracts + accepted-risk register in `docs/plans/22_property_panel_contracts.md`; YAML source mode (`togglePropertiesSource`) and the mac retained-update recovery suite are recorded deferrals | 49 unit facts across `PropertyPhraseTests` (§W-C goldens) / `PropertyValueCodecTests` (tagged `\u{f8ff}` list elements, int-vs-float by raw JSON) / `NotePropertiesTests` (snapshot-hash CAS identity, body-byte identity, refusal totality, delete safety, value-level round-trip fidelity, save-funnel re-derivation) / `AddPropertyTests` (pre-core validation, append-at-end, landed-then-faulted honesty) / `BulkRenameTests` (exact-pair arming, partition statuses, footer identity, contract-8 tab reconciliation); FlaUI `PropertiesHeader_RowsEditSheetsAndRenameCarryTheMacShapes` (live header name, type-cued row edit to disk, both sheets, preview grid columns, axe scan); §W-A: `properties` sections in both serializer twins + the vault `properties.json` key artifact with `properties_metadata.md`/`properties_malformed.md` fixtures | Pending | Pending | Pending |
| Citation surfaces (W4-5) | Two leaf bodies behind the shipped rail: the note-scoped Citations leaf (List / ListItem, AutomationId `PanelCitationsList`) and the Bibliography leaf, whose BOTH segments ride the W4-1 `AccessibleDataGrid` — Entries (Title row-header / Authors / Year / Journal / Key, `BibliographyEntries`) and Unresolved (Key row-header / File, `BibliographyUnresolved`), chosen by a two-RadioButton segment group; three in-window overlays (`CitationDetailsSheet` / `CitationSummarySheet` / `FilesCitingSheet`, `IsDialog`) | Row names are `RenderedCitation.SpeechText` VERBATIM with the single sanctioned "Unresolved citation key. " prefix — the VM never composes citation speech and never parses `VisualText` (contract 1); the `CitationPhrase` vocabulary is HOST-DUPLICATED BY DESIGNATION (§W-C label goldens, `CitationPhraseTests`) like `TaskStatusPhrase`/`PropertyPhrase`; entry rows carry the mac `EntryRowDescription` as `rowAudioDescription`, unresolved rows the mac "Unresolved key: {key} in {path}." verbatim; the entries summary carries the truncation sentence so the 5000-row cap is SPOKEN, never silent (contract 9 / D-3) | Grid; Table (row headers from the `IsRowHeader` column) on both segments; Selection / SelectionItem on citation rows and segment buttons; Invoke on sheet buttons and row actions | Rail arrows choose the leaves; Enter or double-click on a citation row opens the details sheet (a placeholder row has nothing to expand — core never looked one up, contract 2); Ctrl+J (mac ⌘J) reveals the Bibliography leaf and lands on the expanded citation's key, Ctrl+Shift+J (mac ⇧⌘J) opens the note's citation summary; Escape closes any sheet and returns focus to the row that opened it (contract 11); the substrate's Ctrl+Alt+S sort, cell arrows and grid-scoped Ctrl+F are inherited unmodified (contract 8), and export is `null` on both grids (D-7 / G29) | Core owns the walk-through announcement, spoken exactly once (contract 4); the ONLY host-composed texts are the two Ctrl+J outcomes, residue-marked `HostComposed` (`JumpedToEntry` / `SearchingBibliographyFor`); insert-citation is unbuildable (core exports no citation mutator) so the announcement IS the product answer and the menu item stays ENABLED rather than a greyed-out mystery; seeding is silent on BOTH success and failure — the leaf's notice region is the surface, and notices are surfaced verbatim, never swallowed (contract 5); twelve feature contracts + divergence register in `docs/plans/23_citation_surfaces_contracts.md` | 90 unit facts: `CitationPhraseTests` 14 (§W-C goldens) / `CitationsPanelTests` 10 / `BibliographyPanelTests` 18 / `CitationOverlayTests` 10 / `CitationWorkspaceSeamTests` 16 / `CitationAsyncInterleavingTests` 14 (the PRODUCTION scheduling path — gated bodies leave the caller's thread, a failed seed stops BOTH leaves reading core, reload-mid-jump retargets, a reload publishes its clear and runs off the dispatcher, a deferred summary answers only the note it was asked about, teardown releases parked bodies without running them) / `BibliographySeedTests` 5 / `FilesCitingNamingTests` 2 / `CitationWriteSurfaceCensus` 1 (contract 6 enforced by SOURCE SCAN); plus 6 new substrate facts (`GridAutomationId` default + summary rename, `FocusRow` hit/miss, sort survives a rebind unannounced, an unsortable rebind drops it, Enter activates the current row, a double-click resolving to no row activates nothing); FlaUI `CitationSurfaces_GridsSheetsAndChords_AreClean` (both leaves over the live app; the details sheet's FIELD NAMES asserted present — the version that only checked the sheet existed passed while every field was absent from the UIA tree; sheet asserted IN-WINDOW per D-1; Escape focus return; Ctrl+Shift+J; **Ctrl+J asserted to LAND**, not merely announce; both segments' grid headers; axe scans); §W-A: `citations` + `bibliography.json` sections in both serializer twins over the committed citation corpus | Pending | Pending | Pending |
| Bases surfaces (W4-6) | The `.base` tab (header picker/count/quick filter + banners + table on `AccessibleDataGrid` `BaseTabGrid` or the row-navigation list `BaseTabList`), the Queries and Base dock leaves, the dashboard tab, and two in-window IsDialog overlays (builder `BaseQueryBuilderSheet`, dashboard editor `DashboardEditorSheet`) | Grid cells Custom per the substrate; list rows named by core `audio_description`; banners focusable captions | Names/labels from core artifacts verbatim (`AudioSummary`, `audio_description`, GridGroup headings); quick filter name carries transiency; read-only cells hint the BasesCellReadOnly render | Grid/Table patterns via the substrate; Value on the cell editor | Header controls -> banners -> grid cells (F2/Enter edit, Tab commits-next, Esc cancels); Ctrl+F filter, Ctrl+Alt+S sort; overlays cycle-trapped | Canonical Bases family only (#969): sort/save-sort transactional, one cell outcome per write, deduped refresh updates; reveal announces the leaf, content never speaks unsolicited (2.6) | `BasesDocumentTests` + `BasesCellEditTests` + `BasesQueriesTests` + `BasesBuilderTests` + FlaUI `BasesSurfaces_GridBuilderAndLeaves_AreClean` + axe | pending | pending | pending |
| History panel (W4-7) | History leaf behind the shipped rail (`HistoryLeafBody`): two RadioButton segments in a named "History scope" landmark, day-group Expanders (`HistoryDay{id}`) of peered focusable version-row Groups (`HistoryRow{position}`) each carrying a compare CheckBox and Restore / Restore As / Compare Buttons, the structured-diff list (focusable `HistoryDiffSummary` header plus one peered single-stop Group per operation, `HistoryDiffOp{n}`), inline Restore-As destination rows (notice + refusal focusable), and Deleted rows with the Trash footer | Row names host-composed "{date}, {core AudioFragment}[, annotations]"; diff rows are core sentences verbatim ("{SemanticDescription}. {Detail}"); diff header = core `AudioSummary`; day groups "{Today/Yesterday/full date}, {n} versions, collapsed/expanded"; empty states and the Trash footer are the recorded mac strings | SelectionItem on the scope RadioButtons; Invoke on actions/Show older; Toggle on markers and compare selection; ExpandCollapse on day groups; Value on the destination TextBox | Menu `Show History Panel` reveals the leaf and moves focus to the pane; segments, since-open section, version header controls (markers toggle, compare), day groups, rows, row actions; restore returns focus to the head row after the reload publishes (WCAG 2.4.3); Enter commits and Esc closes the inline destination row back to its anchor row; every rebuild preserves keyboard focus by automation identity | Vocabulary `HistoryPanelShown` (Medium, only on an actual switch), `RestoredVersionFrom{host-formatted date}` (High), `RestoredFile` / `RestoredFileAs`; HD-1 dirty-tab refusal sentence High; compaction relay Medium once per path; since-open toggle confirmations Medium | `HistoryPanelTests` (17 facts) + FlaUI `HistorySurfaces_LeafDiffAndRestore_AreClean` + axe (`history-this-note`, `history-deleted`) | pending | pending | pending |
| Sync diagnostics (W4-8) | The Sync leaf behind the shipped rail (`SyncDiagnosticsLeafBody`): a five-state report (unsupported / error / loading / empty / populated, SD2's pure precedence selector) whose populated shape is a Level3 count header plus a Refresh Button (`SyncDiagnosticsHeader` / `SyncDiagnosticsRefresh`), the multi-sync warning row FIRST (`SyncDiagnosticsWarning`), then per-provider peered Groups (`SyncDiagnosticsProvider{Kind}` on `AutomationNamedRowBorder`) each ONE accessible stop with presentation-only children, each followed by its own SEPARATE Evidence Expander (`SyncDiagnosticsEvidence{Kind}`) of focusable per-path lines (`SyncDiagnosticsEvidence{Kind}Path{n}`), and the LiveSync configuration landmark (`SyncDiagnosticsLiveSync`, six combined config rows or the malformed/absent line); error state adds a Retry Button (`SyncDiagnosticsRetry`); loading/empty/unsupported are single focusable lines | Every provider display name, recommendation sentence, evidence path, multi-sync warning, and summary sentence is CORE's verbatim (`DetectedSyncProvider.DisplayName/Recommendation/EvidencePaths`, `SyncDetectionReport.MultiSyncWarning/AudioSummary`), never re-rendered, truncated, or re-cased (SD1); provider rows are composed "{DisplayName}: {risk word}. {Recommendation}" (risk reads by WORD, never colour alone); the empty and unsupported states RELAY `report.AudioSummary` where mac duplicates the sentences in Swift (recorded divergence SDD-2 — byte-identical output, one fewer golden pair); every host-composed label is a `SyncPhrase` §W-C golden pinned by `SyncPhraseTests` in LOCK-STEP with the mac twin `SyncDiagnosticsPanel.swift` ("Sync, {n} {system\|systems} detected", "Refresh" / "Refresh sync diagnostics", "Loading sync diagnostics", "Could not load sync diagnostics: {message}", "Retry", "Warning: {warning}", "Evidence", "LiveSync configuration", the six config labels, "Unknown"/"On"/"Off", the malformed and absent config lines); evidence paths render verbatim post-SD9 normalization (no `\\?\` prefix ever reaches a reader) | Invoke on Refresh and on the error state's Retry; ExpandCollapse on the per-provider Evidence Expanders | Rail selects the leaf (no showPanel command exists on either platform, SD7); within the body: count header → Refresh → multi-sync warning → provider rows → each row's Evidence expander and its focusable path lines → the LiveSync configuration section; every standalone report line is focusable so a keyboard user reaches every sentence; a watcher-driven rebuild restores focus by automation identity so an external republish never drops the reader out of the panel | EXACTLY ONE family (SD6/SDINV-4): core's pre-rendered `report.AudioSummary` relayed as `HostComposed` at High priority, gated to fire only when `MultiSyncWarning` is non-null OR a provider is High risk, and at most once per vault path per session; Low/Medium-only and empty reports, manual refresh completion, and watcher republishes are otherwise SILENT; leaf reveal announces only the generic `LeafPanelShown`. The relay is the W0.5-3 residue designation the mac pins at `AppState.swift:11702` — core's `a11y.rs` has no sync event and a canonical conversion is a deliberate four-place out-of-scope follow-up | `SyncDiagnosticsPanelTests` (+ `SyncDiagnosticsAsyncTests` stale-publish/teardown interleaves + `SyncMarkerWatcherTests` debounce/ceiling/ABA/teardown) + `SyncPhraseTests` label goldens + FlaUI `SyncDiagnostics_LeafReportAndRefresh_AreClean` (rail reveal over a fixture vault with planted `.git` + `.stfolder` markers AND the vault nested under a `OneDrive` component — that arm alone yields canonicalized evidence, so it is what makes the "no `\\?\` prefix reaches a reader" assertion able to fail at all; composed provider-row name, Evidence expander, the chordless `slate.diagnostics.refreshSync` menu route with settle, and the marker watcher proven end-to-end by a fourth marker planted mid-session) + axe (`sync-diagnostics`) | pending | pending | pending |
| Quick Open | Pane; Edit; List / ListItem; Button | “Quick Open”; search HelpText documents targets; result display name; “Close Quick Open” | Value; Selection / SelectionItem; Invoke | Ctrl+O; arrows wrap; Enter/Ctrl+Enter/Ctrl+Alt+Enter; Esc restores prior focus; overlay tab cycle trapped | Typed `QuickSwitcherCount` rendered by core | Edit/list/item/close assertions + overlay axe scan CI-required | Pending | Pending | Pending |
| Command palette (W5-1) | Pane; Edit; List / ListItem; Group headers | “Command Palette”; “Search commands” + HelpText documents the whole key model; row name is one composed string, “{label}, {spoken chord}”; section headers carry their core title | Value; Selection / SelectionItem; Invoke | Ctrl+Shift+P opens (non-toggling); arrows wrap across sections and headers are not stops; Home/End/PageUp/PageDown navigate (PD-1, mac converges under #1105); Enter runs; Esc closes and restores prior focus; overlay tab cycle trapped; shell and menu disabled while open | Canonical `PaletteFilterCount` / `PaletteCommandSelected` / `PaletteCommandUnavailable` / `PaletteCommandFailed` / `PaletteCommandNotFound` / `CommandPaletteNeedsVault`, all rendered by core | Open-by-chord, focus, composed row name, grouped headers, registry round-trip asserted by EFFECT, Esc focus restore, plus an overlay axe scan — CI-required. Axe is taken with an EMPTY query: the Fluent TextBox clear button publishes a private-use glyph once typed (#1106, pre-existing and shared). | Pending | Pending | Pending |
| Search overlay (W5-2) | Pane (`SearchOverlay`, IsDialog); Edit (`SearchOverlaySearch`); List / ListItem (`SearchOverlayResults`); Buttons (recent rows, `SearchOverlayClearRecents`, the tag chip’s `SearchOverlayClearTagScope`, “Close search” `SearchOverlayClose` — S15, mac’s close-button anatomy) | “Search Vault”; field “Search vault” (no trailing period — mac’s actual label) with HelpText documenting the key model; each result row is ONE composed stop, “{basename}: {marker-stripped snippet}” (contract S4) — emphasis runs presentation-only, full path as tooltip; core’s `resultSet.Summary` verbatim as the list ItemStatus; idle/searching/no-results panels mac-verbatim (“Type to search.” / “Searching.” / “No results.”); recents “Recent search: {query}” under a “Recent Searches” Level2 header; tag chip “Filtered to tag {name}” (SD-3: chip + clear button, no scope selector) | Value; Selection / SelectionItem; Invoke on recent/clear/chip buttons | Ctrl+Shift+F toggles (mac ⇧⌘F; Workspace ▸ Search Vault… menu item and the registered `slate.view.toggleSearch` palette row invoke the same unguarded command); arrows move the selection, wrapping (SD-1, mac converges under #1113); Enter opens the selected row — on the field it activates the top result, on a focused button it stays the button’s; Esc closes and restores prior focus (SD-2) unless activation claimed it; overlay tab cycle trapped; text-editing chords pass the modal swallow (S12) | Canonical `SearchResultsSummary` / `SearchFailed` / `SearchNeedsVault` / `SearchResultOpened` / `RecentSearchFocused` / `RowSelected`, all core-rendered; arrow selection speaks the row’s basename only through `RowSelected`, with the publish-time auto-select suppressed so it never double-speaks the summary (S8/SD-1); the searching transition is silent (S8); the summary announcement dedups on the rendered string (S7); activation is the only recents write (S9) | `SearchOverlayViewModelTests` + `SearchRecentsStoreTests` + `SearchLineLocatorTests` + `ReadingTagSearchRerouteTests` + `ModalSurfaceTests`; §W-A `search.json` gained the core `summary` in both twins; FlaUI `SearchOverlay_OpensSearchesActivatesAndRestoresFocus_IsClean` (open by chord over a fixture vault, focus in the field, axe empty + typed, composed row names, Enter opens the target asserted by EFFECT on the editor document, Esc restores the pre-open element, the “Close search” button dismisses by pointer, control-view teardown of every SearchOverlay* id) — CI-required | Pending | Pending | Pending |
| Templates (W5-3) | Two in-window sheets, both IsDialog: the picker (Pane `TemplatePickerSheet`; List / ListItem `TemplatePickerList`; `TemplatePickerCancel`, per-state Try Again buttons) and the prompt/name flow (Pane `TemplateFlowSheet`; one Edit per prompt; Edit `TemplateFlowName`; step footers) | Picker "Choose a template" with the destination subtitle; row name is mac's composed "{name}. {description}." (bare name without one); empty/failed states mac-verbatim with Try Again; flow "Create from template" — prompt fields named by the author's label (never the slug key), name field "New note name. Name relative to {destination}" with the ".md added automatically" HelpText; validation errors focusable, named "Validation error: {error}" | Selection / SelectionItem; Value on every field; Invoke on buttons | Ctrl+Shift+N opens through `DecideTemplateOpen` (supersedes Quick Open/search with focus lineage, retires the palette, refuses beneath sheets — never presents under one, the #1118 rule done right); focus per picker state (first row / Try Again / Cancel); Enter activates the row, is Next on the prompt step, Create on the name step; Esc cancels the whole flow at any step writing NOTHING; after create the caret parks at {{cursor}} (else document end) and focus follows content into the editor | Canonical `TemplatePickerOpened` (count arms) and `TemplateNoteCreated` (High), core-rendered — the FIRST template family in the vocabulary; availability/busy copy is marked W0.5-3 residue (mac parity: its whole template family is residue); validation and create failures are inline focusable text, never announcements | `TemplateFlowTests` (38 facts incl. the caret byte→UTF-16 conversion and the editor-materialization race) + `ModalSurfaceTests` decision/census rows; FlaUI `Templates_PickerPromptsCreateAndCancel_AreClean` (chord open, first-row focus, Esc writes nothing, Value-pattern prompt, seeded name, create, caret proven by TYPING at it and reading the saved bytes, axe over picker/prompts/name) — CI-required | Pending | Pending | Pending |

| File management (W5-4) | The sidebar's file verbs plus one in-window sheet: buttons in the `SidebarFileActions` expander (Duplicate, Move to…, Copy path, Reveal in File Explorer beside the existing create/rename/delete set) and the Move-To picker (Pane `MoveToSheet`, IsDialog; Edit `MoveToFilter`; List / ListItem `MoveToList`; `MoveToCancel`) | Sheet "Move to folder" with title/subtitle prose; rows named by folder leaf with the full path as the composed detail ("{leaf}. {path}."); the pinned "Vault root" and typed "New Folder “{path}”" rows; buttons carry their labels (FD-5: "Reveal in File Explorer" is the recorded label divergence) | Invoke on buttons; Value on the filter; Selection / SelectionItem on the list; ExpandCollapse on the expander | The create hands off to inline rename with the stem selected (F1); F2 renames; tree-scoped Delete trashes the selection (FD-4 — files and empty folders without confirmation, a non-empty folder stages the pinned Recycle-Bin dialog whose Cancel holds focus so bare Enter never confirms); tree-scoped Ctrl+Z/Ctrl+Y drive structural undo/redo (F10); Move To… opens through `DecideMoveToOpen` (supersedes pickers, retires the palette, refuses beneath sheets), focuses its filter, Down/Up drive the list from the box, Enter moves, Esc cancels moving nothing | Mutation sentences are host-composed residue on both platforms (F11) with the canonical `SelectionCopied` and `DuplicateFilesOnly` adopted; structural reports speak the ", updated links in N notes." suffix and surface failed rewrites at High | `FileManagementTests` (25 facts) + `ModalSurfaceTests` censuses + the two-platform mutation harness (`MutationHarnessCensus` / `MutationHarnessTests.swift` over shared goldens); FlaUI `FileManagement_CreateRenameDuplicateMoveDelete_IsClean` (create→rename→duplicate→move-with-typed-new-folder→Delete-key trash, disk verification at each step, axe over the sidebar and the picker) — CI-required | Pending | Pending | Pending |

| Canvas outline (W6-1 PR A) | Tree / TreeItem, with every row projected through `CanvasOutlineRowDataPeer` — WPF's tree DATA-ITEM peer, which is the object a UIA client reads (the container peer is not); Group for the switcher; TextBlock banners; List / ListItem warning rows; the card detail retired (E14) — The `.canvas` tab body (`CanvasSurface`): a header carrying the surface switcher as ONE named group (`CanvasSurfaceSwitcher`, "Canvas view", three RadioButtons), the t0 §5 state regions (loading; empty onboarding `CanvasEmptyOnboarding`; degraded banner `CanvasDegradedBanner` with its focusable warning rows `CanvasWarningRows`; parse-error/failed/retarget banners, which are tab stops with LiveSetting=Assertive), the outline projection `CanvasOutlineTree` (the interim read-only card detail retired by E14's swap for the editor sheet) | Row Name is the t0 §1.1 card reference composed from core's kind word and core's `speakable_name` (A9; §W-C label class by designation, pinned against core's own render); ItemStatus is the t0 §3 slot "⟨n⟩ of ⟨m⟩ in ⟨container‖canvas⟩[, ⟨color⟩][, marked][, filtered]" (A10 — the filtered clause shipped with PR C's filter); a connection row's Name is core's own `CanvasConnectionTraversed` render, the same event the navigator speaks (A11); HelpText is the mac per-kind activation hint verbatim; the degraded banner and the onboarding copy are the SAME core render their announcements speak (A4, CD-37) — composed by `CanvasPhrase.CardReference` and `CanvasPhrase.RowStatus`, the label class §W-G rows P/Q designate | Tree; SelectionItem, ExpandCollapse and ScrollItem from WPF, plus Invoke added on the data-item peer at BOTH nesting levels (A8); Value on the card detail | Opening lands keyboard focus on a REALIZED row, with realization part of delivery and a failed delivery left pending (A14); returning from an activated card lands on that row (WCAG 2.4.3); native tree arrows for row movement, with expand/collapse on Enter-on-group, WPF's numpad +/- and the ExpandCollapse pattern — Left/Right became follow-connection in W6-1 PR C (CD-48), and this cell said otherwise until that PR corrected it: a documented keyboard route the code does not have is the same untrue sentence CanvasSurfaceView's switcher comment records; Enter, double-click and Invoke all activate per kind through one seam (A13); Escape closes the interim detail back to the row that opened it (WCAG 2.1.2); the switcher is one focus stop; `slate.canvas.showOutline/showTable/showVisual` are palette-reachable | Canvas vocabulary only, through `CanvasAnnouncer` (A5/A6, R-C): `CanvasMovedTo` at the persisted verbosity, preceded by `CanvasGroupEntered`/`CanvasGroupLeft` on a container change; `CanvasLoadedDegraded` once per document OPEN; `CanvasOpened`/`CanvasBlocked`/`CanvasFileNotFound` on activation. The landing selection is SILENT (A12); navigation coalesces at 200 ms latest-wins and a High event drops both pending classes; the announcer is retired with its document | `CanvasDocumentTests` (registry, five load states, selection, activation per kind, focus land/restore, persistence, close-gate bypass, the 2,000-node budget in both scheduling modes) + `CanvasAnnouncerTests` + `Censuses/CanvasAnnouncerCensus` (the funnel guard) + `Censuses/AnnouncementSeamCensus` (the production chain) + `Censuses/CanvasAutomationPropertyCensus` (no inert a11y property) + `Censuses/CanvasMediaGateCensus` + `Censuses/ContractsCitationCensus` + `Censuses/ParityHarnessCensus` (`canvas_read` golden) + FlaUI `CanvasSurfaces_OutlineTreeSelectionAndActivation_AreClean` with axe over the outline and the degraded state — CI-required; `CanvasSurfaces_OutlineTreeSelectionAndActivation_AreClean` (the tree, selection, activation, the degraded banner and its rows); `ThePlanServesMacsOrderPerSurfaceAndTarget`; axe: `canvas-outline`, `canvas-degraded` | Pending | Pending | Pending |

| Canvas table (W6-1 PR B) | Grid cells Custom per the substrate; native DataGrid row headers from the Title column (the UIA Table pattern's row identity); the summary is a separately-focusable region — The `.canvas` tab's table projection: the W4-1 `AccessibleDataGrid` (`CanvasTableGrid`, summary region (a separately focusable Text region beside the grid)) in the surface body, reached from the "Canvas view" switcher's Table arm or `slate.canvas.showTable`. Exactly one projection is in the tree — the outline is COLLAPSED while the table shows, so it leaves the tree rather than sitting off screen | Every cell is core's `canvas_table_rows` field under the mac column inventory verbatim — Type · Title · Group · Target · Connections · Color; cell labels are the substrate's "Header: value"; Title is core's `speakable_name` (the field the outline row's name uses, CD-30); the summary is mac's "Canvas table: N cards, M groups." as a static label (never announced — 0a-13) | Grid/Table patterns and per-cell TableItem via the substrate; Invoke-equivalent activation on Enter/double-click; the row-actions menu on Menu/Shift+F10; Selection on the grid and SelectionItem on rows (the substrate's one selection) | Switcher → grid cells; native cell navigation and type-ahead; Ctrl+Alt+S sorts the focused column (§8.7 forbids header-click-only); Enter activates the row through the document's one per-kind seam (media gate included); Ctrl+F is the TABLE's delivery site for `slate.canvas.filterCards` since PR C — the substrate's `FilterCommand` is subscribed to the one canvas filter field | The substrate's canonical grid family (`GridSorted`/`GridRowMoved`/`GridCellMoved`, #969) relayed through `CanvasAnnouncer` with core's own priority (DoD §H), plus the canvas `CanvasMovedTo` on row change at the persisted verbosity — mac's two-line shape, the grid's immediately and the canvas line on the 200 ms navigation window | `CanvasTableTests` (core's rows and order, the six columns and their mac comparators incl. the Color family rule and the numeric Connections sort, selection both ways with no echo, activation per kind, the disabled row actions and their reasons, the summary boundary, the seam swap through the production funnel, the republish that renumbers a speakable name, and the switcher's one-Tab-stop keyboard shape) + `CanvasAnnouncerCensus.EveryGridUnderCanvasRidesTheRelay` + `GridConformanceTests` inherited (cited, not re-run) + FlaUI `CanvasSurfaces_TableGridSortSelectionAndActivation_AreClean` (patterns, header names, summary, Ctrl+Alt+S, the Menu-key row-actions menu with the two disabled reasons readable, Enter activation, axe) — CI-required; `CanvasSurfaces_TableGridSortSelectionAndActivation_AreClean` (headers, Ctrl+Alt+S sort, selection, activation, the row-actions menu); `TheRowMenuEqualsThePlansGridProjectionAndToggleMarkIsLive`; `OpenSampleExposesOutlineTableAndScene` (the sort's pinned oracle); axe: `canvas-table` | Pending | Pending | Pending |

| Graph table (W6-2 PR A) | Grid cells Custom per the substrate; native DataGrid row headers from the Note column (the UIA Table pattern's row identity); Group for the mode switcher; Text for the state region; the summary is a separately-focusable region — The graph tab body (`GraphSurface`): a header with the kind label and the mode switcher as ONE named group (`GraphSurfaceSwitcher`, "Graph surface", the items core's `graph_surface_modes` vector in order — Table live, Diagram present and disabled until the graph's diagram slice, W6-2 PR D), the state region (`GraphStateText`: loading / error / empty with the mac's labels), and the table projection — the W4-1 `AccessibleDataGrid` (`GraphTableGrid`, summary region `GraphTableGridSummary`) | Every cell is core's `graph_table_rows` field under core's column vector verbatim — Note · Links in · Links out · Embeds in · Embeds out · Component · Modified · Folder · Kind (`graph_table_columns`); the row's Name is core's `GraphRow` render at the set verbosity — P1's row copy (A-D3: the mac's rows carry no row label); ItemStatus is the Kind cell (A-D1); cell labels are the substrate's "Header: value"; the summary is core's `audio_summary` verbatim; the grid's label "Graph, data grid" and the state labels are the mac's | Grid/Table patterns and per-cell TableItem via the substrate; Invoke-equivalent activation on Enter/double-click and the modified activation on Ctrl+Enter/Ctrl+double-click; the row-actions menu on Menu/Shift+F10 with core's action vectors; Selection on the grid and SelectionItem on rows; SelectionItem on the switcher's items | `slate.graph.openTab` opens the tab (palette; chordless); the switcher is one focus stop; native cell navigation and type-ahead; Ctrl+Alt+S sorts the focused column through the external-sort seam (a rows-only token, `GridSorted` once on adoption); Enter opens the note in the graph's own pane (a ghost creates its note); Ctrl+Enter opens in a new tab; the row's Name carries the row copy so a reader hears P1's sentence on every row | The graph family only, through `GraphAnnouncer` (A-10): `GraphStatus{Opened}` then `GraphSnapshotSummary` on an explicit open, the summary alone on an activation, the shell's `ReopenedGraph` between the two on a reopen; `GraphBlocked` High flushes the pending classes; the substrate's canonical grid family relayed with core's priority; the shell's `OpenedFile` on an activation (A-D2); one `NoteCreated` from the workspace after a ghost's create | `GraphDocumentTests` (rule L's paths, the load token and its receiver, the probe, selection, actions, activation, the create funnel, the §W-A comparison against `graph_queries`) + `GraphTableTests` (columns, the sort on adoption, the row Name and ItemStatus, the cell lookup, the switcher, the states, the 10k virtualisation) + `GraphAnnouncerTests` + `Censuses/GraphAnnouncerCensus` (the funnel guard, the one caller, the one cell lookup, no typed header, no mutable shadow) + `Censuses/AnnouncementSeamCensus` (the graph arm) + `Censuses/GraphQuerySurfaceCensus` + `Censuses/GraphContractsCitationCensus` + FlaUI `GraphSurfaces_TableSortSelectionAndActivation_AreClean` (the switcher, the headers, the row Name, the summary, Ctrl+Alt+S, Enter activation, axe) — CI-required; `GraphSurfaces_TableSortSelectionAndActivation_AreClean`; `TheDocumentsRowsAndSummaryEqualTheArtifactsUnderTheArtifactsFilter`; axe: `graph-table` | Pending | Pending | Pending |

| Graph connections leaf (W6-2 PR B) | Tree / TreeItem, with every row projected through `ConnectionsRowDataPeer` — WPF's tree DATA-ITEM peer, the object a UIA client reads, at BOTH nesting levels; ComboBox for the depth; Text for the summary and the state; the state's host is the focus anchor — The right pane's `connections` leaf body (`ConnectionsLeafBody`): the heading `ConnectionsHeading` (the note's file name; "Connections" with no note), the summary region `ConnectionsSummary`, the depth control `ConnectionsDepth`, the state label `ConnectionsStateText` under the anchor `ConnectionsLeaf`, and the tree `ConnectionsTree` with its two group rows | Row Name is core's `GraphRow` render at the set verbosity (B-8) through the workspace's one `GraphAnnouncer`; ItemStatus is the badge — Unresolved before Embed before Attachment, the mac's precedence; HelpText is the mac's activation hint ("Opens the note." / the ghost's); the group headers are "Linked from, N notes" / "Links to, N notes" and their empties the mac's; the summary is core's `GraphNeighborhoodSummary` render; the state labels T1–T5 and the depth control's name, hint and tags are `ConnectionsPhrase`, the mac's byte for byte (B-16) | Tree; SelectionItem, ExpandCollapse and ScrollItem from WPF, plus Invoke added on the data-item peer at both nesting levels (B-9, B-13); Selection on the tree; Selection and ExpandCollapse on the depth ComboBox | `slate.graph.showConnections` reveals the leaf for the note in view (palette; chordless), `slate.graph.connectionsDeeper` / `slate.graph.connectionsShallower` change the depth (palette; always enabled, a bound a no-op through core's clamp); Return opens, Ctrl+Return opens in a new tab, Enter on a ghost creates its note; Alt+Up / Alt+Down move between the groups; Menu / Shift+F10 open the row's actions with Show connections listed and disabled (its reason on the action alone, B-D6); the right-pane focus boundary lands on the anchor and Ctrl+Alt+Left from inside the tree returns to the editor (rule C, Term 9) | The graph family only, through the workspace's ONE `GraphAnnouncer` (A-10 as amended, BD-12): the summary on a mount, a stale switch, a Show, a root change or a depth change, spoken only when the leaf is ACTIVE at apply; `GraphBlocked{ConnectionsLoadFailed}`; the panel line once per invocation (B-D5); `LoadingConnections` / `NoConnections` on focus entry with nothing to read (0a-D3); the shell's own lines unchanged | `ConnectionsLeafTests` (the record and the golden's projection, the presentation and the request, the receiver's rejections, the trigger matrix, the probe's state machine, supersession, SPEAKING at apply, the selection scope, focus entry, the ghost create, the commands, the labels) + `ConnectionsLeafViewTests` (the four patterns at both levels, the occurrence identity, expansion and its clears, the state labels, the depth control, the row menu) + `GraphAnnouncerTests` (the stored gate, the cross-surface flush, the live relay) + `Censuses/ConnectionsLeafCensus` (the triggers, the recorder and reconciler, the reveals and the seed, the gated count, the depth's dataflow, the leaf body and the boundary) + `Censuses/GraphAnnouncerCensus` (the instance census) + `Censuses/AnnouncementSeamCensus` (the two boundaries) + FlaUI `GraphConnections_LeafWalkDepthAndReRoot_AreClean` (the heading, the tree, the four patterns on a top-level and a nested row, the summary, the group headers, the row's Name and ItemStatus, the depth control before and after a change, a ghost's Create note, Left to the editor, axe) — CI-required; `GraphConnections_LeafWalkDepthAndReRoot_AreClean`; `TheLeafsTreeIsTheSessionsRecordFieldByFieldForEveryPinnedPair`; axe: `graph-connections` | Pending | Pending | Pending |

| Canvas navigator, filter and Where-am-I (W6-1 PR C) | Edit for the filter field; Text for the summary region (its own focus stop); Group for the Where-am-I panel with a read-only Edit inside it; Button for Clear/Commit/Cancel; the surface itself (a peered `UserControl`) carries the mode's ItemStatus — The canvas-wide COMMAND LAYER every projection hosts (not a fourth view): the header's filter field (`CanvasFilterField`, "Filter cards") with its result-summary region (`CanvasFilterSummary`, "Filter results: …") and Clear button (`CanvasClearFilter`), the transient Where-am-I panel (`CanvasWhereAmIPanel` with the focusable readback `CanvasWhereAmIReadback` and its `CanvasWhereAmIClose` button), and the mode stack's visible controls (`CanvasCommitMode` / `CanvasCancelMode`, shown only while a mode runs) | The filter field's Name and hint are mac's verbatim; the summary's Name carries its own value ("Filter results: 3 of 40 cards match") because a bare region name would replace the text a reader wants; the Where-am-I readback is ONE render of the `CanvasWhereAmI` event — the same string the announcement speaks; the mode's ItemStatus is "⟨Mode⟩: ⟨card⟩" composed from the same typed fields core's mode sentence uses and pinned against it — the field's Name is "Filter cards" and the summary's "Filter results: …" | Value on the filter field and on the Where-am-I readback; Invoke on Clear/Commit/Cancel and on the panel's Close; ItemStatus on the surface (M3); `LiveSetting=Off` on the readback — pull, not push | `ChordScope.Canvas` chords delivered from the SURFACE's tunnelling handler, so they are live wherever focus sits inside the surface — the filter field, the summary, a banner, the mode buttons — and R2's "only while a projection has the keys" is enforced PER ARM by the verbs that need it (`ProjectionHasFocus`) rather than by the delivery, which is what lets Ctrl+F reach the field from the field and lets the palette drive the same verbs with no keys at all (contract C1/C2) — Ctrl+F to the field, Ctrl+Alt+Shift+I to the panel, Down/Up deferred to the projection and answered by the navigator only at the boundary, Right/Left following a connection ALWAYS on the outline (a connectionless card answers rather than falling silent — mac's own rule; expand/collapse keeps Enter-on-group, WPF's numpad +/-, and the ExpandCollapse pattern, and the table keeps Left/Right for the grid's cell navigation), Enter committing an active mode only while a PROJECTION owns the keys — the gate C3 chose over mac's bubbling precedence, so anything else focusable in the surface (the result summary, the onboarding region, the warning rows, a banner, the switcher radios) leaves Enter alone rather than the code carrying a list of control types that own it; Escape walks the M5 ladder ONE rung per press (mode → filter → transient region → the workspace, unconsumed) from the SURFACE, never the window, so the shell's own Escape is untouched — and while the Where-am-I panel is OPEN Escape is the PANEL's, ahead of every rung and wherever the reader is standing (CD-47 keys on the panel being open, not on focus being in it) — closing it with any filter needle intact, and returning focus to the element they came from only if they were INSIDE it, because dismissing a panel someone was not in must not relocate them | Movement announces `CanvasMovedTo` at the LIVE verbosity (read at every announce); dead ends answer with the typed family (`End of canvas.`, `Start of canvas.`, `At canvas level.`, `Group "X" is empty.`, `No outgoing connection.`, `No outgoing path from "X".`, `No cards match the filter.`); the filter count rides the announcer's 200 ms filter class; Where-am-I is always verbose-grade; every read verb answers in every load state through one state→response mapping, and a query that threw is never reported as a query that came back empty | `CanvasNavigatorTests` (the read mapping over every load state, every verb answering in every state, movement over `sample`/nested/cycle/2,000-node fixtures incl. dead ends, group boundaries, multi-edge follow and the cycle-safe trace, filtered-set movement with both projections narrowed by one answer, the filter's count/summary/clear, Where-am-I's panel↔speech equality with mode and filter clauses, live verbosity at three levels, the ladder end to end, M3's ItemStatus off the surface's own peer, the filter field's Value pattern, the Where-am-I chord landing focus, `AFocusDeliveryToANodeOtherThanTheSelectionDoesNotDouble` in BOTH projections, `EscapeInsideThePanelWhileFilteringActsOnThePanel`, `ARightArrowOnAConnectionlessLeafStillAnswers`, `ExpandCollapseSurvivesTheArrowsBeingClaimed` and `EnterOnAFocusedModeButtonActivatesTheButtonNotTheChord`) + `CanvasModeControllerTests` (M1–M7 over every mode, the refused-commit arm, the M4 departure table, the M5 rung table) + `CanvasAnnouncerCensus` (`TheSnapshotVisibilityPredicateMatchesTheSurfaceRender`, `TheShellInstallsTheModalOverlayAnswerForModeCancellation`) + `ChordTableTests` (the Canvas-scope scrape from `CanvasNavigator.Bind`, both directions) + FlaUI `CanvasSurfaces_NavigatorFilterAndWhereAmI_AreClean` (Ctrl+F focus and Value, the summary element, the outline narrowing, one-rung Escape, the Where-am-I chord landing focus in the panel, axe) — CI-required; `CanvasSurfaces_NavigatorFilterAndWhereAmI_AreClean` (the filter, its summary, Where-am-I); `CanvasModes_MoveResizeAndConnectPicker_AreReachable` (the mode HUD's Commit/Cancel with the mode); `TheZoomVerbsSpeakCoresZoomEventWithTheirContext`; axe: `canvas-navigator`, `canvas-move-mode-active` | Pending | Pending | Pending |

| Canvas visual (W6-1 §D) | A Group container peer whose children are one Button peer per MATERIALIZED card (windowing: viewport plus one margin each side; a retained off-window peer is a virtualized placeholder; a first-touch search answers from the population's descriptor index); labelled connections contribute one peer each, unlabelled edges none (DD-2) — The `.canvas` tab's visual projection: the spatial board (`CanvasRendererView`, "Canvas visual view") in the same surface body, reached from the switcher's Visual arm or `slate.canvas.showVisual`; exactly one projection stays in the tree (B11 widened to three arms) and outside Ready the arm shows the state pane, never an empty canvas | Card Name is core's `speakable_name` verbatim (CD-23 — the renderer composes nothing); the container's Value is "Zoom N percent" through a declared read-only Value pattern (DD-5); ItemStatus carries "marked" as read-only decoration (§G owns its semantics); card fills are the six preset tints composited at 0.18 over the surface token with borders as the solid tints, an author hex takes the SAME arithmetic at runtime, and the color's MEANING travels as text in the outline, table and Where-am-I — never color alone (D13) | Selection (single: CanSelectMultiple false, Select replaces, AddToSelection with another card selected throws — D6's matrix cell by cell), ItemContainer find-by-property over unrealized cards, VirtualizedItem ALWAYS on placeholders with action patterns gated on realization, Invoke = the announced select, Value on the container; Invoke and SelectionItem on each card peer, VirtualizedItem on a retained off-window peer (the container declares Value, Selection and ItemContainer) | The renderer is ONE focus stop after the switcher; arrows are the navigator's (Down/Up reading order; Right/Left stay the outline's per frozen C1); Ctrl+= / Ctrl+- / Ctrl+0 zoom (centre-preserving, clamp 0.1–4.0, step 1.25), Shift+1 fit and Shift+2 zoom-to-selection on the VISUAL surface only when the projection owns the keys (R2, both halves); realization and on-surface selection always scroll into view, external selection pans only while follow-selection is ON (D4's origin-sensitive rule); viewport transforms are instant in every mode (DD-7) | Zoom and fit announce `CanvasZoom` with the container Value updating in the same commit; selection through the announced door speaks `CanvasMovedTo` at the live verbosity; a viewport verb with no addressable pane answers the typed `CanvasViewportNoPane` refusal, and zoom-to-selection with nothing selected answers the canonical no-selection sentence BEFORE any pane is consulted (D14) | `CanvasPresentationEngineTests` (the dispatcher-owned commit, both barrier orders, single-flight coalescing, the shut-down guard) + `CanvasPeerTopologyTests` (windowing, the crossing-edge rule, discriminated identity, tombstones) + `CanvasPaletteTests` + `CanvasTextScaleServiceTests` + the windowed facts in `CanvasDocumentTests` (the third arm rendering the ready publication; a viewport verb moving the installed zoom) + `ThemeTokenContrastTests` (the canvas APCA matrix, the hostile hex, the token-drift census) + `CanvasEnablementCensus` (the flip, derived) + `CanvasRendererBenchmarks` (the three §K budgets asserted) + the FlaUI visual journey — CI-required; `CanvasSurfaces_VisualBoardPeersAndZoom_AreClean` (peer traversal, Invoke, zoom); `TheZoomValueIsCoresRenderMinusItsPeriod`; `FitCanvasContainsAndCentresCoresBounds`; axe: `canvas-visual` | Pending | Pending | Pending |
| Canvas card editor (W6-1 §E) | The modal sheet a text card's activation, Enter, Edit Card Text… and a New Card landing open (`CanvasCardEditorSheet`, a named landmark Group through `AutomationNamedGroupPeer`) holding the multi-line text field `CanvasCardEditorText` (Edit) | The sheet's Name is the card's reference; the field's Name and its commit/cancel hints are XAML strings the modal census scans; Escape commits (TE-11b) | Group; Value and Text on the field | Focus lands in the field on open; Tab cycles inside the sheet (the modal keyboard fence); Escape commits and returns focus to the row (§G2 TG2-4's receipt) | The commit publishes through the funnel and speaks core's confirmation; a refused commit speaks the blocked arm | `CanvasAuthoring_NewCanvasCardEditorAndUndo_AreReachable` (open by chord, type, Escape commits, undo); `AuthoringLoopThenUndoChainRestoresTheCommittedBytes` (the commit through `CanvasCommitCardEdit`); axe: `canvas-card-editor` | Pending | Pending | Pending |
| Canvas prompt sheets (W6-1 §F, §G, §G2) | The one prompt sheet (`CanvasPromptSheet`, a named landmark Group) every staged verb presents: a label prompt with the answer field `CanvasPromptDraft` (Edit — New Group…, Add Link Card…, Rename Group…, Set Color…, the connection label, Convert Card to Note…), a choices prompt with the list `CanvasPromptChoices` (List / ListItem — Move into Group…, the connection direction, the connection picker, the connected-card direction) and the marks list with its `CanvasPromptClearMarks` Button; one sheet at a time (§G TG-1) | The sheet's Name is the prompt's `Title`; the field's Name is the XAML hint; a choice item's Name is the choice's `Name` and its ItemStatus the choice's `Status` (the container style binds both — §G2 TG2-9's fix); the button's Name is XAML | Group; Value on the field; Selection / SelectionItem on the list; Invoke on the button | Focus lands on the successor's first field after an atomic stage swap (IG2-45); Tab cycles inside; Enter submits (on the marks list Enter jumps, Delete unmarks); Escape cancels | Submit outcomes land on the workspace's dispatcher; refusals speak their typed arms through the document's boundary methods | `CanvasMarks_ToggleListJumpDeleteAndUndo_AreReachable` (the marks list, Jump); `CanvasVerbs_GroupConnectDuplicateLinkConvertAndUndo_AreReachable` (New Group…, Move into Group…, the two-stage Edit Connection… with the focused element read after the swap, Add Link Card…, Convert Card to Note…); `TheSubmitFamilyAndTheChoiceStatusAreShapedAsFrozen`; axe: `canvas-marks-list`, `canvas-verbs-group-prompt` | Pending | Pending | Pending |
| Canvas pickers (W6-1 §E, §F, §G2) | The one picker sheet slot (`CanvasCardPickerSheet`, a named landmark Group) typed `ICanvasPickerSheet`: the card picker (Connect To…, Align With…, Place…) and the vault file picker (Add Note to Canvas…, Add Media…, Locate File…), each with the filter field `CanvasCardPickerFilter` (Edit) and the rows `CanvasCardPickerRows` (List / ListItem) | The sheet's, field's and list's Names are the picker's `SheetName`, `FilterName` and `RowsName`; a row's Name is its `Label` and its ItemStatus its `Status` (the container style binds both) | Group; Value on the filter; Selection / SelectionItem on the rows | Focus lands in the filter; typing narrows; Enter picks the selected row; Escape cancels; a stale pick refuses "pick a different target" (IG2-49) | The pick applies through the funnel and speaks core's confirmation | `CanvasModes_MoveResizeAndConnectPicker_AreReachable` (Connect To…'s picker); `CanvasVerbs_GroupConnectDuplicateLinkConvertAndUndo_AreReachable` (Add Note to Canvas…'s vault picker listing the vault's note); `AStaleVaultPickRefusesPickDifferentTarget`; axe: `canvas-card-picker`, `canvas-verbs-note-picker` | Pending | Pending | Pending |
| Canvas context menus and row actions (W6-1 §E, §G2) | The outline row's context menu and the grid's row-actions menu (Menu / MenuItem, WPF's own peers), both built from the ONE plan `CanvasContextMenuPlan` over (surface, target): node rows in mac's order, connection rows on their captured edge, the grid's subset | Item Names are the plan's labels (`CanvasContextMenuPlan.Label`, mac's verbatim); a staged item would carry its reason as HelpText — none remain | Menu; Invoke on items | Shift+F10 / the Apps key opens on the focused row; arrows walk the items; Enter invokes through the one dispatch (`CanvasContextDispatch`), which seats the row silently first; Escape closes | Each verb speaks as the command it is; the row is reseated before the verb | `TheOutlineMenuEqualsThePlan`; `TheRowMenuEqualsThePlansGridProjectionAndToggleMarkIsLive`; `AConnectionRowsVerbsActOnTheCapturedEdgeFromItsSeatedSource`; `CanvasSurfaces_TableGridSortSelectionAndActivation_AreClean` (the row-actions leg at the real keyboard); axe: none — a popup HWND outside the window scan (the substrate's note) | Pending | Pending | Pending |
## Wave-close status

**W3 wave closed 2026-07-30** — all five issues merged (W3-1 #1052 · W3-4 #1054 · W3-2 #1057 · W3-3 #1058 · W3-5 #1059), each shipped through the full per-issue loop: adversarial review to approval (7–9 rounds per issue), PR, CI green on both twins. The wave grew the Windows suite 250 → 351 tests and added four §W-A parity sections byte-checked cross-platform (`code_blocks`, `math_blocks`, `diagram_blocks` — SVG bytes deliberately excluded as machine-dependent — and `embed_resolutions`, the first cross-platform pins of MathCAT speech/braille and embed resolution; the fixture vault now carries binary and `.canvas` targets). Dependency currency checks were discharged in-wave per the 07 §4.2 doctrine — WPFMath 2.1.0 (2026-07-27, pass with caveats: coverage subset matrix-rowed, #1057) and Svg.Skia 5.1.1 (2026-07-28, pass with caveats: click-directive-href fallback triaged, G19 row) — next re-run at W4 close. Owner-visible divergences recorded G21–G25; upstream defect issues filed #1053 (mac consumes core code preamble) and #1056 (MathSpeak unimplemented in MathCAT). Human AT status per row above: W3-1 NVDA field-verified 2026-07-26/27; W3-2/W3-3/W3-4/W3-5 rows pending field passes.

- Automated local at W3 close: 351/351 Windows tests, 3/3 accessibility tests (production startup, bounded transient-UIA retry, and the reading identity contract with cross-process math/diagram assertions), 1,788 `slate-core` lib tests (nextest ci profile green in CI on every W3 PR), evidence validation for 182 command rows (52 chorded)/16 leaves via `generate-parity-matrix.py`.
- Automated local at W2 close: 212/212 Windows tests, 2/2 accessibility tests (production startup plus bounded transient-UIA retry), the complete Windows `slate-core` non-census package tier (1,616 unit, 364 integration, and 2 doctests), evidence validation for 65 command rows/seven surfaces, and all W1/W2 accessibility code compiles. Repository-contract tests pin accessibility artifact retention and the HostLogProbe build-only dependency, while structure censuses guard representative sidebar filter/tree/import/session-work and workspace persistence/layout declarations. Behavioral tests, rather than source strings, enforce shutdown admission/draining, anchored replacement-redirection, post-commit cleanup safety, bounded ordered/reentrant scan progress, process-wide Quick Open serialization with canceled-queue rejection, ordinary child-expansion blocked-provider return/supersession/refresh precedence and cancellation cascade/detached admission/shared-provider serialization/canceled queues/private failure/restoration/close/retry/disposal, and operation correctness.
- Automated interactive: pass on final PR #1031 revision `d2da2cdb023bf0a988b9ba52d2046f8287c61825` in [Windows Actions run 29990822473](https://github.com/coryj627/slate/actions/runs/29990822473). The complete non-census core and CLI tiers ran in the same successful job as the live RT13 tree assertions, RT17 2/2 scan RangeValue plus exact sidebar pattern census, and RT18 Windows tests. The retained `slate-windows-accessibility-d2da2cdb023bf0a988b9ba52d2046f8287c61825` artifact plus its passing TRX and dated, revision-bound `workspace`, `quick-open`, and `welcome` axe evidence steps were green; expanded empty shortcuts retain nonzero accessible geometry.
- W2 editor interactive evidence is CI-required by `ShellAccessibilityTests`. The keyboard/popover/menu assertions **passed** on the W2-3 deliverable ([#1039](https://github.com/coryj627/slate/pull/1039), merged as `50482b1`); they are no longer awaiting a run.
- W6-1 (canvas, #745, PR H): the eight canvas rows above are pinned to the shell by `WcMatrixCanvasEvidenceCensus`; their human cells stay Pending until the named run recorded in [reports/w6_1_canvas_at_checklist.md](reports/w6_1_canvas_at_checklist.md) — T's ten items re-expressed for UIA, the release residual (§H HD-3).
- Human AT: Narrator, NVDA, and JAWS are pending for every row. Per WGA-9, the milestone cannot be called release-complete until NVDA/JAWS evidence is recorded; Narrator is smoke scope.
- Contrast themes: runtime two-layer Fluent + Slate switching is implemented. The four built-in themes plus one customized-theme human pass remains pending.
