# Gap analysis — program vs. milestone-22 text vs. portability review

Convention (17_bases precedent): every deliberate divergence between this program and its upstream authorities, each with a reason. The program supersedes upstream where they differ (`../../06_v1_milestones.md` rule). Upstream texts: [`../01_milestone_brief.md`](../01_milestone_brief.md) (vendored milestone 22 description) and [`../../07_portability_review.md`](../../07_portability_review.md).

| # | Upstream said | Program does | Why |
|---|---|---|---|
| G1 | Milestone brief workstreams: W6 = UIA a11y, W7 = settings/theming, W8 = packaging+verification; no home for canvas/graph/Bases/history/Excalidraw/autocomplete/LaTeX (the brief predates T/P/N/O/V/X/FL/XD specs). | **Renumbered + extended:** W6 = structural surfaces (canvas, graph), W7 = UIA program, W8 = settings + theming + packaging + close-out. Post-brief milestones land as rows in W2 (V/X), W3 (XD), W4 (N/O), W6 (T/P); milestones created after this program map the same way — E note export → W5/W8 rows (export commands + settings), PD image OCR → W3/W4 rows (render/panel; engine seam per PD decision 4 + program decision 4's adapter clause). | The parity surface grew ~2× since the brief; structural surfaces are the two biggest ports and need their own wave; settings/theming/packaging compress into one close-out wave without loss. |
| G2 | Brief freezes parity as a list of mac Swift file names per workstream. | Parity is defined by a **generated matrix at port start** (W0-4); file lists in the brief are treated as illustrative, not normative. | The mac app keeps shipping while W is parked; a frozen list would silently under-scope (e.g. `FileListSidebar` is already superseded by the FL program). |
| G3 | Brief/07 §4.1 spike scope: "callback + object-handle + cancellation." | Probe surface adds **`CommandAction`** (a further `with_foreign` trait alongside `ScanProgressListener`; line anchors kept current in the w0 baseline), a **`DocumentBuffer` apply-edit hot path**, and **`VaultError` mapping totality**. | Verified in code 2026-07-06: command invocation re-enters the host through a foreign callback — it's on the W5 hot path; the keystroke path is the §W-B budget carrier; error mapping is where raw P/Invoke quietly aborts. *(2026-07-12: the FFI grew a third `with_foreign` trait post-authoring — `VaultEventListener`, O follow-ups #791/#846 — now part of the probe surface; see w0 baseline.)* |
| G4 | 07 §3 W0.5 list: command palette + announcements. | Adds **quick-switcher ranking** (`QuickSwitcherModel.score`, shipped with #565 — after the review). | Same drift class, same fix window; cheaper before the port than after. |
| G5 | Brief: "JAWS/NVDA matching the VoiceOver semantics." | Pins **Narrator = smoke-only** (full support reserved as W-E1); pins the tooling gate (axe-windows 0 failures via FlaUI, §W-C matrix instrument, human passes per T's residual convention). | Three-AT full conformance triples the human verification load without being the product bet; the brief's acceptance was tool-silent and needed an executable form. |
| G6 | Brief acceptance: "cross-platform differential check" (one line). | Formalized as **§W-A**: three-job CI pipeline (mac + windows + diff), every read-side FFI surface, byte-identical modulo an exhaustive normalization list (W8-4). | "Check" without a mechanism becomes a demo; the census discipline is the repo's standard for load-bearing invariants. |
| G7 | Brief silent on testing stack. | Pins xUnit + FlaUI + BenchmarkDotNet (decision 17). | Somebody has to; recorded here so it's a decision, not an accident. |
| G8 | Brief scope = "100% parity with macOS." | Adds **Windows-only obligations** (decision 15: IME composition, per-monitor DPI, single-instance, jump list, file associations, High Contrast themes) — capabilities macOS never needed. | A parity-only reading would ship an app that feels broken *as a Windows app*; High Contrast in particular is an a11y-first obligation. |
| G9 | Brief silent on CLI. | `slate-cli` builds + tests on the Windows runner (W0-3); distribution/packaging reserved (W-E5). | The CLI is already 100% portable per 07 §3; proving it in CI is nearly free; shipping it is a separate product decision. |
| G10 | 07 §5 non-editor follow-ups #3 (APCA shared spec), #4 (graph canonical representation), #6 (heading-nav pass), #7 (sync registry). | #3 → owned by Milestone R, W8-2 consumes; #4 → owned by Milestone P, elevated to a W **entry criterion**; #6 → W3-1 (native UIA heading nav) + outline-as-feature (W4-2); #7 → W4-8 with Windows probes core-side. | These were review recommendations without owners; the program assigns them to the milestones that own the code, and takes only the consumption side. |
| G11 | Brief: csbindgen named in 05 §2.3 as the C# generator. | The generator is decided by the W0-1 spike; ADR-13's "one FFI definition feeds all generators" is the tiebreak preference, and the FFI's **proc-macro (UDL-less) style** is a pinned evaluation constraint. | 05 §2.3 predates the 07 §4.1 asymmetry finding; the milestone brief itself already walks it back to a spike — the program adds the proc-macro constraint discovered in code. |
| G12 | Brief: "W0.5" as a separate half-phase. | W0.5 issues live inside the w0 spec/wave with explicit **pre-unpark eligibility** (also W0-1). | They're mac-side refactors verifiable today; letting them start early shortens the port's critical path without violating the parking decision. |
| G13 | Parking note: "revisit once T + P are delivered and the Mac alpha is feature-complete." | Entry criteria sharpened to four testable gates + an owner call on queue state (program §Entry criteria). The brief's "months 9–12" calendar claim (§Goal) is likewise superseded: no calendar commitment survives, only the gates. | "Feature-complete" is unfalsifiable while the roadmap grows; the owner's standing instruction is "majority of open milestones ship first," which the matrix mechanism absorbs. |
| G14 | Brief/decision-6 text: math speech is "MathML + **MathCAT**" (engine named). | W3-2 binds to the **canonical speech-artifact contract** (the host never generates speech); core's `math.rs` already delegates to MathCAT today, so behavior is identical — the wording difference is recorded here, not silently. | Naming the engine in a host spec would couple the port to a core implementation detail; the artifact is the contract (05 §1.2). |
| G15 | *(none — a W-imposed constraint on another milestone, recorded for visibility)* | W2-4 presumes **Milestone V ships its completion a11y announcements via the canonical vocabulary** (W0.5-3). Filed on V as [#888](https://github.com/coryj627/slate/issues/888); V implementers: keep announcement strings/triggers in one marked module if #719 hasn't landed, so the W0.5-3 inventory sweeps them mechanically. | W2-4's parity premise must not fail silently when V ships first. |

## 2026-07-14 live accuracy and completeness audit

Scope: all **46** live Milestone W issue bodies, the milestone description, this program and every W0–W8 executable spec, and the current implementation contracts at `4acd18a`. The audit also checked the live entry milestones and Microsoft's current [Contrast themes guidance](https://learn.microsoft.com/en-us/windows/apps/design/accessibility/high-contrast-themes). Historical snapshots (`01_milestone_brief.md`, `02_handoff_audit_2026-07-11.md`, and `03_codex_adversarial_review_2026-07-12.md`) remain dated evidence and are not rewritten as if they described today's inventory.

| ID | Severity | Gap and verified evidence | Remediation / executable contract | Synchronized artifacts |
|---|---|---|---|---|
| WGA-1 | P1 — execution-blocking | Milestone 22 gained [#911](https://github.com/coryj627/slate/issues/911), but the program still described a 45-issue bijection and no W phase owned it. `vault/fs.rs::rename_no_replace` is atomic on macOS/Linux-glibc but uses check-then-`fs::rename` on Windows/musl, leaving a real destination-clobber TOCTOU window. | Add **W1-0** as a one-issue/one-PR portability prerequisite. It uses an atomic no-replace primitive on Windows, fixes or fails closed on other fallbacks, and gates W1-2 file mutation. | Live milestone; `00_program.md`; `w1_spec.md`; #911; #721. |
| WGA-2 | P1 — execution-blocking | The corrected W0 spec requires W0-1 → W0-2 → W0-3 because #715 consumes the runner/scaffold created by #603. Live #715 had regressed to “depends on W0-1 only” and claimed W0-2 parallelism. | Restore the sequential dependency in the executable issue body; no program/spec change is needed. | #715. |
| WGA-3 | P1 — behavioral correctness | The program and #724 claimed mac text undo routes through the core op-log. Current mac routing has three domains: note text uses the responder-chain `NSUndoManager`, while canvas and structural operations use separate core-backed domains (`SlateMacApp`, `AppState`, `StructuralUndoTests`). | AvalonEdit `UndoStack` owns editor-text undo; undo/redo changes still feed `DocumentBuffer` deltas and save through the normal core/op-log pipeline. Focus routes text, canvas, and structural undo to the correct domain. | `00_program.md`; `w2_spec.md`; #724. |
| WGA-4 | P1 — data ownership | `RecentVaultsStore` and `CommandPaletteRecentsStore` are global host-local files under macOS Application Support. `.slate/prefs.json` is per-vault synced state. The specs conflated these and offered per-vault prefs/SQLite as storage. | Core owns ranking/recency policy, state transitions, and serialization shape; thin host adapters own global app-data paths and atomic I/O. Windows uses `%LOCALAPPDATA%\Slate\{recent-vaults,command-palette-recents}.json`; neither store enters vault prefs. | `w0_spec.md`; `w1_spec.md`; #717; #720. |
| WGA-5 | P1 — cross-platform data integrity | The docs said “Slate writes LF” and “LF canonical.” Current `read_text` decodes supplied bytes, `save_text` writes supplied contents, and CRLF-preservation tests/specs treat authored CRLF as data. No global normalization contract exists. | Do not normalize line endings in the parity harness. Require identical output for identical input + edit sequences over LF, CRLF, and mixed fixtures; any future normalization policy needs its own explicit migration/behavior decision. | `00_program.md`; `w1_spec.md`; `w8_spec.md`; #720; #754. |
| WGA-6 | P1 — accessibility correctness | The docs applied APCA Lc ≥ 75 to `SystemParameters.HighContrast` values. Windows Contrast-theme colors are dynamic and user-customizable; Microsoft requires compatible `SystemColor` pairings and warns against hard-coded colors. Slate cannot truthfully gate colors the user controls. | APCA gates Slate-owned dark/light pairs. Contrast themes bind semantic roles to dynamic compatible system pairs and are tested across all four built-ins plus a customized theme, runtime switching, selected/disabled states, visible boundaries, and no color-only meaning. | `00_program.md`; `w1_spec.md`; `w6_spec.md`; `w8_spec.md`; #720; #745; #752. |
| WGA-7 | P2 — ownership ambiguity | Program decision 5 said trigger conditions move into core, while W0.5-3 correctly described host interaction sites posting typed core events. Moving UI trigger logic into Rust would violate the permitted host-state-machine boundary. | Core owns event kinds, parameters, templates, priority, and rendering. Host interaction sites own when an event fires; scenario tests and §W-D prove trigger parity. | `00_program.md`; `w0_spec.md`; #719 already matched and required no body edit. |
| WGA-8 | P2 — stale status | Milestones T (20) and P (16) are now closed with 0 open issues, satisfying entry criteria 1–2. W remained generically parked without a current gate snapshot. | Record the dated snapshot: criteria 1–2 met; queue-state owner call, W0.5, and W0-1 remain. Do not unpark W automatically. | Live milestone; `00_program.md`; this audit. |
| WGA-9 | P1 — release-gate contradiction | W8-6's issue body said the milestone stays open until the human JAWS/NVDA pass is recorded, while `w8_spec.md` said W8-6 closes “modulo” that residual. | The human residual is release-blocking: W8-6 closes the milestone only after the recorded pass. | `w8_spec.md`; #756. |

## 2026-07-19 W0-4 executed — G16 recorded

| # | Upstream said | Program does | Why |
|---|---|---|---|
| G16 | w0_spec §W0-4 item 1: the command-registry dump is "driven via the mac app/test target: the registry is populated at runtime by the host." | `scripts/generate-parity-matrix.py` reads the **drift-test-enforced source catalog** instead: `SlateCommandID` (whose stability contract + `SlateCommandsTests` assert every id resolves to a registered `Command`) and the registration chord tables. | W0 executed from a Windows machine with no mac hardware; the catalog is the same id set the runtime registry carries, enforced by the mac drift tests on every CI run. A runtime dump on mac hardware can reconcile at any time — matrix drift = re-run, diff, re-triage. |

## 2026-07-20 W1 executed — G17–G18 recorded

| # | Upstream said | Program does | Why |
|---|---|---|---|
| G17 | W1-3 prefers a shared core workspace store if canonicalized by execution time; otherwise it permits same-shape JSON with a schema pin. The mac host also owns sidebar organization state. | Windows writes mac schema-v1-compatible `.slate/workspace.json` and `.slate/sidebar.json` through bounded, fail-closed host stores. Unknown workspace tab discriminators drop only that tab; unknown sidebar siblings and reserved shortcut kinds round-trip. Operations hold verified vault/`.slate` handles, reject reparse children, verify child identity, and issue final replacement as a simple same-directory rename on the already-open temporary source handle. | No canonical core store exists. Same-shape JSON provides cross-platform restoration now without inventing a second schema. The Windows host boundary now fails closed on redirected child opens and does not reopen ancestors for final replacement; deterministic native-buffer, post-commit-cleanup, and adversarial external-sentinel coverage pin that guarantee. Canonicalization remains the preferred architectural follow-up, not an unclosed host security dependency. |
| G18 | Decision 12 maps Command→Control and Option→Alt by convention, but the mac chords `Control Command Left/Right` would then collide with the focus-pane chords; mac import cancellation is Command-Period. The parity matrix previously inferred all W1 delivery from the owning issue number. | Windows uses **Ctrl+Alt+Shift+Left/Right** for move-tab-left/right, preserving **Ctrl+Alt+Left/Right** for focus-pane navigation, and uses **Escape** to cancel while import owns the operation (overlay dismissal retains precedence outside import). `apps/slate-windows/chords.json` records these divergences and now explicitly maps every W1 command to validated implementation/test evidence groups; generation fails on missing, extra, stale-file, or stale-marker evidence. | The added Shift modifier makes both spatial command families reachable without collision. Escape follows the Windows cancellation convention and remains context-scoped. Evidence-backed status prevents a newly inventoried command from becoming “implemented” merely because its issue starts with #720–#723. |

## 2026-07-25 W3-1 spike + owner calls — G19–G20 recorded

| # | Upstream said | Program does | Why |
|---|---|---|---|
| G19 | **SharpVectors** is the SVG/diagram renderer, named in `00_program.md:61` (decision 7), `01_milestone_brief.md:19,34`, and `w3_spec.md:37,41` — where W3-3's acceptance requires a "**SharpVectors** currency check" in its PR. | The shipped renderer is **`Svg.Skia` 5.1.1** (`SlateWindows.csproj:15`), already load-bearing: `EditorInteractions.cs` uses `SKSvg`/`SvgDocument` for the editor's inline SVG path, including the hardened `ParseSecureSvg` loader. It entered in **W2-3 ([#1039](https://github.com/coryj627/slate/pull/1039))**, ahead of the wave that specified the choice. | The divergence is already in shipped W2 code, so re-litigating it in W3-3 would mean either a second SVG stack or ripping out a hardened path that works. Recording it makes the substitution deliberate. **Consequence for W3-3:** the currency-check obligation in `w3_spec.md:14,41` must be re-pointed at `Svg.Skia`, or it will be discharged against a library the app does not use — a check that passes while proving nothing. **Discharged 2026-07-27 (#730 PR): pass with caveats** — shipped 5.1.1 IS the current release (2026-06-15), actively maintained, zero advisories, net10.0-windows load-bearing since W2-3. Triaged fallback recorded per the never-silent rule: Mermaid **click-directive** diagrams emit external `href`/`xlink:href` attributes that the W2-3 `IsSafeSvg` sanitizer rejects, so they land on the source-visible fallback by design (the sanitizer stays mandatory even for locally-generated artifacts); mermaid-rs-renderer 0.3.1 emits zero `foreignObject`, mooting Svg.Skia's HTML-in-foreignObject limitation on three independent grounds (renderer never emits it, static renderer never draws it, sanitizer rejects it). |
| G20 | Mac conveys list structure by **flattening**: reading-view list rows are not nested, and depth is announced through an AX *value* string, `"list item, level N"` (`ReadingView.swift:610,639`). Parity doctrine is that Windows matches mac. | Windows exposes **native UIA `List`/`ListItem`** so JAWS/NVDA `L`/`I` quick-nav works, per `w3_spec.md:23` and program decision 6 ("no outline crutch"). Owner call, 2026-07-25. A follow-up brings **mac** up to the same semantics rather than leaving a permanent divergence — filed as [#1048](https://github.com/coryj627/slate/issues/1048). | `w3_spec.md:23` is stricter than mac's shipped behaviour, and the stricter reading is the correct one: an AX value string is not list navigation, and a reader cannot jump between items with it. Converging mac upward keeps the two hosts aligned without ratifying the weaker behaviour. **Cost discovered by the W3-1 spike** ([`w3_1_container_spike.md`](w3_1_container_spike.md)): *neither* candidate container emits a single `ListItem` control type — WPF's `System.Windows.Documents.List`/`ListItem` are layout elements — so this obligation costs custom `AutomationPeer`s whichever container wins, which neither §10.6 nor `w3_spec.md` costed. |

## 2026-07-26 browse-mode research — G21 recorded

| # | Upstream said | Program does | Why |
|---|---|---|---|
| G21 | Decision 6 / 07 §3 row B: "Heading navigation is *not* ported from the mac outline workaround — **Windows ATs navigate headings natively via UIA**; the outline sidebar ships as a feature, not as the a11y crutch." `w7_spec.md` W7-3 repeated it ("headings/links/landmarks native per W3-1"). | Structural navigation is delivered in **layers**: app-owned chorded commands moving the real caret + canonical announcements (guaranteed, all ATs, stock settings); AT-idiom letter layers as **W-E7** (an NVDA add-on granting genuine browse mode, Add-on Store then upstreaming à la Kindle; JAWS scripts mapping JAWS-convention letters onto the app commands); in-app bare letters only as an opt-in setting, default off. All §W-C gates run against stock ATs — the AT-idiom layers are never load-bearing. | The premise was false, verified at the source 2026-07-26: NVDA browse mode (and single-letter quick-nav with it) is granted by NVDA-side per-app class selection — `NVDAObjects/__init__.py` `_get_treeInterceptorClass` raises `NotImplementedError` by default, the 2,860-line generic UIA base class contains **zero** browse-mode wiring, and every grant in the tree is a browser engine, an Office canvas, or a bespoke app module (Kindle, Acrobat, Mail). **No UIA property can request it, in any framework** — WinUI3's XAML branch yields only focus-mode `XamlEditableText`, and VS Code's app module exists to *suppress* the interceptor its Chromium surface would otherwise get. Bare app-owned letters were considered and rejected: they squat on the AT's key namespace, cannot be disabled from the user's own AT settings, collide with any future JAWS/NVDA claim on those keys, and die under the project's own planned add-on. The layered shape is the industry pattern (Kindle/Acrobat/Word: app exposes structure + caret, AT owns the letters). Webview remains prohibited (§1.3); its unlock — every technical route closed — is unreachable, since the chord layer is plain keyboard handling and cannot fail. |

## 2026-07-26 reading link target — G22 recorded

| # | Upstream said | Program does | Why |
|---|---|---|---|
| G22 | Parity doctrine: mac is the behavioral spec. Mac reading-view links reuse `openLink` → `openFile(_:target:)`, which navigates the **current tab**; a new tab needs the ⌘-click modifier (`openTargetFromCurrentEvent`, `ReadingLinkRouter.swift:185`). | Windows reading-view link/embed activations open in a **new tab by default**, with an Editor-menu preference ("Open Reading Links in New Tab", persisted device-locally in `preferences.json`) flipping back to the mac-style in-place navigation. Editor-pane navigation is untouched — current-tab either way. Owner call, 2026-07-25, made live during the W3-1 NVDA field passes. | Reading mode is a *consumption* posture: in-place navigation silently discards the reader's position in the source note, which the owner hit immediately in field testing. A modifier-gated new tab (mac's shape) is also the weakest option for the AT audience W3-1 serves — ⌘/Ctrl-click is a pointer idiom, and the keyboard activation paths (Enter, Ctrl+Enter) would all take the destructive default. Divergence is deliberate and recorded; if the setting proves right it should migrate to mac rather than be ratified as Windows-only (same convergence posture as G20/#1048). |

## 2026-07-31 field pass 3 decisions — G26–G27 recorded

| # | Upstream said | Program does | Why |
|---|---|---|---|
| G26 | Mac (the behavioral spec) UPSCALES embedded images to the full reading-column width: `.resizable().scaledToFit().frame(maxWidth: .infinity)` (`EmbedView.swift:151-157`), column capped at the 680pt `readingMeasure` (`DesignTokens.swift:104`); no maxHeight, no natural-size floor. | Windows renders embed images at NATURAL size, downscale-only to fit width (`StretchDirection.DownOnly`, `MaxHeight` 600 defensive) — Obsidian's behavior. Owner call, field pass 3 2026-07-31: a 64×64 image stretched to column width reads as a rendering bug, not a design. The 2026-07-30 `Both` call was made against a 2×2 fixture whose natural size was invisible. | Consumption-honest rendering; the mac-side no-upscale fix is the convergence candidate (G22/G24 posture: migrate the better call to mac rather than ratify a one-sided divergence). Observed while deciding: Windows has no 680pt reading-measure equivalent (mac caps ALL reading content) — unfiled parity question, noted for W8 typography. |
| G27 | Mac announces block quotes via an invisible `.accessibilityValue("block quote, level N")` on the quote container (`ReadingView.swift:682-695`), spoken by VoiceOver at defaults; sighted users see only an accent bar. | Windows exposes quotes through the text attributes only: `StyleId_Quote` (source-verified UNCONSUMED by NVDA — nvaccess/nvda@5ba9521 maps StyleId solely to Heading1–9; kept for other ATs) plus `StyleName` "Quote" (NVDA's "report style" channel, OFF by default: speaks "style Quote" once enabled). No visible in-range prefix. Owner call, field pass 3 2026-07-31: zero visual change chosen over the only NVDA-default-reliable route (a visible caption prefix, the code-preamble idiom). | The G23 mechanism bars element Names/values from NVDA's caret stream, and NVDA has no UIA path to its `block quote` role (IA2-only, verified in source). Tester instruction recorded in the smoke vault: enable NVDA Preferences → Document Formatting → Style. Revisit if the setting toggle proves burdensome in field practice. |

## 2026-07-31 W4-1 reading-table substrate — G28 recorded

| # | Upstream said | Program does | Why |
|---|---|---|---|
| G28 | Mac (the behavioral spec) renders reading-view tables directly ON `AccessibleDataGrid` (#566): the table IS the grid, in place. `w3_spec.md` item 3 transfers the obligation ("Tables render on the W4-1 grid substrate where mac uses `AccessibleDataGrid`"), and the program wave table defers W3-1's table rows to W4-1. | Windows LAYERS it (the G21/G23 shape): the table stays IN-RANGE as a plain FlowDocument table for linear reading, and **Enter at the caret opens the SAME block source on an `AccessibleDataGrid` window** (Escape returns; cells re-derived by core `ReadingTableCells`, never a host re-parse per §10.8). Export commands stay disabled in this view — reading tables have no core export surface, and w4_spec 3b forbids silent host composition. Degenerate tables (cells core cannot derive) stay linear-only and Enter falls through. | An embedded DataGrid inside the reading document is an embedded object — blank in say-all and the caret stream with its Name suppressed (the G23 mechanism, the same measured class that moved W3-5 embeds and the W3-4 copy control in-range) — so mac's in-place shape would regress W3-1's field-verified linear reading in order to reach §8.7. VoiceOver has no such constraint (AX children read in place). Both hosts end at the same powers; the Enter hop is the recorded delta, and the table's grid window inherits the full W4-1 conformance gate. |

## 2026-07-29 W3-5 embed decisions — G24–G25 recorded

| # | Upstream said | Program does | Why |
|---|---|---|---|
| G24 | Mac (the behavioral spec) resolves reading-view embeds UNBOUNDED: `resolve_embed` with `EmbedResolveBudget::Unlimited`, images to the 50 MiB attachment ceiling, no pixel cap (`AppState.swift:11885`); the bounded `resolve_embed_preview` exists in core but mac never calls it. | Windows reading cards resolve through **`resolve_embed_preview`** (64 KiB text / 128 nodes / 8 MiB image, cumulative per key, `truncated` flag) plus per-note host budgets (128 resolved keys, 16 MiB cumulative image bytes); over-budget keys degrade to header-only cards that still activate, and truncation renders an explicit "Preview truncated" notice. Display decodes through the hardened `DecodeImage` path (8 MiB / 1120 px). | The entire W3-2/W3-3 review arc established that unbounded per-note artifact work is a defect class (renderer starvation, FFI payload exhaustion, dispatcher allocation) — porting mac's unbounded path would re-introduce it knowingly. Stricter-with-notice loses no information silently. Converging mac DOWN to the bounded path is the natural follow-up, not the blocker. |
| G25 | Mac nested embeds are `DisclosureGroup`s: collapsed by default below the top level (audit #195), expandable IN PLACE. `.canvas` embeds render as a full-note card of raw canvas JSON (no canvas-specific `EmbedResolution` variant exists in core). Mac's reading-view per-key fallback resolver drops the authored alt (`alt: nil`, `AppState.swift:12003` — documented deferral). | Windows renders nested embeds in mac's INITIAL state — an indented header-only child card whose Jump button opens the source — with no in-place expansion (a FlowDocument section rebuild per toggle is real complexity for content one activation away). `.canvas` embed cards mirror mac's raw-JSON note card verbatim (cross-platform presentation follow-up candidate rather than a silent one-sided fix). The alt ALWAYS threads from the embed's link record on Windows — mac's fallback-path alt loss is a defect, not a contract, and is not ported. | The collapsed initial state is the accessibility-reviewed posture (VO users skip a card without traversing its levels); rendering it permanently keeps say-all bounded and the card model simple. Raw-JSON canvas cards are mac-verbatim; inventing a Windows-only presentation would diverge the twins in the other direction. |

## 2026-07-27 math-over-UIA research — G23 recorded

| # | Upstream said | Program does | Why |
|---|---|---|---|
| G23 | `w3_spec.md` §W3-2 item 1: expose MathML "via a UIA route pinned as this issue's first task — candidates: the registered custom UIA property (the MathPlayer-era convention MathCAT inherited) vs. an attached automation property", implying an existing consumable route. Acceptance: "JAWS/NVDA read = VoiceOver read". | Math ships in **layers** (the G21 shape). **Guaranteed layer**: focusable math blocks with Name = canonical MathCAT speech (spoken on focus/Tab/object-nav in NVDA and JAWS — verified paths), the `Ctrl+Alt+M` chord with the speech as the canonical landing announcement (works on every AT in every mode), and an Enter details popover (speech/source/braille — the UIA analog of mac's custom content). **Convention layer**: the `{FA170AB3-3229-4E7C-827F-DD05EE0481D9}` "MathML" custom UIA property + AriaRole "math" + `AnnotationType_Mathematics`, served from WPF via a guarded private-reflection injection into `AutomationPeer`'s property table (owner call, 2026-07-27: dependency accepted, with a startup smoke test that fails loudly on any dotnet/wpf change). **W-E7 layer**: a Slate NVDA appModule mapping our elements to Role.MATH and feeding `mathMl` from the property — unlocking NVDA's built-in MathCAT speech, braille, NVDA+Alt+M, and full math navigation (the Kindle-precedent upstreaming path). Say-all over a math block announces a marker, not the speech — in-flow speech parity with VoiceOver is deferred to the W-E7 layer (owner-reviewed posture: marker-plus-focusable beats visibly printing speech text). | Verified in NVDA master (July 2026): NO generic UIA surface produces Role.MATH — the control-type map has no math entry, LocalizedControlType is ignored for roles, AriaRole "math" is absent from NVDA's aria map, and the Word.MathML property is consumed only inside the Word overlay (gated on the `_WwG` window class). JAWS documents math intake from browsers and Word only. NVDA's math pipeline itself is browse-mode-independent (focus/objnav speech, navigator-object NVDA+Alt+M, fake-focus interaction) — the blocker is purely classification, which is exactly what an appModule provides (NVDA core ships one for Kindle; its math portion is ~15 lines). The convention layer is newly load-bearing: Microsoft engineers landed the same GUID + Custom control type + AriaRole "math" in Chromium (Jan 2026, `kUiaMathMlSupport`, built for Narrator), and Word/PowerPoint now expose MathML in the accessibility tree with Narrator 25H2 reading Office math via MathCAT — shipping the property positions Slate for the consumer generalization that is visibly underway. Say-all name suppression is structural: WPF's text pattern yields a blank for embedded objects and NVDA suppresses control-field names during say-all/caret reading unless an overlay TextInfo opts in (`speech.py:2305`), which only its Word/Edge/Chromium overlays do. |

## 2026-07-18 W0-1 spike executed — G11 resolved

The G11 question (csbindgen named in 05 §2.3 vs. spike-decided generator) is now answered on evidence: **`uniffi-bindgen-cs`**, decision record + scored dimensions in [`w0_spec.md`](w0_spec.md) §Decision. Both candidates bound the full rule-1 probe surface and passed all ten probe sections on native Windows ARM64 (x64 twin in CI); the discriminator is cost structure, not capability. Headline numbers: the counter-candidate's hand-written boundary for the probe slice alone is 821 lines of unsafe-bearing Rust (61 `unsafe` occurrences) + 667 lines of hand-written C# wrapper over 133 generated extern lines, versus 22,922 generated lines and zero hand-written binding code covering the entire FFI under uniffi — with typed-error fidelity (all 17 `VaultError` variants), nested-enum lifting, and callback-lifetime ownership all on the generated side. Losing path's evidence is preserved in-tree (`examples/csharp-probe/{shim,ShimProbe}/`) and exercised by the throwaway workflow until W0-3.

### Verified as current; no corrective edit required

- The W0 binding probe covers all three current foreign-callback traits (`ScanProgressListener`, `VaultEventListener`, `CommandAction`), object handles, cancellation, error mapping, and the `DocumentBuffer` hot path.
- W0-3 owns the early §W-A harness skeleton; W8-4 owns exhaustive surfaces, normalizations, and three-job CI hardening. The corresponding issue bodies agree.
- W7-2's early dispatcher delivery and later full announcement census are explicitly split; this is a dependency exception, not a hidden second issue.
- Math, diagram, span, grid, canvas, and graph specs consistently consume canonical Rust artifacts rather than re-implementing product logic in C#.
- W8 retains both automated UIA/axe checks and human JAWS/NVDA release evidence; Narrator remains smoke-only by explicit scope decision.
