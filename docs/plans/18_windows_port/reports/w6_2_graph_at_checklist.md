# W6-2 — graph manual AT checklist (the spec's eleven items re-expressed for UIA, W7-4 form, #746)

Run at close-out on a real machine with the shipped build, by a named
tester, per the field form `w3_1_nvda_field_verification.md`. The
automated gates (axe zero over the five graph FlaUI journeys, the
announcement-grammar facts, the end-to-end suite, the censuses) are
CI-enforced; this list covers what only a human and real assistive
technology can verify. Every human cell below is **Pending** until a
run is recorded — an unexecuted item is the release residual, listed as
such in the contracts document's reconciliation, never green by
omission (§F FD-3).

**Tester:** Pending — release residual · **AT:** Pending (NVDA version; JAWS version; Narrator is smoke scope, D-7) · **OS:** Pending (Windows 11 edition and build) · **Build:** Pending (branch and verified commit) · **Corpus:** the committed `graph_vault` fixture (`crates/slate-core/tests/fixtures/graph_vault`: eight files, twelve nodes, four of them ghosts) opened as a vault, plus the 1,500-note and 1,501-note linked vaults `GraphEndToEndTests` generates (`FixtureVault.Create`) for scaling and the tier boundary; for a run against a working vault, the demo vault's `graph/` folder (`demo-vault/graph/Graph testing guide.md` maps its hub, spokes, depth chain, island, orphans, ghosts and colour-group names to the items below) and `scripts/make_graph_tier_b_vault.py` for the 1,501-node vault · **Method:** Pending (Speech Viewer transcript or JAWS speech history, saved beside this file; one row per item) · **Run date:** Pending · **Evidence reference:** Pending (the transcript file beside this record)

| # | Spec item | Check | How (the UIA route) | Observable outcome | Automated twin | Narrator | NVDA | JAWS |
|---|---|---|---|---|---|---|---|---|
| 1 | table walk (names, sort) | Table walk and the sort | Open the graph (Graph → Open Graph, or `slate.graph.openTab` from the palette); with the screen reader, read the header row of `GraphTableGrid`, then walk the rows top to bottom; press Ctrl+Alt+S on the Note column, then again | The nine headers read in core's column order (Note, Links in, Links out, Embeds in, Embeds out, Component, Modified, Folder, Kind); every row reads its label, kind and counts as core's row copy; the summary region reads the snapshot summary; the sort is announced and the rows re-read in the new order, ascending then descending | `GraphSurfaces_TableSortSelectionAndActivation_AreClean`; `OpenGraphVaultExposesTheTableTheSummaryAndTheSortAgainstTheGolden` | Pending | Pending | Pending |
| 2 | leaf walk | The Connections leaf: depth, re-root, Back, Create note | On a seated table row open its actions (Applications key or Shift+F10) and run Show connections; walk the tree (`ConnectionsTree`); change the depth (`ConnectionsDepth`) to 2 and 3; re-root through a child row's own Show connections action (Applications key or Shift+F10 on the row; the palette row `slate.graph.showConnections` only reveals the leaf for the note in view and never re-roots); Ctrl+[ from inside the leaf (Back), then again; on a ghost row run its Create note action; Enter on a child row, then, back in the leaf, Ctrl+Enter on another | "Connections panel." is spoken; the summary reads the centre's counts and the depth; the rows read direction, label and counts, and a note row's hint reads "Opens the note. Control Enter opens it in a new tab."; each depth change reloads and re-reads the summary; the re-root speaks "Connections: ⟨label⟩"; Back returns to the previous root, and with nothing left to pop does nothing; Create note speaks "Created note ⟨name⟩." and the row becomes a Note; Enter opens the note ("Opened ⟨file name⟩.") and Ctrl+Enter opens it in a new tab, and neither re-roots nor adds a Back step; Ctrl+Enter aside, every open in this item (Show connections from the table or a row, Back, Create note, Enter) lands in the active pane's current tab, replacing the Graph tab when that is the current tab, as on mac | `GraphConnections_LeafWalkDepthAndReRoot_AreClean`; `TheConnectionsLeafWalksReRootsAndCreatesAgainstTheGolden`; `TheTreeOpensTheSelectedNoteInANewTabOnControlEnterAndInTheCurrentTabOnEnter`; `TheRowsNameIsCoresCopyAndItsHintIsItsActivations` | Pending | Pending | Pending |
| 3 | diagram traversal (no dead end, tier B) | Diagram traversal, the pan at the edge, tier B | Switch to Diagram (the "Graph surface" switcher); move the screen-reader cursor node by node across `GraphDiagram`'s peers (`GraphNode:` ids), past the window's edge; Invoke a node; read the container's Value; then open the 1,501-note vault's graph in Diagram | Each node reads its row copy and its "Connects to" help; traversal past the edge PANS the diagram — no dead end; Invoke selects and speaks the row; the container's Value reads the zoom percent; at 1,501 notes "Large graph" is spoken once and the one `GraphTierSummary` element's name reads the count and the Table-mode hint in place of per-node peers | `GraphSurfaces_DiagramPeersTiersAndZoom_AreClean`; `AnnouncementGrammarConformsPerVerbosity`; `LargeGraphOpensLaysOutPansAndStepsUnderBudget` | Pending | Pending | Pending |
| 4 | arrow-only | Arrow-only traversal in each surface | In the table: Down/Up through the rows; in the leaf: Down/Up, Right/Left (expand, collapse) through the tree; in the diagram: the four arrows (core's spatial step), Tab and Shift+Tab (core's structural step, wrapping), a letter (type-ahead) | The selection sentence follows every move at the set verbosity; the diagram's arrows land on the spatially nearest node in that direction, Tab walks the visible order and wraps, a letter jumps to the first label starting with it. Rotor equivalents: not applicable — UIA has no rotor; the tree's levels and the grid's rows are the navigation structure | `GraphNavigatorTests`; `GraphDiagramTests`; `TheDiagramReproducesTheGoldensSixtiethTickThenConvergesStepsZoomsAndReadsBack` | Pending | Pending | Pending |
| 5 | keyboard-only | Keyboard-only tab-through | Tab through the graph header (the "Graph surface" switcher, the filter field `GraphFilterField`, Clear, the inspector toggle `GraphInspectorToggle`), the showing surface, the Where-am-I panel (Ctrl+Alt+Shift+I, then Escape), the inspector pane (its name field, check boxes, pickers and sliders) and the Connections leaf (its depth control and tree) | The focus ring is visible on every stop; no stop is skipped and none is unreachable; Escape closes the Where-am-I panel, then clears the needle, then returns to the surface — one rung per press | `GraphSurfaces_NavigatorFilterAndWhereAmI_AreClean`; `GraphInspector_FiltersGroupsAndForces_AreClean`; `GraphTableTests` (the tab order facts) | Pending | Pending | Pending |
| 6 | Voice Access (D-7's twin) | Windows Voice Access | On the diagram say "show numbers"; then dictate, in order: "Click 3", "Show connections", "Where am I", "Zoom in", "Fit graph" | Every visible node peer gets a number; "Click 3" selects the third and speaks its row; the four verbs run as the palette rows they name (`slate.graph.showConnections`, `slate.graph.whereAmI`, `slate.graph.zoomIn`, `slate.graph.fitGraph`) — the leaf mounts, the readback opens, the zoom percent is spoken twice | `ChordTableTests` (the rows exist and are executable); none for the tool itself — human only | Pending | Pending | Pending |
| 7 | switch access | Switch access | With a switch (or the keyboard as one), scan through the header into the diagram, select a node with one switch, open its connections (the row action), then Escape the ladder | The scan reaches every header control and the surface; one switch selects and speaks the node; the leaf mounts with the node as its root; Escape exits the panel, then the needle, then returns to the surface | `GraphTableTests` (the Escape ladder facts); `GraphNavigatorTests`; none for the tool itself — human only | Pending | Pending | Pending |
| 8 | braille | Braille inspectability | With a braille display (or the reader's braille viewer), read the filter field's Value, the diagram container's Value, the state region (`GraphStateText`), the leaf's depth control and the inspector's sliders without waiting for an announcement | The needle, the zoom percent, the state text, the depth and the four force values are readable from element Values | `GraphSurfaces_NavigatorFilterAndWhereAmI_AreClean`; `GraphInspectorViewTests` (the RangeValue facts); `GraphDiagramTests.Renderer` (the container's Value) | Pending | Pending | Pending |
| 9 | text scaling | Text scaling 225 % | Set Windows text scaling to 225 %; open the graph in Table and Diagram, open the inspector and the Connections leaf | Node labels scale with no clipping; the inspector's sections and the leaf reflow; nothing is cut off | none — human only | Pending | Pending | Pending |
| 10 | Contrast | Contrast themes | Apply each of the four built-in Contrast themes and one customised theme; open the diagram with a colour group configured in the inspector and a node selected | Node fills and group colours collapse to the theme's colours, labels remain readable, the selection ring is visible on every fill | none — human only (`ThemeTokenContrastTests` pins the tokens) | Pending | Pending | Pending |
| 11 | Reduce Motion | Reduce Motion | Enable Reduce Motion; open the graph in Diagram, pan, zoom and move the selection | The layout lands in one converged frame with one "Graph layout settled." — no visible settling; pan, zoom and selection changes land instantly | `ReduceMotionAppliesOneConvergedFrame` (`GraphDiagramTests`) for the one-shot frame; the rest human only | Pending | Pending | Pending |

Residual: items 1–11 require human judgment with real AT; CI cannot
substitute. File findings as `audit`-labelled issues per repo
convention, credited to the tester. The `w_c_matrix.md` graph rows
stay Pending until this record carries a named run.

Agent-operated NVDA evidence for every row here (not a human cell; no cell above changed): [2026-09-22 matrix pass](nvda_agent_matrix_pass_2026-09-22.md), with per-row results, findings F1–F14 and the rows it could not execute.
