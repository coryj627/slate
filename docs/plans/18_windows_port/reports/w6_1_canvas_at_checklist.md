# W6-1 — canvas manual AT checklist (T's `at_smoke_checklist.md` re-expressed for UIA, W7-4 form, #745)

Run at close-out on a real machine with the shipped build, by a named
tester, per the field form `w3_1_nvda_field_verification.md`. The
automated gates (axe zero over the eight canvas FlaUI journeys, the
announcement-grammar facts, the end-to-end suite, the censuses) are
CI-enforced; this list covers what only a human and real assistive
technology can verify. Every human cell below is **Pending** until a
run is recorded — an unexecuted item is the release residual, listed as
such in the contracts document's reconciliation, never green by
omission (§H HD-3).

**Tester:** Pending — release residual · **AT:** Pending (NVDA version; JAWS version; Narrator is smoke scope, D-7) · **OS:** Pending (Windows 11 edition and build) · **Build:** Pending (branch and verified commit) · **Corpus:** the committed `sample.canvas` (nine nodes, five edges) in a vault with its two notes, plus `large_2000.canvas` for scaling · **Method:** Pending (Speech Viewer transcripts captured verbatim; `SLATE_UIA_DIAGNOSTICS=1`) · **Run date:** Pending · **Evidence reference:** Pending (the transcript file beside this record)

| # | T item | Check | How (the UIA route) | Observable outcome | Automated twin | Narrator | NVDA | JAWS |
|---|---|---|---|---|---|---|---|---|
| 1 | 1 | Outline walk | Open the sample canvas; with the screen reader, walk the outline tree (`CanvasOutlineTree`) top to bottom | Every card reads its title, "n of m in ⟨container⟩" and its colour name; connection rows under the seated card read the direction ("Connects to", "Connected from", "Linked with") and the label | `CanvasSurfaces_OutlineTreeSelectionAndActivation_AreClean`; `CanvasVerbs_GroupConnectDuplicateLinkConvertAndUndo_AreReachable` (a connection row's name with its label) | Pending | Pending | Pending |
| 2 | 1 | Table headers and the sort announcement | Switch to the table (Canvas view → Table); read the header row; press Ctrl+Alt+S on the Type column | The six headers read Type, Title, Group, Target, Connections, Color; the sort is announced ("Sorted by Type, ascending") and the rows re-read in the new order | `CanvasSurfaces_TableGridSortSelectionAndActivation_AreClean`; `OpenSampleExposesOutlineTableAndScene` | Pending | Pending | Pending |
| 3 | 1, 3 | Visual board traversal | Switch to the visual view; move the screen-reader cursor card by card across the board (`CanvasSurfaces_VisualBoardPeersAndZoom_AreClean`'s peers) | Each card reads its speakable name; traversal past the window's edge PANS the board — no dead end; Invoke on a card selects it; the container's Value reads "Zoom N percent" | `CanvasSurfaces_VisualBoardPeersAndZoom_AreClean`; `TheZoomValueIsCoresRenderMinusItsPeriod` | Pending | Pending | Pending |
| 4 | 2 | Arrow-only traversal | In each surface, move with the arrow keys alone (Down/Up in the outline and table; the navigator's Next/Previous card, Enter/Exit group, Follow connection chords on the visual board) | The selection sentence follows every move at the set verbosity; groups enter and exit; connections follow. Rotor equivalents: not applicable — UIA has no rotor; the outline's tree levels (groups), the connection rows under the seated card, and the navigator's Follow-connection chords are the routes | `CanvasSurfaces_NavigatorFilterAndWhereAmI_AreClean`; `AnnouncementGrammarConformsPerVerbosity` | Pending | Pending | Pending |
| 5 | 4 | Keyboard-only tab-through | Tab through the canvas header (the view switcher, the filter field and its Clear button), the showing surface, and every sheet (the card editor, a prompt, a picker) | The focus ring is visible on every stop; no stop is skipped and none is unreachable; the sheets fence Tab inside themselves | `CanvasAuthoring_NewCanvasCardEditorAndUndo_AreReachable`; `CanvasMarks_ToggleListJumpDeleteAndUndo_AreReachable` (the modal fence) | Pending | Pending | Pending |
| 6 | 5 | Windows Voice Access (the Voice Control twin, D-7) | On the visual board say "show numbers"; then dictate, in order: "Click 3", "Toggle Mark", "Connect To", "Delete Marked Cards", "Where am I" | Every visible card gets a number; "Click 3" selects the third; the four verbs run as the palette rows they name — the mark toggles, the picker opens, the marked cards delete in one action, Where-am-I speaks its readback. Record the tool and its version | none — human only (the verbs' own facts: `TheMarkChordRoutesToTheVerb`, `DeleteMarkedIsOneActionAndUndoRestoresIt`, `WhereAmIRendersTheSameStringItSpeaks`) | Pending | Pending | Pending |
| 7 | 6 | Switch access | With a switch (or the keyboard as one), cycle into move mode through the visible Commit/Cancel controls, nudge the card, commit; then Escape the ladder | The mode's visible controls appear with the mode and collapse with it; the nudge and the commit speak; Escape exits mode, then clears the filter, then returns to the surface — one rung per press | `CanvasModes_MoveResizeAndConnectPicker_AreReachable`; `TheEscapeLadderConsumesExactlyOneRungPerPress` | Pending | Pending | Pending |
| 8 | 7 | Braille inspectability | With a braille display (or the reader's braille viewer), read the surface's ItemStatus, the marks list's rows and the filter field's Value without waiting for an announcement | The mode state ("Move: ⟨card⟩"), the marks (", marked" on rows) and the filter needle are readable from element Values and statuses | `AMarkToggleRefreshesTheRowsStatusInPlace`; `TheFilterFieldAndItsSummaryAreReadable` | Pending | Pending | Pending |
| 9 | 8 | Text scaling 225 % | Set Windows text scaling to 225 %; open the canvas in each view and open a prompt and a picker | Renderer labels scale with no clipping; the sheets and pickers reflow; nothing is cut off | none — human only (`CanvasTextScaleServiceTests` pins the service) | Pending | Pending | Pending |
| 10 | 9 | Contrast themes | Apply each of the four built-in Contrast themes and one customised theme; open the visual board with coloured and marked cards | Fills collapse to the theme's colours, names remain readable, the selection ring is visible on every preset fill | none — human only (`ThemeTokenContrastTests` pins the tokens) | Pending | Pending | Pending |
| 11 | 10 | Reduce Motion | Enable Reduce Motion; pan, zoom and move the selection on the visual board | Pan, zoom and selection changes land instantly — no spring, no animation | none — human only | Pending | Pending | Pending |

Residual: items 1–11 require human judgment with real AT; CI cannot
substitute. File findings as `audit`-labelled issues per repo
convention, credited to the tester. The `w_c_matrix.md` canvas rows
stay Pending until this record carries a named run.
