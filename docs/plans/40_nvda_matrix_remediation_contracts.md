# W7-7 NVDA matrix remediation — feature contracts & registers (#1244–#1257)

Scope: [W7-7 executable spec](18_windows_port/specs/w7_7_nvda_matrix_remediation_spec.md). This contract-only commit precedes every implementation PR and every review round, per [24_red_team_protocol.md](24_red_team_protocol.md). Contract numbering is per-wave (`R-n`); each PR cites its numbers in its review prompts and appends its rounds to its own section below. A **planned witness** is a test or journey the owning PR has yet to land: it becomes evidence only when that PR's round record names its mutation run. `W77RemediationDocsCensus` pins the spec-section ↔ contract mapping (each PR's design section cites exactly its own `R-n`). Paths are relative to `apps/slate-windows/src/SlateWindows` unless stated.

## Contracts

**R-1 — Announcements reach a listening client from the first frame (PR 1, #1244).** The one production raiser (`AccessibilityNotificationDispatcher.cs`, contract 38 D-2) raises through `AutomationInteropProvider.RaiseAutomationEvent(AutomationElementIdentifiers.NotificationEvent, provider, NotificationEventArgs)` with the status element's connected peer provider (`AutomationPeer.ProviderFromPeer`; the HWND host provider raised without error and delivered nothing, so it is not used), guarded by `AutomationInteropProvider.ClientsAreListening` — and, because UIA's advise of the process lands after the launch lines are raised, a monotonic UI-thread state machine queues only while the process is unadvised, drains the queue once in order on the first advise, then raises every later line immediately; the launch phase expires after 30 s without a drain into an `Expired` state that keeps the queue-only-while-unready rule (a later line raises when a client listens and the provider is connected, otherwise waits and drains once when it connects) and the queue keeps the last 16 lines (OD-7); the state machine and provider are one per window — production grids receive the window's announcer by injection and the census rejects an independent production dispatcher — so every line is delivered exactly once, never twice. Evidence: WPF's `RaiseNotificationEvent` is gated on `EventMap.HasRegisteredEvent(Notification)`, a process-static map populated only by `ElementProxy.AdviseEventAdded`; the 2026-09-22 record shows the map stays empty until a popup HwndSource appears. Planned witness: `AnnouncementSeamCensus` (the raise shape and the provider at the replay boundary; no `RaiseNotificationEvent` in authored code — the census owns the gated-call mutation), the hosted state-machine facts with exact counts, and the FlaUI journey `Announcements_ReachADesktopScopedListenerFromLaunch` (desktop-root subscription created before launch; the exact launch tuple sequence and "Right pane hidden." each received exactly once without a menu having opened; mutation: replay disabled or the transition never flipping).

**R-2 — Keyboard selection never takes focus out of the Files tree (PR 2, #1245).** A selection-driven open (tree arrows, filter results, dual pane) opens the note with `requestEditorFocus: false`; `FocusedRegion()` stays `Files`. Enter opens the selected row and moves focus into the note; Ctrl+Enter opens it in a new tab; Space toggles the focused row's batch check box and the row's `ItemStatus` reflects it; the check box is `Focusable="False"`. The three keys are routed from the focused row container, never from "the tree has focus": inside the inline rename box Enter commits and Escape cancels the rename, and the same Enter/Ctrl+Enter serve the filter-results and dual-pane lists. The three gestures are chord rows (`windows.filesTree.*`) and the tree's help is composed from them (contract 39 N-1/N-3). Evidence: `FilesSidebarViewModel.cs:555–586`, `WorkspaceViewModel.cs:2044` (`OpenPathCore(requestEditorFocus)`), `WorkspaceTemplates.xaml:31–36`. Planned witness: unit facts on the setter and the open commands; FlaUI `FilesTree_ArrowsKeepFocusEnterOpens`.

**R-3 — Tag activation composes core's grammar (PR 2, #1250).** The split is core's: `sidebar_filter::tag_filter_activation(tag)` answers `#tag` for a tag without whitespace and a tag scope (`filter_files`'s `scope_tag`) otherwise — the rule `AppState.swift:7120–7131` applies host-side — and `ActivateTag(tag)` applies that answer, never re-implementing it (protocol rule, `24_red_team_protocol.md:64`). The scope is complete filter state: `IsFilterActive` is true while a scope is set, the query runs with an empty text, the scope is visible and spoken through the filter status, `ClearFilterCommand` executes from `IsFilterActive` and clears text and scope atomically, typing keeps the scope, the request identity `(query, scopeTag)` keys the stale-result guard and `(query, scopeTag, total)` keys the announcement de-duplication (a changed total still announces), and the spoken result is core-rendered scope-aware (`FileListCount { count, scope_tag }`: an equal-count switch and a zero-result scope both name the tag; mac's producer passes `scopeTag: nil` in the same PR). The field's help text names only prefixes `parse_sidebar_filter` accepts. Evidence: `crates/slate-core/src/sidebar_filter.rs:153–229`, `FilesSidebarViewModel.cs:834`, `FilesSidebarViewModel.Filter.cs:55–80`. Planned witness: the Rust facts for `tag_filter_activation`; `ActivateTag_ComposesCoreGrammar`; the whitespace-tag hosted fact over a temp vault through the real FFI; `ReadingTagSearchRerouteTests` flipped to `#atag`; the FlaUI tag-tree journey finds the fixture's tagged note.

**R-4 — Every reachable item is named; layout containers are not control elements (PR 3, #1246).** No UIA Name in the shell reads as a runtime type or record rendering (an identifier chain rooted in one of the app's type namespaces, brackets and generic arity included, or a record dump); ordinary dotted user content such as `note.md`, `README.MD` or `Smith.Jones` is never flagged. Item containers the reader lands on carry `AutomationProperties.Name` from the item's speakable name; containers that only wrap the real stop answer `IsControlElementCore => false` and are never named instead — a duplicate sibling is disambiguated on the actionable child, so every item exposes exactly one Control-View stop. Every `AccessibleDataGrid.Bind` call passes a non-empty `rowAutomationName` (first non-empty cell, else an ordinal assigned from source order that survives sorting, re-realization and rebinding, wherever the identity source can be empty). Evidence: the ten sites in spec §4.1; `Grids/AccessibleDataGrid.cs:215–224, 468–480`. Planned witness: the name census inside `AssertAxeClean` (every journey), the XAML container census, `AccessibleDataGridTests` per caller.

**R-5 — Arrow keys stay in their region (PR 4, #1247).** `MainMenu` sets `KeyboardNavigation.DirectionalNavigation="None"` beside `Focusable="False"` and `TabNavigation="None"`; no focus fallback lands on a bare `Selector` when it has items (a shared helper focuses the current or first item); the right-pane boundary lands on the shown leaf's first focusable stop when it has one; radio groups (`Tasks Review` filters, the canvas and graph view switchers) cycle with arrows and check the radio that receives keyboard focus, and the surface follows (the filter, the projection, the view); `ContentPaneBorder` contains directional navigation. Evidence: `MainWindow.xaml:79–85`, `MainWindow.ShellRegions.cs:180–188`, `MainWindow.xaml.cs:641–685`, `MainWindow.Citations.cs:343, 363, 654`. Planned witness: `MenuBarCensus`, the fallback census, FlaUI `RegionStops_ArrowsStayInRegion` with a per-surface radio block (Tasks, canvas, graph: focused radio checked, surface changed, next F6 landing).

**R-6 — A sheet fences Tab (PR 5, #1248).** Every overlay with `FocusManager.IsFocusScope="True"` carries `SheetKeyboardFence`: an edge command binding per Tab command refuses `EditingCommands.TabForward`/`TabBackward` only when nothing inside the sheet answered them and lets the key keep routing so WPF's own traversal moves focus through the sheet's cycle, plus a guard on a direct `Execute`; no Tab typed inside a sheet changes the note behind it; controls that own Tab (`AcceptsTab` fields, an editing grid cell) keep it; an IME-claimed Tab stays with the IME. Shell-command suppression while a sheet is up stays the shipped modal-owner swallow (contract 30 TR-7, #1118); the fence adds no second mechanism and the two coexist. Evidence: `MainWindow.xaml:3751–3754` and the fifteen sibling overlays; the CommandManager focus-scope re-targeting (dotnet/wpf) and AvalonEdit's unconditional `TabForward` binding. Planned witness: `SheetFenceCensus` (fence and `Cycle`, element and style branches); the WPF-hosted `TabInsideASheetNeverReachesTheEditor` (genuine input; plus its coexistence assertions: a text-editing chord inside the sheet still works, a forbidden shell chord does not execute behind the scrim) and the command-source, direct-`Execute`, IME, `AcceptsTab` and grid-editor facts; FlaUI `Templates_PromptTabTraversalStaysInTheSheet`.

**R-7 — A failed save speaks a sentence, never a diagnostic (PR 6, #1249; amends contract 38 D-10).** A `VaultException.WriteConflict` posts `NoteSaveConflict(filename)` → "Save blocked. {filename} was modified externally. Your edits remain in the editor." and the inline status says the same; other failures keep `NoteSaveBlocked(filename, detail)`. No announcement or status text contains a content hash, an `@field=` token or a modification time. Evidence: `WorkspaceViewModel.cs:682–684`, `crates/slate-core/src/lib.rs:250–262`, `a11y.rs:1872–1877`. Planned witness: `RecoveryAnnouncementTests.ConflictingSaveSpeaksTheConflictSentence`; corpus rows; the trigger ledger.

**R-8 — A popover or sheet announces its outcome when it opens (PR 6, #1251).** Core variants `CitationPopoverShown`, `EmbedPreviewShown`, `EmbedPreviewUnavailable`, `CitationSummaryShown`, `CitationDetailsShown` and `CitationDetailsUnresolved` (High) post at the moment the outcome exists (citation popover and both sheets at open, the details sheet choosing its resolved or unresolved variant; embed previews when the result lands); an unavailable embed's body and UIA Name equal the core rendering and, by construction-site census, consume it rather than any host literal. Every sentence is rendered by core: `CitationPopoverShown` carries the preview's raw speech (or its semantic fields) and `a11y.rs` applies the "Citation." prefix rule; `EmbedPreviewUnavailable` carries a concrete semantic `EmbedUnresolvedReason` for every failure including a resolver exception, never a host-composed string or a null reason; `CitationDetailsUnresolved { key }` speaks the sheet's established wording, moved into core; the host never prefixes or composes, and the popover's UIA Name is the rendered text. Save-error detail sentences are core-rendered too (R-7): the host passes structured errors or core text only. Read-only preview text (`EditorEmbedPreview.cs`, the "Preview content" box) is caret-navigable (`IsReadOnlyCaretVisible = true`, non-focusable scroll host). Evidence: `EditorInteractions.cs:1286, 1385–1395, 1679–1683, 2772–2776, 3162–3171`, `WorkspaceViewModel.Citations.cs:255–314`, `Panels/AddPropertyViewModel.cs:57` (the pattern). Planned witness: `W2EditorInteractionTests` trigger facts; `A11yCorpusCensus`; the journey's two-line read.

**R-9 — A rescan is the reconciliation, and it says what it found (PR 7, #1252; amends contract 38 D-3).** `VaultLifecycleViewModel.RescanAsync` runs core's incremental scan on the open session; "modified" means the committed content hash differs (never `files_indexed`, which counts reads); an external rename is Deleted + Created (AR-8: no filesystem identity in the index, so hash equality never retargets a tab); the scan exposes its delta through bounded pages of an immutable, addressable generation retained until acknowledged (locked decision 05: no unbounded list crosses the FFI; paging proven with a tiny limit and a multi-page host fact; a page failing after earlier pages were applied posts `VaultRescanIncomplete`, the next rescan resumes that generation from its failed cursor, idempotently, before scanning again, the acknowledgement follows the last page's effects, core pages every generation removal-first so the host consumes it in one bounded pass without deferral, a retry's announcement reduces recovered and new effects per path to the net change against the last acknowledged state, the index mutations and the retained generation commit in one transaction, each retained row carries its prior and new hash, a retry resumes and marks the recovered generation applied before the new scan's generation is applied in full — effects are never netted, only the spoken outcome aggregates the two per path, and rows are released only after that reduction (acknowledgement is two-phase: applied, then released) — and a generation orphaned by a crash or exit is discarded by the next initial open scan before it scans — no unreconciled tail is ever lost while the workspace lives); a foreground activation during a running rescan joins the lattice rather than being dropped, the cooldown applying only to starting a run that the host reconciles through `ReconcileScanDelta`, removals before creations (so a case-only external rename still re-seats its tab) and silently (no "missing from disk" line during reconciliation), applying the operations `HandleFileChange` applies — a clean tab reloads through `WorkspaceViewModel.ReloadCleanTab` (new baseline, undo reset, caret kept or clamped), a dirty tab keeps its buffer — plus reading dependencies and Quick Open, while the file-change event channel stays Slate-owned writes only (locked decision 05); counts alone reconcile nothing open; a request during a run coalesces with Explicit dominating Foreground and Foreground joining Foreground as Foreground; then the sidebar refreshes, the Quick Open list is replaced, the graph notify is sent, and exactly one completion sentence is posted: `VaultRescanFinished(reason, changed, removed)`, or `VaultRescanIncomplete(errors)` — an honest error count, "results may be incomplete" — when the walk was partial, any per-file stat / read / index failed, or the scan call threw before a report existed (a partial scan never says "No changes", prunes neither files nor directories, and a retry that recovers a generation announces the recovered and new effects together). A rescan never speaks `VaultScanStarted`/`VaultScanFinished` (reason-aware progress policy). Explicit Refresh always announces; the foreground rescan (window re-activated after ≥ 2 s away, ≥ 5 s since the last rescan, no modal, no initial open scan / import / trash in flight — an activation during a running rescan is forwarded into the lifecycle coalescer and joins the pending-reason lattice, never dropped) announces only when something changed or the scan was incomplete, and its unchanged run posts nothing at all. A file created, modified or deleted outside Slate is reflected after one rescan, in the tree, Quick Open and any clean open tab; a dirty open tab keeps its edits. `VaultScanFinished { files_seen, files_changed }` speaks both counts ("{seen} files, {changed} new or changed") on both hosts (OD-6), `files_changed` being core's hash-authoritative count — a touched-but-unchanged vault says "0 new or changed". Evidence: `session.rs:2868`, `VaultLifecycleViewModel.cs:455–472, 729–797`, `FilesSidebarViewModel.TreeOperations.cs:56, 449–452`, `QuickSwitcherViewModel.cs:242–259`, `vault/fs.rs:973–978`. Planned witness: Rust rescan facts; Windows unit facts; FlaUI `ExternalFiles_AppearAfterRefresh`.

**R-10 — The reading surface is the editor stop (PR 8, #1253).** `FocusEditorPane` asks the visible `ReadingSurface` of a reading-mode tab to land focus before falling back — immediately when the current model's projection is applied (never on block count: the loading placeholder is a block), otherwise as a pending landing addressed to that model generation, fulfilled when its content is applied and withdrawn on rebind, focus departure or unload, so neither an unapplied surface nor the placeholder is ever focused (an applied content-empty note is a valid `Landed` at `ContentStart`) and no late apply steals focus — every apply-time focus claim of the surface goes through that one request (the legacy unconditional claims are gone); the landing is tri-state through one ring-owned request token that every asynchronous editor arm (reading, canvas, graph) reports through, the ring announces the editor region only when focus has actually entered the surface, a delayed refusal (a failed `Focus()`, a load failure, a model torn down before the apply while still current — never an applied empty note) resumes the same traversal so the next region is landed exactly once, every token having one atomic terminal transition (a rebind, unload, focus departure or tab closure cancels it and stales later callbacks), and a repeated F6 / Shift+F6 while pending cancels the landing and starts one fresh traversal in the second press's direction from the pending target's ring position while the landing is still held and focus has not moved, otherwise from where focus is (F6 → the region after the editor, Shift+F6 → the tab bar; the witnesses start on the active tab item or the right-pane content, never skipping a locked D-2 stop), a stale completion being ignored (the W7-6 ring spec is amended to say so); `ToggleViewMode` requests editor focus in both directions; F6/Shift+F6, Quick Open and the palette's close fallback land there with a readable document. The W7-6 F6 ring spec's editor row records it. Evidence: `MainWindow.xaml.cs:1679–1742`, `MainWindow.ShellRegions.cs:133–145`, `Reading/ReadingSurface.cs:199–205, 424–440`. Planned witness: the WPF-hosted arm facts (content present; content arriving after the request), asserting document content after the handoff; FlaUI `ReadingView_IsTheEditorStop`.

**R-11 — The palette announces one selection per query change and renders at typing speed (PR 9, #1254).** The `ItemsSource` swap runs inside the selection-sync guard and the list is not synchronized with its current item, so the view never re-selects on the user's behalf; the view model's `SetSelection` is the only `PaletteCommandSelected` source, and a query that removes the selected id announces the replacement selection exactly once (contract 28 P7/P10). Recompute cost scales linearly in row count; grouped rows virtualize. Evidence: `MainWindow.Palette.cs:206–255`, `CommandPaletteViewModel.cs:708–769, 878`. Planned witness: the view-hosted selection fact; `RecomputeScalesLinearlyInRowCount`; FlaUI `Palette_TypingAnnouncesOnlyTheFinalCountAndSelection`; the recorded before/after timings.

**R-12 — The board's arrows move the seat and its cards have a menu (PR 10, #1255, #1256; amends contract 34 D15 and lifts G2D-12).** On the Visual projection Down/Up run `SelectAdjacent` (reading order, End/Start of canvas at the bounds) and Right/Left follow connections, and after every Visual move or follow the presenter's reveal pans the renderer to contain the seat (the navigator's `FocusRow` path answers false for Visual); outline rows and the renderer carry a persistent `ContextMenu` from construction, mutated per request, keyboard requests targeting the focused row or the seated card, a pointer request seating the hit card silently first (contract 34 G2-12's consumer rule) — a menu action acts on the seat and no other card. Shift+F10 and the Applications key, each on an outline row and on a board card, open the same menu and never the tab's; the renderer's rows are pinned independently per target kind against contract 34 E17's inventory. Evidence: `Canvas/CanvasSurfaceView.cs:403–411`, `Canvas/CanvasNavigator.cs:183–186, 1079–1127, 1603–1627, 1659–1668`, `Canvas/CanvasOutlineView.cs:354–358, 1032–1047`, `Grids/AccessibleDataGrid.cs:1289–1327`, `Graph/GraphDiagramView.cs:1364–1400`. Planned witness: `CanvasNavigatorTests` visual facts including the recorded reveal (arrows and the palette's `NextCard`/`PreviousCard` route); `CanvasContextMenuTests` renderer plan, keyboard-request facts (a fresh, non-first row's menu is the row's), the independent per-kind inventory fact and the popup-service pointer facts (a right-click on a non-seated card acts on it alone; empty board opens no menu); the two FlaUI journeys — the reveal block starts from a destination outside the viewport and asserts the viewport changed and the destination is contained, a move and a follow separately — and Toggle Mark from the keyboard menu changed only the seated card / focused row.

**R-13 — The Connections leaf documents its activation truthfully (PR 11, #1257).** Row activation opens the note (Ctrl+Enter in a new tab) and only the row's Show connections action re-roots — the palette row `slate.graph.showConnections` loads without re-rooting (contract 35 B-9 and B2-3, `w6_2_graph_spec.md:198`, mac parity); the checklist says so; the row hint composes both gestures from chord rows (contract 39 N-1/N-2). Evidence: `Graph/ConnectionsLeafViewModel.cs:1036–1044, 1100–1113`, `Graph/ConnectionsPhrase.cs:49`. Planned witness: the hint fact; `ChordSpeechAuditCensus`; `WcMatrixEvidenceCensus` over the corrected row's shape and a semantic census fact over its activation wording (the shape census alone accepts the wrong row).

## Accepted risks, divergences and owner decisions (off-limits for re-litigation)

- **OD-1 (2026-09-22):** no live watcher in this wave; Refresh and foreground rescans are the reconciliation. **AR-3:** changes made while Slate is in the foreground and no rescan runs stay invisible until the next Refresh or activation; the follow-up issue for a core watcher is filed from PR 7.
- **OD-2 (2026-09-22):** keyboard selection keeps opening the note (mac parity) with focus in the tree; Enter/Ctrl+Enter move focus; Space toggles the batch box. **AR-5:** a pointer single-click also keeps focus in the tree; mouse users click into the note.
- **OD-3 (2026-09-22):** the visual board carries the derived card menu (G2D-12 lifted).
- **OD-4 (2026-09-22):** eleven grouped feature PRs plus the docs PR.
- **OD-6 (2026-09-23):** `VaultScanFinished` becoming `{ files_seen, files_changed }` is a cross-host signature change: mac's post site passes both counts from the report and speaks the same copy (the mac lane builds and arbitrates); the spec's mac non-goal is carved out for this one variant. Because mac's helper builds reports with `filesSeen == filesIndexed` today, PR 7 ships a mac fact with distinct counts (9 seen, 2 changed) over the event payload and the rendered sentence, and a touched-but-unchanged render fact.
- **AR-7:** the rescan keeps the `(mtime, size)` short-circuit, so an external same-size edit that preserves mtime stays invisible until the file is next read; the delta is authoritative for every file the scan reads.
- **AR-8:** the index carries no stable filesystem identity, so an external rename is Deleted + Created — the old path's tab becomes a missing tab and nothing is retargeted by content equality (a same-hash delete/create must never move a dirty tab).
- **AR-9:** the full FlaUI suite is CI's shell accessibility gate; locally each PR runs its touched journeys plus the whole unit project, the solution build and format gates, and the workspace-wide Rust gates.
- **AR-10:** a PR that touches no Rust file leaves `make ci` to CI's rust-tests job; every PR that touches Rust runs it locally before push.
- **AR-15:** PR 10's targeting journey proves Applications-on-board and Shift+F10-on-outline; the reciprocal key/target combinations share the same production handler and are not separately witnessed.
- **AR-13:** `W77RemediationDocsCensus` normalizes leading-zero identifiers and does not pin the displayed section ordinals; the mapping, duplicate and malformed-heading checks are its purpose.
- **AR-14:** PR 9's palette journey checks the on-screen selection only until its announcement leg lands with PR 1's listener; the hosted exact-one replacement-selection fact is the behavioural oracle meanwhile.
- **AR-11:** `W77RemediationDocsCensus` reads the two documents as text: a fenced-code heading or negated prose mentioning `R-n` would count; neither document carries either, and a fenced heading would still have to match the strict form.
- **OD-7 (2026-09-24):** replay on advise — the dispatcher queues only while UIA has not advised the process and drains the queue once, in order, on the first advise (PR 1's arbiter and an NVDA fresh-launch run showed the ungated raise alone loses the launch lines); the transient-window and accept-the-residual options were declined. **AR-12:** a process UIA never advises within the 30 s launch phase keeps its launch lines lost (as today); the queue keeps the last 16 lines.
- **OD-5 (2026-09-23, defaults amendable at review):** board Right/Left follow connections; the conflict sentence reuses mac's wording minus the dialog clause; #1257 is a checklist correction plus a composed hint; the scan-finished copy states both counts; #1244 is fixed at the raise call, not with a transient window.
- **AR-1:** the FlaUI desktop-root subscription (an `IUIAutomation6` handler group registered before launch) is itself advised asynchronously and was silent in some runs; the journey ships in the shell gate only when 6/6 stable on the final build, otherwise as a manual-trait journey with recorded counts, the hosted state-machine facts and the census being the gate evidence (spec §2.4–2.5).
- **AR-2:** mac does not post the five new popover/sheet variants in this wave; the corpus mirrors them and the mac owner decides.
- **AR-4:** the palette ratio fact tolerates 2.5× on doubling; absolute timings are recorded, not asserted.
- **AR-6:** a keyboard request on a container with no current item still lands on the container when it has no items (an empty list's notice is the stop where one exists).

## Review record

Each PR appends its rounds here, in its own section, newest last: the prompt's invariant list, the findings with dispositions (taken / refuted with evidence / accepted risk), the fix commits, and the verdict.

### PR 0 — docs (this document and the spec)

**Round 1 (2026-09-23; codex `gpt-5.6-sol`, effort xhigh; base `origin/main` 937dd3dd, branch scope).** Prompt: challenge the plan per PR — is the diagnosed mechanism supported by the cited code, does the design regress focus routing / mac parity / contracts 28, 30, 34, 35, 38, 39 and the W7-6 F6 ring, which named tests could pass with the bug present, what an independent developer still needs; weighted PR 1, 2, 5, 7; exhaustive single pass; OD-1..5 and AR-1..6 off-limits. Verdict: `needs-attention` (12 high, 2 medium; "PR 1's ungated UIA mechanism is supported; no separate blocker for PRs 3, 9, 11"). Dispositions — all taken, none refuted, no new accepted risk:

1. PR 7 — foreground rescans cannot be silent through the progress path (`ScanAnnouncementGate.Started/Finished` are unconditional): reason-aware progress policy, exactly one completion sentence, whole-event-sequence assertions (spec §8.2–8.4; R-9).
2. PR 7 — count-only rescans cannot reconcile open workspace state, and `HandleIndexPhase` notifies only the graph: the rescan emits per-path `notify_file_change` through the `HandleFileChange` funnel; open-tab facts (§8.2 e; R-9).
3. PR 7 — a partial scan could be announced as a no-op: `complete` on the report and `VaultRescanIncomplete` (§8.3; R-9).
4. PR 2 — whitespace tag activation is inert under the current filter state (`IsFilterActive` and `ScheduleFilter` ignore a scope) and the split re-implemented core's rule host-side: core `tag_filter_activation`, the scope as filter state with visibility and clearing, a hosted fact through the real FFI (§3.3; R-3).
5. PR 2 — the key arm could steal Enter from the inline rename: routing from the focused row container, rename commit/cancel facts, Enter/Ctrl+Enter on all three lists (§3.2; R-2).
6. PR 5 — R-6 promised more than the fence delivers and the journey's opener was Bulk Rename: R-6 narrowed to Tab/Shift+Tab with the shipped TR-7 swallow named, coexistence assertions, real openers (§6; R-6).
7. PR 8 — focusing an empty reading surface recreates the "blank" regression: pending focus landing fulfilled on apply; content asserted after the handoff (§9; R-10).
8. PR 10 — Visual moves had no path to the pan (`FocusRow` answers false for Visual): a presenter reveal after every Visual move/follow; bounding-rectangle assertions (§11; R-12).
9. PR 10 — the board-menu witness could not detect wrong-card targeting: Toggle Mark from a non-first seated card / focused row changes only that one (§11.3; R-12).
10. Spec R-n citations shifted by one from PR 4 on, and PR 8 named contract 39 for the F6 ring: every mapping corrected, the W7-6 ring spec named, `W77RemediationDocsCensus` added to pin section ↔ contract.
11. PR 4 — the radio witness covered Tasks only: a per-surface block for Tasks, canvas and graph (§5.3; R-5).
12. PR 6 — `CitationPopoverShown { speech }` laundered host-composed text: core renders the sentence with the prefix rule, the host never prefixes, both corpus shapes and two mutations (§7.3; R-8).
13. (medium) Prospective tests read as existing witnesses: "Planned witness" labels and the preamble rule.
14. (medium) PR 7 both required and forbade a mac change: OD-6 and the §14 carve-out.

Fixes: the docs PR's second commit (3e5bd369).

**Round 2 (2026-09-23; same model, effort and base; branch scope).** Prompt: verify each round-1 disposition closes its finding; re-check the PR 7, 2, 8 and 10 redesigns against the code; enumerate anything new. Verdict: `needs-attention` — "the R-to-PR mappings are now correct; dispositions 3, 5, 6, 9, 11, 12, 13 substantively closed; 10 and 14 introduce sibling test gaps; 1, 2, 4, 7, 8 remain incomplete" (8 high, 1 medium). Dispositions — all taken, none refuted; AR-7 recorded:

1. PR 7 — `files_indexed` is not "changed" (a slow-path re-read of unchanged bytes counts) and Windows reports `ctime_ms = 0`: modified = committed content hash differs, `ScanDelta` path lists, the short-circuit kept as AR-7, facts for a touched-unchanged file and a `ctime = 0` provider (§8.2 e; R-9).
2. PR 7 — no producer for `Renamed`: unique hash-matched removed + created pair → `Renamed`, ambiguity → Deleted + Created, a real filesystem rename tested through the scan and listener (§8.2 f; R-9).
3. PR 7 — the funnel never reloads a clean tab (`InvalidateModifiedPath` marks staleness only): `WorkspaceViewModel.ReloadCleanTab` seam with caret/undo/dirty rules, exercised through the real rescan → event → tab path; `WorkspaceViewModel.cs` added to the touch list (§8.2 item 2; R-9).
4. PR 7 — coalescing could drop the Explicit reason: a pending-reason lattice (Explicit dominates Foreground) with both interleaving facts over parked scans (§8.2 item 2, §8.4; R-9).
5. OD-6 — mac could forward `filesIndexed` for both counts: a mac fact with distinct counts over payload and sentence (§8.3; OD-6).
6. PR 2 — the scope was partial state (`ClearFilterCommand` gated on text length; announcement key `(query, total)`; zero-result summary silent on the scope): Clear from `IsFilterActive` clearing both atomically, request identity `(query, scopeTag)`, the zero-result summary names the scope, three facts (§3.3; R-3).
7. PR 8 — block count accepted the loading placeholder and a pending request had no owner: readiness = the current model's applied projection; requests addressed to a generation and withdrawn on rebind / focus departure / unload; placeholder and no-steal facts; the journey asserts the target text and no loading notice (§9.2–9.3; R-10).
8. PR 10 — the reveal witnesses passed with a no-op renderer: an off-screen precondition with viewport-changed and destination-contained assertions, move and follow separately, `NextCard`/`PreviousCard` through the reveal, a hosted renderer fact where hostable (§11.3; R-12).
9. (medium) `W77RemediationDocsCensus` accepted contradictory duplicates: `TryAdd` rejection of duplicate `R-n` / PR sections / review records and exact key-set assertions.

Fixes: the docs PR's third commit (81113600).

**Round 3 (2026-09-23; same model, effort and base; branch scope).** Prompt: verify each round-2 disposition closes its finding; confirm no remaining design ships wrong behaviour, breaks a shipped contract or makes a claimed test unable to fail; enumerate anything new. Verdict: `needs-attention` — "round-2 dispositions 5 and 8 closed; 1, 3, 4, 6, 7 incomplete; 2 introduces unsafe rename identity; 9 leaves a sibling oracle gap" plus a locked-contract break and an ineffective PR 11 witness (11 high, 1 medium). Dispositions — all taken; one round-2 design reversed; AR-8 and AR-9 recorded:

1. PR 7 — hash-equal rename correlation could retarget a dirty tab to an unrelated file: **reversed** — no rename correlation, an external rename is Deleted + Created (AR-8), the same-hash delete/create with a dirty tab pinned (§8.2 f; R-9).
2. PR 7 — `files_indexed` still rendered as "new or changed": `ScanReport.files_changed` (hash-authoritative) is the rendered count on both hosts; the touched-but-unchanged vault facts on Windows (launch copy, foreground silence, explicit "No changes") and mac (§8.2 e, §8.3; R-9, OD-6).
3. PR 7 — streaming external changes through `notify_file_change` broke locked decision 05 (`:338–342`): the host reconciles the returned `ScanDelta` through `ReconcileScanDelta`, reusing `HandleFileChange`'s operations, not its channel (§8.2 g; R-9).
4. PR 7 — `complete` could derive from the walk flag alone: any per-file stat / read / index failure makes it false; `AnUnreadableFileMakesTheRescanIncomplete` through the Windows completion path (§8.2 h, §8.4; R-9).
5. PR 7 — the lattice facts did not prove Foreground ⊔ Foreground = Foreground: `TwoForegroundRequestsStayForeground` (§8.2 item 2, §8.4; R-9).
6. PR 7 — the clean-tab witness omitted baseline, caret, undo and reading re-projection: asserted through the real path (§8.2 item 2, §8.4; R-9).
7. PR 8 — the legacy apply-time claims still stole focus after departure: every claim goes through the one generation-owned request; the legacy claims are removed or gated (§9.2; R-10).
8. PR 8 — a pending landing announced the region early: tri-state landing, deferred region announcement on actual focus entry, an event-sequence fact around a delayed F6 (§9.2 item 4, §9.3; R-10).
9. PR 2 — scoped filtering could stay audibly scope-blind (`FileListCount` renders the count only; `AudioSummary` is a non-live status): `FileListCount { count, scope_tag }` core-rendered; rendered-speech facts (§3.3; R-3).
10. PR 11 — `WcMatrixEvidenceCensus` never reads the row's wording: a semantic census fact over row 2 with a wrong-wording mutation (§12.3; R-13).
11. Docs census — the mutable contracts doc was the only ownership oracle: the R → PR map is fixed in the census, and every heading-shaped line must match the strict parse (look-alike dashes cannot vanish).
12. (medium) §13.2 named narrower gates than `CONTRIBUTING.md`: the CONTRIBUTING gates are mandatory; the local FlaUI subset is AR-9.

Fixes: the docs PR's fourth commit (ba80b478).

**Round 4 (2026-09-23; same model, effort and base; branch scope).** Prompt: verify each round-3 disposition; confirm nothing left ships wrong behaviour, breaks a shipped or locked contract or makes a claimed test unable to fail; enumerate anything new; list accepted-risk candidates separately. Verdict: `needs-attention` — "round-3 dispositions 2–7 and 10 substantively close their findings; 1, 8, 9 create or leave sibling blockers; 11 and 12 partially incomplete below the bar" (6 high, 1 medium, 2 accepted-risk candidates). Dispositions — all taken; AR-10 recorded:

1. PR 2 — `(query, scopeTag)` as the announcement key would suppress a changed total (`SidebarAnnouncementEtiquetteTests`): stale identity `(query, scopeTag)`, de-duplication `(query, scopeTag, total)`, both facts (§3.3; R-3).
2. PR 2 — `FileListCount { scope_tag }` leaves `FileTreeSidebar.swift:4896` uncompilable: the mac producer passes `scopeTag: nil` in the same PR (§3.3; R-3).
3. PR 5 — the `PreviewCanExecute` half was untestable through a physical Tab: command-specific facts invoking `TabForward`/`TabBackward` directly, mutation of that hook alone (§6.3; R-6).
4. PR 6 — `EmbedPreviewUnavailable { reason: String }` laundered `Describe`'s copy: the semantic `EmbedUnresolvedReason` crosses to core and core renders per case (§7.3; R-8).
5. PR 7 — Created-before-Removed would defeat `ReseatMissingTabs` for a case-only rename: removals reconcile first; a case-insensitive-provider host fact (§8.2 g; R-9).
6. PR 8 — a delayed landing had no refusal path: a delayed `Refused` resumes the same ring traversal, two hosted facts (§9.2 item 4, §9.3; R-10).
7. (medium) PR 7 — "N items could not be read" mislabelled heterogeneous errors: "{errors} errors; results may be incomplete." (§8.3; R-9).
8. (candidate) the census had no loose-vs-strict check for `### PR n` headings: added.
9. (candidate) §13.2 limited `make ci` to core PRs: AR-10 records that non-Rust PRs leave it to CI's rust-tests job.

Fixes: the docs PR's fifth commit (bf32f067).

**Round 5 (2026-09-24; same model, effort and base; branch scope).** Prompt: verify each round-4 disposition; confirm nothing left ships wrong behaviour, breaks a shipped or locked contract or makes a claimed test unable to fail; enumerate anything new; candidates separately. Verdict: `needs-attention` — "round-4's PR 2, semantic embed reason, PR 7 ordering/error copy, reading delayed-refusal and AR-10 amendments are present; the PR 5 and census dispositions still do not discriminate their mutations" plus eight design gaps (6 high, 3 medium, 4 candidates). Dispositions — all taken; AR-11 recorded:

1. PR 1 — `ProviderFromPeer` is null before a client connects the peer (`ValidateConnected`, verified in dotnet/wpf): the window's HWND host provider is the event source, the peer provider the fallback, a first-frame fact records which was non-null, and a still-deaf process stops the PR for an owner decision (§2.3; R-1).
2. PR 3 — an empty first cell left a reading-table row unnamed (`CellText` returns ""): first non-empty cell, else a stable ordinal; empty / ragged / wholly-empty facts (§4.2–4.3; R-4).
3. PR 6 — the details sheet's unresolved branch had no honest variant: `CitationDetailsUnresolved { key }`, both branches pinned at the open site (§7.3, §7.5; R-8).
4. PR 7 — complete path lists across FFI broke locked decision 05:1492: the delta is read through bounded pages (`scan_delta_page`), counts on the report (§8.2 e; R-9).
5. PR 7 — `InvalidatePath` speaks "missing from disk" during reconciliation: silent reconciliation scope, only the final core-rendered outcome; whole-sequence facts for an open deletion and a case-only rename (§8.2 g; R-9).
6. PR 8 — the tri-state protocol covered only the reading arm and said nothing about repeated presses: one ring-owned request token for every asynchronous editor arm (reading, canvas, graph); a repeated F6 / Shift+F6 while pending cancels and restarts; interleaving facts (§9.2 item 4, §9.3; R-10).
7. (medium) PR 5 — `RoutedCommand.Execute` never consults `PreviewCanExecute`: the command facts go `CanExecute` then `Execute`; the key fact uses genuine input translation (§6.3; R-6).
8. (medium) PR 6 — the unavailable embed's body and UIA Name could stay host-composed: asserted equal to the core rendering, the `Describe` mutation fails them (§7.3, §7.5; R-8).
9. (medium) census — column-zero / single-space regexes and `R-10x`: CommonMark-tolerant loose detectors, the closing bold required, two-sided citation boundaries; mutations for a two-space heading and `R-10x`.
10. (candidate) PR 7 — a scan that throws before any report: `VaultRescanIncomplete { errors: 1 }` (§8.2 item 2; R-9).
11. (candidate) PR 6 — the inline conflict `Status` equals the announcement: asserted (§7.2; R-7).
12. (candidate) PR 5 — the key fact must use real input translation: stated (§6.3).
13. (candidate) census — fenced-code headings and negated prose: AR-11.

Fixes: the docs PR's sixth commit (de2c78eb).

**Round 6 (2026-09-24; same model, effort and base; branch scope).** Prompt: verify each round-5 disposition; confirm nothing left ships wrong behaviour, breaks a shipped or locked contract or makes a claimed test unable to fail; candidates separately; approve if only sub-bar items remain. Verdict: `needs-attention` — "round-5 dispositions for PR 1, PR 5, citation details, silent reconciliation, thrown scans and the docs census are substantively present; PRs 3, 6, 7, 8 can still satisfy their named tests while violating promised behaviour" (3 high, 1 medium, 3 candidates). Dispositions — all taken:

1. PR 7 — paging was declared, not exercised, and a page failing mid-reconciliation had no outcome: tiny-limit Rust facts, a multi-page host fact, `VaultRescanIncomplete` on a page-N failure with the rescan retryable (§8.2 e; R-9).
2. PR 6 — equality of body / name / rendering proves text, not provenance: a Roslyn construction-site census with a rewire-to-`Describe` mutation (§7.5; R-8).
3. PR 8 — the restart witness accepted a retained direction or a stale completion, and the W7-6 row was claimed amended: exact destination / direction / announcement / token assertions per arm, the stale token completed and ignored, the W7-6 ring spec amended by PR 8 (§9.2 item 4, §9.3; R-10).
4. (medium) PR 3 — "non-empty" accepted a constant or a display-index ordinal: exact distinct names from source order, stable across sort / re-realization / rebind (§4.3; R-4).
5. (candidate) §8.2 item 2 said both refuse and coalesce: refuses the initial scan / import / trash, folds a request during a rescan into the lattice.
6. (candidate) the raw-input mechanism for PR 5's key fact is named (§6.3).
7. (candidate) `ContractHeading` could take an interior bold as its closing delimiter: no `*` is allowed before the closing bold; mutation-tested.

Fixes: the docs PR's seventh commit (16984b72).

**Round 7 (2026-09-24; same model, effort and base; branch scope).** Prompt: verify each round-6 disposition; confirm nothing left ships wrong behaviour, breaks a shipped or locked contract or makes a claimed test unable to fail; candidates separately; approve if only sub-bar items remain. Verdict: `needs-attention` — "round 6's paging disposition is not recovery-safe, and PR 8 gives contradictory behaviour for an applied empty reading document; the other round-6 dispositions are substantively present" (2 high, 2 candidates). Dispositions — all taken:

1. PR 7 — a retry after a page-N failure could overwrite the unreconciled tail with a later scan's empty delta: the delta is an immutable, addressable generation retained until acknowledged, resumed from its failed cursor before any new scan, with idempotent page application and the five-change / page-2-failure fact (§8.2 e; R-9).
2. PR 8 — an applied content-empty note was both a valid landing and a terminal refusal: `Landed` at `ContentStart` with one announcement; `Refused` reserved for a failed `Focus()`, a load failure or a torn-down model; an applied-empty-note ring fact (§9.2 items 1 and 4, §9.3; R-10).
3. (candidate) `ContractHeading` anchored to the heading's `.**` followed by whitespace, so an interior bold after `(PR n, …)` cannot close it; mutation-tested.
4. (candidate) the repeated-press witnesses name their start and destinations concretely (the active tab item → `Pending(Editor)` → F6 lands the right pane content's first stop / Shift+F6 the active tab item; corrected in round 8: the locked D-2 order visits the tab bar between Files and the editor) (§9.2 item 4).

Fixes: the docs PR's eighth commit (27af3d2d).

**Round 8 (2026-09-24; same model, effort and base; branch scope).** Prompt: verify each round-7 disposition; confirm nothing left ships wrong behaviour, breaks a shipped or locked contract or makes a claimed test unable to fail; candidates separately; approve if only sub-bar items remain. Verdict: `needs-attention` — "three round-7 dispositions are substantively closed, but the repeated-press disposition still contradicts the locked W7-6 ring; no other blocker" (1 high, 3 candidates). Dispositions — all taken:

1. PR 8 — the pending-editor witnesses started in Files, but locked D-2 visits the tab bar between Files and the editor, and the reverse from the rail visits the right-pane content first: the scenarios start on the active tab item / the right pane content's first stop, and the journey asserts the intervening landings (§9.2 item 4, §9.3; R-10).
2. (candidate) the stale "never `Focus()` an empty surface" wording now says "unapplied surface" (§9.2 item 1).
3. (candidate) `LooseContractHeading` also catches confusable dashes (`R–10`, `R—10`).
4. (candidate) PR 7 acknowledges a generation only after the last page's effects, and orders removals before creations across the whole generation (§8.2 e; R-9).

Fixes: the docs PR's ninth commit (92d0cb77).

**Round 9 (2026-09-24; same model, effort and base; branch scope).** Prompt: verify each round-8 disposition; confirm nothing left ships wrong behaviour, breaks a shipped or locked contract or makes a claimed test unable to fail; candidates separately; approve if only sub-bar items remain. Verdict: `needs-attention` — "three round-8 dispositions close cleanly, but PR 7's new global ordering rule is not recovery-safe with its page-failure rule; no other blocker" (1 high, 3 candidates). Dispositions — all taken:

1. PR 7 — a host deferring a page-1 creation to honour removals-first would lose it when page 2 failed and the retry resumed at page 2: core pages every generation removal-first so the host consumes it in one bounded pass; the combined witness (removal on page 1, creation on page 2, page-2 failure, retry, both effects, acknowledgement afterward) and a Rust ordering fact (§8.2 e, §8.4; R-9).
2. (candidate) reverse-initiated Shift+F6→Shift+F6 / Shift+F6→F6 pending witnesses, including the hidden-pane and no-stop states (§9.3).
3. (candidate) the acknowledgement and cross-page witnesses named in §8.4's inventory.
4. (candidate) the loose contract-heading detector covers every Unicode dash (`\p{Pd}`).

Fixes: the docs PR's tenth commit (dc7d819b).

**Round 10 (2026-09-24; same model, effort and base; branch scope).** Prompt: verify each round-9 disposition; confirm nothing left ships wrong behaviour, breaks a shipped or locked contract or makes a claimed test unable to fail; candidates separately; approve if only sub-bar items remain. Verdict: `needs-attention` — "all four round-9 dispositions close, but five independent design/test gaps can still ship wrong behaviour or reject a correct implementation" (5 high, 3 candidates). Dispositions — all taken, three of them already delivered on the feature branches:

1. PR 7 — an incomplete walk pruned directories unconditionally (`session.rs:9607–9614`): neither files nor directories are pruned on an incomplete walk; a Rust survival fact and a host tree assertion (§8.2 h; R-9).
2. PR 7 — a retry recovering a generation then announced the new scan's empty delta as "No changes": one retry outcome covering recovered and new effects, the exact final sentence asserted (§8.2 e; R-9).
3. PR 3 — the type-name census rejected `note.md`: the delivered census flags only runtime type / record renderings (capitalised final segment, brackets, arity) with negative cases for dotted user content (§4.2 item 4; R-4).
4. PR 10 — the board's Shift+F10 route was never exercised: both keys on both targets, with a board-only mutation (§11.3; R-12).
5. PR 10 — the renderer's menu was compared only with its own plan: an independent per-target-kind inventory fact against contract 34 E17 (§11.3; R-12).
6. (candidate) §9.3's document oracle is the reading arm's; canvas and graph have their own (§9.3).
7. (candidate) the reverse origins are named: `RightPaneRail` for a no-stop leaf, `StatusBar` for a hidden pane (§9.2 item 4).
8. (candidate) retained generations live in a bounded session-side SQLite table (§8.2 e).

Also recorded this round from the delivered branches: PR 5's fence is an edge command binding per Tab command plus a direct-`Execute` guard, with no key hook and an IME-claimed Tab left to the IME (§6.2–6.3; R-6); PR 3's split panes are unnamed structural Panes and duplicate-sibling cases are named (§4.2 item 2); PR 8's repeated press starts from the held region only while the landing is still held (§9.2 item 4; R-10); OD-7 replay on advise for PR 1 (§2.3 item 6; R-1).

Fixes: the docs PR's eleventh commit (3ea0d39a).

**Round 11 (2026-09-24; same model, effort and base; branch scope).** Prompt: verify each round-10 disposition; evaluate OD-7's replay-on-advise design; confirm nothing left ships wrong behaviour, breaks a shipped or locked contract or makes a claimed test unable to fail; candidates separately; approve if only sub-bar items remain. Verdict: `needs-attention` — "round-10's behaviour fixes close; new blockers in OD-7's provider, transition and mutation design; PR 9's witness permits complete announcement suppression; PR 7 leaves retry count conflicts undefined; no other locked-contract or census-mapping break" (5 high, 2 candidates). Dispositions — all taken; AR-12 recorded:

1. OD-7 named the HWND host provider that delivered nothing: the source is the status element's connected peer provider, pinned at the replay boundary (§2.3 items 1 and 6; R-1).
2. OD-7 had no race-safe, finite transition: a monotonic UI-thread state machine that queues only while unadvised, polls during the launch phase, drains once on the first advise, expires after 30 s and keeps the last 16 lines; exact-count facts (§2.3 item 6; R-1, AR-12).
3. OD-7 made the gated-call mutation non-discriminating for the journey: the journey's mutation is replay disabled / the transition never flipping, with the exact launch tuple sequence; the census owns the gated-call shape (§2.3 item 6, §2.4; R-1).
4. PR 9 — "at most one" selection announcement accepted total suppression: exactly one for the replacement selection when the selected id disappears (§10.2; R-11).
5. PR 7 — conflicting per-path effects across generations were undefined: a net reduction against the last acknowledged state with one fact per conflicting pair (§8.2 e; R-9).
6. (candidate) the capitalised-final-segment heuristic rejected `README.MD` / `Part.One` / `Smith.Jones`: the discriminator is a chain rooted in the app's type namespaces, with uppercase dotted negatives (§4.2 item 4; R-4; relayed to PR 3).
7. (candidate) §2.3's manual-trait fallback reconciled with §2.5's acceptance and AR-1.

Fixes: the docs PR's twelfth commit (a684f653).

**Round 12 (2026-09-24; same model, effort and base; branch scope).** Prompt: verify each round-11 disposition; evaluate OD-7; confirm nothing left ships wrong behaviour, breaks a shipped or locked contract or makes a claimed test unable to fail; candidates separately. Verdict: `needs-attention` — "the PR 7, PR 9, namespace-discriminator and manual-fallback dispositions are present; OD-7 remains incomplete in two material ways" (2 high, 3 candidates). Dispositions — all taken:

1. OD-7 — production has a second dispatcher source (every `AccessibleDataGrid` constructs its own; the seam census required both sites): the state machine and provider are one per window, grids receive the window's announcer by injection, the census rejects independent production constructions, a hosted fact covers a grid line during the launch phase (§2.3 item 6; R-1; relayed to PR 1).
2. §2.4 still mandated the gated-call mutation for the journey: replaced by the replay-disabled / transition-never-flips mutation, the gated shape left to `AnnouncementSeamCensus` (§2.4).
3. (candidate, accepted) `W77RemediationDocsCensus` normalizes leading-zero identifiers and does not pin the displayed section ordinal — **AR-13**: the mapping and duplicate checks are what the census exists for; section ordinals are cosmetic.
4. (candidate, accepted) PR 9's journey permits zero final-selection announcements until its listener leg lands with PR 1 — **AR-14**: the hosted exact-one replacement-selection fact is the behavioural oracle; the journey leg is completed after PR 1 merges.
5. (candidate) §2.3's stale "must not depend on a peer connection" phrase reconciled with the queue-until-advised design.

Fixes: the docs PR's thirteenth commit (5f6c71dd).

**Round 13 (2026-09-24; same model, effort and base; branch scope).** Prompt: verify each round-12 disposition; confirm nothing left ships wrong behaviour, breaks a shipped or locked contract or makes a claimed test unable to fail; candidates separately. Verdict: `needs-attention` — "round 12 is closed; four new gaps" (4 high, 2 candidates). Dispositions — all taken:

1. PR 6 — one unresolved trigger fact could not catch a mapping collapsed to one case: parameterized facts per structured unresolved result plus an injected exception, asserting the exact variant and fields (§7.5; R-8; relayed to PR 6).
2. PR 7 — persisted generations had no restart protocol: an orphaned generation is discarded by the next open scan (no workspace state survives a restart), with a close/reopen fact (§8.2 e; R-9).
3. PR 7 — `Activated` skipped while any scan was in flight: only the open scan, an import or a trash blocks the route; an activation during a rescan joins the lattice; a route-level fact (§8.2 item 4; R-9).
4. PR 10 — the targeting witnesses were keyboard-only in the spec: the pointer facts PR 10 delivered (popup-service right-click on a non-seated card; empty board) are named (§11.3; R-12).
5. (candidate) the round-12 fix commit is recorded (5f6c71dd).
6. (candidate) the renderer inventory witness names its target kinds (§11.3).

Fixes: the docs PR's fourteenth commit (9f208810).

**Round 14 (2026-09-24; same model, effort and base; branch scope).** Prompt: verify each round-13 disposition; confirm nothing left ships wrong behaviour, breaks a shipped or locked contract or makes a claimed test unable to fail; candidates separately. Verdict: `needs-attention` — "the PR 6 and PR 10 round-13 dispositions close; both PR 7 dispositions remain defective" (2 high, 3 candidates). Dispositions — all taken:

1. R-9 still gated the foreground rescan on "no scan in flight": the clause now names the initial open scan, an import or a trash, and forwards an activation during a rescan into the coalescer (R-9).
2. PR 7 — the restart witness used an orderly close: it is now crash-equivalent (persist, abandon without close/dispose, reopen → discarded before scanning, no stale effects, no sentence), and the cleanup is required on the initial-open path (§8.2 e; R-9).
3. (candidate) R-12's witness summary names the popup-service pointer facts.
4. (candidate) the index mutations and the retained generation commit in one SQLite transaction, fault-injected at the boundary (§8.2 e; R-9).
5. (candidate) clearing a scoped filter by emptying the field is a witnessed route beside the Clear command (§3.3; relayed to PR 2).

Fixes: the docs PR's fifteenth commit (3810a6c4).

**Round 15 (2026-09-24; same model, effort and base; branch scope).** Prompt: verify each round-14 disposition; confirm nothing left ships wrong behaviour, breaks a shipped or locked contract or makes a claimed test unable to fail; candidates separately. Verdict: `needs-attention` — "round 14's crash-restart, atomicity, pointer-witness and filter-clear amendments are present; the foreground-rescan disposition remains incomplete; three further contradictions" (4 high, 2 candidates). Dispositions — all taken:

1. PR 7 — the ≥ 5 s cooldown could still drop an activation during an active rescan: the cooldown applies only to starting a run; an eligible activation during a rescan is always recorded; a second route-level fact with a < 5 s prior completion (§8.2 item 4; R-9).
2. PR 7 — `(kind, path)` rows cannot distinguish remove→create with the same hash from a different one: retained rows carry prior and new hashes and core reduces a recovered generation with the new delta into one net generation; the conflicting pairs are driven through the real retry path (§8.2 e; R-9).
3. PR 10 — the pointer witness kept the seat on the old card, contradicting locked contract 34 G2-12 (a context consumer seats its row silently before dispatching): a pointer request seats the hit card silently first; witness amended (§11.2–11.3; R-12; relayed to PR 10 on #1265).
4. PR 8 — withdrawal and delayed refusal had no precedence for a teardown coinciding with a rebind/close: one atomic terminal transition per token, stale later callbacks, a combined interleaving fact (§9.2 item 4; R-10; relayed to PR 8).
5. (candidate) the `DirectionalNavigation` mutation is `MenuBarCensus`'s, not the journey's (§5.3).
6. (candidate) PR 10 proves Applications-on-board and Shift+F10-on-outline action targeting; both keys share one production handler — **AR-15**: the reciprocal combinations are not separately witnessed.

Fixes: the docs PR's sixteenth commit (d873484c).

**Round 16 (2026-09-24; same model, effort and base; branch scope).** Prompt: verify each round-15 disposition; confirm nothing left ships wrong behaviour, breaks a shipped or locked contract or makes a claimed test unable to fail; candidates separately. Verdict: `needs-attention` — "round-15 dispositions 1, 3 and 4 close; disposition 2 leaves partial rescan recovery inconsistent; a PR 3 design exception violates R-4; no other blocker across PRs 1–2 and 4–11 or the locked contracts" (2 high, 4 candidates). Dispositions — all taken:

1. PR 7 — netting effects could strand a tab at an already-applied hash: a retry resumes and acknowledges the recovered generation first, then applies the new scan's generation in full; only the spoken outcome aggregates; the conflicting-pair facts assert the final host state (§8.2 e; R-9).
2. PR 3 — naming a wrapper beside an actionable child made two stops: wrappers stay structural, the child's Name is disambiguated, a one-stop hosted fact (§4.2 item 2; R-4; relayed to PR 3).
3. (candidate) the OD-7 transition predicate names the connected provider explicitly with a listener-present / provider-null fact (§2.3 item 6; PR 1 already delivers it).
4. (candidate) `W77RemediationDocsCensus` treats an indented non-PR H2 as a section boundary and requires the complete `(PR n, …)` owner clause.
5. (candidate) the mac divergence note in §7.3 cites R-8, not R-7.

Fixes: the docs PR's seventeenth commit (562d4f12).

**Round 17 (2026-09-24; same model, effort and base; branch scope).** Prompt: verify each round-16 disposition; confirm nothing left ships wrong behaviour, breaks a shipped or locked contract or makes a claimed test unable to fail; candidates separately. Verdict: `needs-attention` — "round 16 closes the PR 3 one-stop defect and adds the promised predicate/census/citation amendments; two high-risk state-lifetime contradictions remain in PRs 1 and 7" (2 high, 2 candidates). Dispositions — all taken:

1. PR 1 — expiry entered `Done` with the provider still null, so later lines could be skipped: a distinct `Expired` state keeps the queue-only-while-unready rule; a post-expiry provider-null fact (§2.3 item 6; R-1; relayed to PR 1).
2. PR 7 — acknowledging the recovered generation released the rows the speech reduction needs: two-phase acknowledgement (applied, then released after the reduction), cleanup on every path, a release-at-applied mutation (§8.2 e; R-9).
3. (candidate) `ContractHeading` requires the `(PR n, #issue …)` owner clause with an issue number.
4. (candidate) PR 10's round-4 record carries its verdict (approved on the corrected behaviour).

Fixes: the docs PR's eighteenth commit. Round 18: pending.

### PR 1 — #1244 announcements from launch

(pending)

### PR 2 — #1245 + #1250 Files sidebar

(pending)

### PR 3 — #1246 accessible names

(pending)

### PR 4 — #1247 arrows stay in the region

(pending)

### PR 5 — #1248 sheet keyboard fence

Branch `claude/w7-7-pr5-sheet-fence` (base 937dd3dd). Delivered: `SheetKeyboardFence` on all sixteen focus-scope overlays — one edge command binding per Tab command that refuses `TabForward`/`TabBackward` only when nothing inside answered and lets the key keep routing, plus a guard on a direct `Execute`; no key hook (controls that own Tab keep it; an IME-claimed Tab stays with the IME); `SheetFenceCensus` requiring the fence and `Cycle` in the element and style branches; contract 30 T4 amended. Follow-up #1259 (Ctrl+Z/Y/X reach the note behind a sheet).

**Round 1 (2026-09-24; codex `gpt-5.6-sol`, xhigh, base `origin/main`, branch scope; prompt: R-6, contract 30 TR-7, the settled register, exhaustive single pass).** `needs-attention`: (1) IME Tab trapped — refuted with WPF source (`TextServicesManager.PostProcessInput` hands the key to the IME only while `PreviewKeyDown` is unhandled; `Key.ImeProcessed` means mid-composition; outside composition Tab arrives plain) and pinned by `AnImeClaimedTabStaysWithTheImeAndNeverReachesTheEditor`; (2) the key hook preempted `AcceptsTab` fields and editing grid cells — taken by redesign to edge bindings with no key hook, `AcceptsTab` and grid-editor facts; (3) the style-branch census did not require `Cycle` — taken, mutation named at `WorkspaceTemplates.xaml:604`; (4) contract 30 T4 — amended. Mutations each failing only their fact: no edge, no guard, `ContinueRouting=false`, a refusal on the way down, an unconditional guard, a reinstated key hook, the IME normalization; all seven census mutations caught.
**Round 2.** `approve` — "No substantive R-6 defect found. All 16 scopes are fenced and cyclic … the edge binding is subtree-scoped, preserves control-owned Tab and WPF traversal, handles both directions and direct execution, and coexists with modal command suppression … the IME rationale matches WPF's `TextServicesManager` and `KeyboardNavigation`; normalizing `ImeProcessed` would be incorrect. No material findings."

### PR 6 — #1249 + #1251 failed-save sentence, popover outcomes

(pending)

### PR 7 — #1252 rescan on Refresh and foreground

(pending)

### PR 8 — #1253 reading surface is the editor stop

(pending)

### PR 9 — #1254 palette selection and speed

Branch `claude/w7-7-pr9-palette` (base 937dd3dd). Delivered: the `ItemsSource` swap inside the selection-sync guard and `IsSynchronizedWithCurrentItem="False"` (the stale selection was the view's); grouped virtualization (`IsVirtualizingWhenGrouping`, `ScrollUnit="Pixel"`) as the one change the profile justified — first keystroke 812/717 → 133/109 ms (medians, first open / reopen), steady state 203/242 → 54/58 ms (the ≤ 50 ms target missed by 4–8 ms, recorded under AR-4); `CommandPaletteResultsPresenter.cs`; the `PaletteQueryChangeTimed` diagnostic; `RecomputeScalesLinearlyInRowCount` (2.05–2.08; an injected quadratic step 2.84–2.88).

**Round 1 (2026-09-24; codex `gpt-5.6-sol`, xhigh, base `origin/main`, branch scope; prompt: R-11, contract 28 P7/P8/P10, AR-4, the settled register, exhaustive single pass).** `needs-attention`: (1) the guard source pin did not prove the handler honours the guard — taken: `TheGuardAloneKeepsASynchronizedSwapSilent` re-enables current-item synchronization so the guard is the only lock; mutations (early return deleted; swap outside the guard) fail it; (2) the announcement-stream journey observed no announcements while cited as evidence — taken: the evidence cells cite the hosted facts and state the journey's announcement leg is pending PR 1's listener (#1244), to be completed after PR 1 lands. From the spec's round 11: `AQueryThatRemovesTheSelectionAnnouncesItsReplacementThenTheCount` (mutations: all selection announcements suppressed; count scheduled first).
**Round 2.** `approve` — "the guarded swap, authoritative selection resync, keyboard/invocation paths, grouped virtualization, availability recheck, announcement ordering, diagnostics gate, and ratio witness support R-11; the steady-state timing shortfall is explained under AR-4, and the #1244 listener dependency is accurately scoped. No material findings."

### PR 10 — #1255 + #1256 canvas board arrows and menus

Branch `claude/w7-7-pr10-canvas` (base 937dd3dd). Delivered: board Down/Up through `SelectAdjacent`, Right/Left following connections, a presenter `RevealSeat` after every seat move (arrows, the palette's Next/Previous Card, Follow Connection, Enter/Exit Group, Trace Path, the connect-mode restoration) with the board's pan-to-contain; persistent menus on every outline row and on the renderer (keyboard → the seated card, pointer → the hit card, empty space → none) from one shared plan builder with a `Renderer` surface; contract 34 D15 amended, E17's renderer card delivered, G2D-12 lifted (the G2-12 table rows rewritten); follow-ups #1260 (focus loss after Delete rebuilds the outline) and #1261 (the board's accessibility tree is not refreshed after a redraw).

**Round 1 (2026-09-24; codex `gpt-5.6-sol`, xhigh, base `origin/main`, branch scope; prompt: R-12, OD-3, OD-5, contract 34 D15/E17/G2D-12, the settled register, exhaustive single pass).** `needs-attention` with no production-code finding: the live G2-12 consumer table still prohibited the renderer menu — taken (Renderer / node = the outline node rows; Renderer / connection = no rows).
**Round 2.** `needs-attention`: the pointer-menu fact bypassed the production route — taken: `ARightClickOnAnUnseatedCardOpensItsMenuAndActsOnItAlone` and `ARightClickOnEmptyBoardOpensNoMenu` enter through WPF's popup service with the hit position as the right-button-up handler does; mutations (target the seat; let empty space fall through) fail them.
**Round 3.** `approve` — "No blocker remains. R-12's navigation, reveal, persistent-menu targeting, discriminating witnesses, and contract 34 amendments are consistent with the production paths inspected. No material findings."
**Round 4 (after PR 0's round 15 found the pointer witness contradicting locked G2-12).** A right-click on a board card seats it silently before its menu opens; the pointer fact asserts the seat moved without an announcement (mutation: dispatch without seating → "the right-click did not seat card B"). `approve` — "No substantive R-12 defect or non-discriminating witness remains; navigation, reveal, menu targeting, persistent-menu behavior, and contract amendments align with the inspected production paths. No material findings." Codoki: approved on the final head.

### PR 11 — #1257 Connections leaf documentation

PR #1262 (`claude/w7-7-pr11-connections-doc`, base 937dd3dd). Delivered: chord row `windows.connections.openInNewTab` (Ctrl+Enter, Connections scope); the composed hint "Opens the note. Control Enter opens it in a new tab." through `NavigationHelp`; the checklist row corrected (Enter opens, Ctrl+Enter in a new tab, re-root only through the three row actions — the palette row `slate.graph.showConnections` never re-roots, contract 35 B2-3); the matrix leaf cell and contract 35 B-9 note.

**Round 1 (2026-09-24; codex `gpt-5.6-sol`, xhigh, base `origin/main`, branch scope; prompt: R-13, contract 39 N-1/N-2, the settled register, exhaustive single pass).** `needs-attention`: (1) the Ctrl+Enter witness bypassed the keyboard route — taken: keys flow `OnTreeKeyDown` → `TryHandleTreeKey` → `TryActivationFromKey(key, modifiers, out newTab)`, a 10-row theory and a view fact with injected modifiers; mutations caught: Control inverted, Return dropped, `newTab` ignored, no modifiers passed, KeyDown unsubscribed; (2) the hint witness did not prove chord-row provenance — taken: a `ConnectionsLeafCensus` fact pins `RowHint`'s single `NavigationHelp.Spoken("windows.connections.openInNewTab")` and rejects chord-table access and display-form text; mutations caught: `slate.editor.activateAtCaret`, a literal "Ctrl+Enter".
**Round 2.** `needs-attention` with no behaviour finding: the record's F14 row said `#TBD` and the checklist twin cell did not cite the new facts — taken ("Fixed in #1262"; `TheTreeOpensTheSelectedNoteInANewTabOnControlEnterAndInTheCurrentTabOnEnter` and `TheRowsNameIsCoresCopyAndItsHintIsItsActivations` cited).
**Round 3.** `approve` — "R-13 is truthfully implemented and documented, activation behaviour is unchanged, the composed hint uses the correct Connections-scoped chord row, the checklist evidence is discriminating, generated parity passes, and human-owned matrix cells are untouched. No material findings." Codoki: approved, no issues.
