# XD gap analysis — adversarial pre-lock review

**Date:** 2026-07-05/06. **Method:** an independent cold-start + citation-audit red-team read all six XD documents against this worktree (calibrated on the canvas program's gap analysis), verified every pinned code citation by grep/read, and answered seven targeted feasibility questions. 28 findings (4 blockers · 9 major · 15 minor); **every disposition below is already folded into the specs** — this file is the audit record, not a to-do list. Two format facts the review flagged as unpinned (default palette hexes, corner-radius clamp) were re-verified against excalidraw primary sources and added to the brief before lock.

## Findings & dispositions

| # | Sev | Finding | Disposition (folded) |
|---|-----|---------|----------------------|
| G1 | BLOCKER | Tab dedup is path-only (`activeGroupTab(forPath:)`, WorkspaceState.swift:121–123) — funnel would activate the wrapper's *markdown* tab instead of the viewer; "Open as Markdown" would re-activate the viewer. "Both tabs coexist" was impossible as specced. | xd2-1 rule 1: dedup becomes (kind, path)-aware; funnel prefers same-kind; Open-as-Markdown dedups on `(.markdown, path)`; coexistence test added. |
| G2 | BLOCKER | "No CommandRegistry enum" was false-as-read: a core `CommandRegistry` struct exists (commands.rs:106; FFI :3621) with `CommandSection` `#[repr(u8)]` (`Canvas = 8`); XD2-5's commands can't register without a section, and nothing owned `CommandSection::Excalidraw`. Help-table drift (#526) also needed wiring. | Decision 10 rewritten (FL-15's correction was about command-*ID* enums); `CommandSection::Excalidraw = 9` + bindings regen assigned to XD2-1 (first registering PR; canvas R16 precedent); help-table drift entry assigned to XD3-1 with the help file. FL program's own self-contradiction noted, not inherited. |
| G3 | BLOCKER | Raw `.excalidraw` files invisible to quick-open: palette lists `FileFilter::MarkdownAndCanvas` (session.rs:3375; AppState.swift:3221). "Palette needs no special cases" was false (canvas gap R7 déjà vu). | Decision 9 corrected; xd2-1 rule 2 adds `FileFilter::OpenableDocuments` (additive) + palette call-site switch. |
| G4 | BLOCKER | `ImageSources` referenced in xd0-4 but never defined, and bytes alone can't drive LaTeX/URL/missing placeholders; a cold dev on XD0-4 would invent the contract. | xd0-4 pins `ImageSources = BTreeMap<String, ImageSource>` with `Bytes/Latex/Url/Missing`; session populates it (xd0-5 + xd1-1 shared helper); renderer stays I/O-free. |
| G5 | MAJOR | Geometry/reading-order contradiction: xd0-4's "every non-deleted element in `geometry`" vs census 3 vs reading order excluding bound labels — both couldn't pass. | `geometry` = reading-order entries exactly; labels fold into container geometry; property + census wording fixed. |
| G6 | MAJOR | Deleted count couldn't reach the description (dropped into a warnings vec `derive` never sees). | `ExcalidrawScene.deleted: u32` added (mirrors `skipped`). |
| G7 | MAJOR | Default palette "pinned table" pinned nowhere. | Verified from `packages/common/src/colors.ts` and added to brief §1 (stroke/background/canvas picks); xd0-3 rule 5 now cites it and requires exact match, no nearest-color math. |
| G8 | MAJOR | Embed end-to-end budget (< 250 ms "resolve→rendered") unowned and criterion-unmeasurable (includes Swift rasterization). | Decision 12 re-scoped to core resolve→SVG (xd0-5 bench); rasterization recorded in xd3 E2E. |
| G9 | MAJOR | Two tiering mechanisms (program's "5k banner" vs xd2-4's 1,500-visible AX tier), one unowned. | Decision 12: the 1,500-visible AX tier **is** the mechanism; 5k = best-effort render. |
| G10 | MAJOR | `ExcalidrawOutlineRow` lacked color_label/size/split counts that xd2-2/xd2-5 phrase from; no `excalidraw_where_am_i` despite "canvas mirror". | Row fields added (xd0-5); the missing where-am-I pinned as deliberate divergence (mode/zoom are UI state; Swift composes readback). |
| G11 | MAJOR | "Canvas external-reload seam if one exists" was answerable now: none exists (reload is mutation-driven, CanvasDocument.swift:164). | Resolved: no auto-reload in v1; Refresh only; help documents it (xd2-1 rule 4, xd3). |
| G12 | MAJOR | `EmbedDisclosure` has no header actions/badges; xd1-2 presented extension as reuse. | xd1-2 rule 4 now owns the header-accessory slot extension (existing variants byte-identical; no `.isHeader` trait — audit-#194 preserved). |
| G13 | MAJOR | `excalidraw_kind` via index only: stale/unscanned wrappers would open as markdown (decision-9 violation); index-vs-bytes disagreement unhandled. | xd0-5: bounded frontmatter-sniff fallback when no properties row; xd2-1: stale-positive ⇒ viewer tab in degraded state with Open as Markdown. |
| G14 | MINOR | WrapperError surface incomplete (MissingDrawing, from_utf16, Err-to-degraded owner). | xd0-2: MissingDrawing ⇒ degraded scene + warning; from_utf16 ⇒ DecompressFailed; session named as converter. |
| G15 | MINOR | `InvalidKind` wasn't a `VaultError` variant. | Pinned to existing `InvalidPath { path, reason }` (lib.rs:121) — no new variant. |
| G16 | MINOR | CRLF tolerance unpinned (LF-exact heading regexes). | xd0-2 rule 2 normalizes `\r\n`; CRLF corpus fixture. |
| G17 | MINOR | xd1-2's "either order" for Open Drawing contradicted the wave gates. | XD1-2 ships without it; XD2-1 adds it to the accessory slot. |
| G18 | MINOR | `seed` unparsed though decision 3 requires XD-E1 to slot in renderer-internally. | `ElementCommon.seed: i64` parsed now (0 = unseeded, brief §5). |
| G19 | MINOR | `theme_dark` parsed, consumed by no rule. | xd0-1 rule 7: exposed, doesn't alter v1 rendering; help documents. |
| G20 | MINOR | Adaptive-radius clamp formula unpinned. | Verified `getCornerRadius` verbatim (packages/element/src/utils.ts) → brief §1 + xd0-4 rule 3. |
| G21 | MINOR | `RelativePosition` ninths' reference frame unpinned. | "Ninths of the union bounds of non-deleted elements" (model-side). |
| G22 | MINOR | Geometry-skip rule scoped to non-linear, but linear elements also require x/y. | xd0-1 rule 4: x/y required for all; width/height for non-linear only. |
| G23 | MINOR | Dangling `frameId` unpinned. | Unframed + `DanglingFrame` warning (canvas G8 precedent); census 2 covers. |
| G24 | MINOR | Placeholder "naming their URL" vs "⟨host⟩" inconsistency. | Decision 6 + xd0-4 rule 8 aligned to host (full URL in description/links; canvas R5 precedent). |
| G25 | MINOR | "Scan/save benches" — no save bench exists. | Reworded: scan bench + save-path tests (§XD-D, xd0-5). |
| G26 | MINOR | Anchor warning asymmetry (wrapper warned, raw stripped silently). | xd1-1 rule 1: `AnchorIgnored` in both forms. |
| G27 | MINOR | `excalidraw_kind` absent from the FFI inventory yet consumed by xd2-1. | Moved into xd0-5's Wave-1 inventory; xd2 baseline updated. |
| G28 | MINOR | §XD-B promised "real plugin-written files" the corpus can't guarantee. | Softened: format-faithful corpus + the executably-proven fixture; genuine captures added when available. |

## Citation audit (verified correct, this worktree)

embeds.rs `EmbedResolution`:26 (variant order), `MAX_EMBED_DEPTH`:22, `IMAGE_EXTENSIONS`:108, `looks_like_image`:113, `infer_mime`:125 · session.rs `resolve_embed`:1183/`resolve_embed_at_depth`:1192 (image short-circuit doc :1176), `read_attachment`:782 + `large_attachment_refuse_bytes` (config :74, default 50 MiB :118), `outgoing_links`:1167, `files_with_property_key`:1533, `classify_path`:3889, `FileFilter`:138/:3375 · slate-uniffi/src/lib.rs `EmbedResolution`:1970, `resolve_embed`:719, canvas handle API :3672/:4511/:4542/:4547/:4557/:4567/:4581, `VaultError` variants :114–141 · diagram.rs :53/:55–63/:144 · frontmatter.rs :137/:465 · AppState.swift `openFile`:799 + canvas arm :807, quick-open call site :3221 · WorkspaceModel.swift `EditorItem`:42 (+ kind decode :54–63), WorkspaceState.swift :121–123/:238, WorkspaceStore.swift :311 · AppState+Canvas.swift :26–45/:107–115 · CanvasDocument.swift `reloadAfterMutation`:164 · commands.rs :38/:106, FFI :3621, SlateCommandsTests.swift drift suite (~:255 focus routing, ~:1024 help-table #526) · AccessibleDataGrid v2 (#519) confirmed as CanvasTableView's substrate · serde_json `preserve_order` workspace-wide (root Cargo.toml:75) · SwiftDraw pinned (Package.swift:42–49) + MermaidView pattern · CLI surface = search/read/list/links/properties (slate-cli/src/main.rs:209–213) · census/bench conventions + BENCHMARKS.md · docs/help/canvas.md + canvas at_smoke_checklist.md exist as templates · greenfield confirmed (no `excalidraw` code in tree) · wave gates/issue counts consistent across 00 ↔ xd0–xd3.

**Round 2 (adequacy):** after folding, the specs were re-read for cross-doc consistency (decision numbers, census names, FFI inventory, wave gates) — consistent. Residual risks are the two the brief flags as unverifiable today (which excalidraw release ships the master-branch binding shapes; historical plugin chunk widths), both already mitigated by tolerant-parse rules rather than assumptions.
