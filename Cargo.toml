[workspace]
resolver = "2"
members = [
    "crates/yana-core",
]

[workspace.package]
version = "0.0.1"
edition = "2021"
license = "MIT"
authors = ["Cory Joseph <cj@startingblind.com>"]
repository = "https://github.com/coryj627/YANA"
rust-version = "1.75"

[workspace.dependencies]
pulldown-cmark = { version = "0.10", default-features = false }
thiserror = "1"

[profile.dev]
# Faster incremental builds during development.
opt-level = 0
debug = true

[profile.release]
opt-level = 3
lto = "thin"
strip = "symbols"
